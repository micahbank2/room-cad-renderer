---
phase: 23-label-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RoomTabs.tsx
  - src/components/SidebarProductPicker.tsx
  - src/components/ProductLibrary.tsx
  - src/components/PropertiesPanel.tsx
  - src/data/roomTemplates.ts
  - src/canvas/fabricSync.ts
  - src/App.tsx
  - src/components/TemplatePickerDialog.tsx
  - src/components/WelcomeScreen.tsx
  - src/components/AddRoomDialog.tsx
  - src/components/Sidebar.tsx
  - src/components/Toolbar.tsx
  - src/components/StatusBar.tsx
  - src/components/ProductForm.tsx
  - src/components/AddProductModal.tsx
  - src/components/RoomSettings.tsx
  - src/components/FramedArtLibrary.tsx
  - src/components/WallSurfacePanel.tsx
  - src/components/CeilingPaintSection.tsx
  - src/components/PaintSection.tsx
  - src/components/FloorMaterialPicker.tsx
  - src/components/CustomElementsPanel.tsx
  - src/components/WainscotPopover.tsx
  - src/components/WainscotLibrary.tsx
  - src/components/SwatchPicker.tsx
  - src/components/HelpModal.tsx
  - src/components/onboarding/OnboardingOverlay.tsx
  - src/components/help/helpContent.tsx
  - src/components/help/helpIndex.ts
  - src/components/help/HelpSearch.tsx
  - src/components/onboarding/onboardingSteps.ts
  - src/three/ThreeViewport.tsx
autonomous: false
requirements:
  - LABEL-02
  - LABEL-01

must_haves:
  truths:
    - "All toolbar, sidebar, status bar, and modal labels display spaces instead of underscores"
    - "Product names, wall segment IDs, and material names display with spaces (no .replace(/\\s/g, '_') transforms)"
    - "No code identifiers, CSS class names, data-testid attributes, icon names, or store keys are changed"
  artifacts:
    - path: "src/components/RoomTabs.tsx"
      provides: "Dynamic transform removed"
      contains: "room.name.toUpperCase()"
    - path: "src/components/PropertiesPanel.tsx"
      provides: "Dynamic transform removed"
      contains: "product?.name?.toUpperCase()"
    - path: "src/components/Toolbar.tsx"
      provides: "Static labels with spaces"
      contains: "FLOOR PLAN"
    - path: "src/components/Sidebar.tsx"
      provides: "Static labels with spaces"
      contains: "ROOM CONFIG"
  key_links:
    - from: "src/components/RoomTabs.tsx"
      to: "room.name"
      via: ".toUpperCase() without .replace()"
      pattern: "\\.toUpperCase\\(\\)(?!\\.replace)"
    - from: "src/components/ProductLibrary.tsx"
      to: "p.name"
      via: ".toUpperCase() without .replace()"
      pattern: "\\.toUpperCase\\(\\)(?!\\.replace)"
---

<objective>
Remove all underscore characters from user-facing labels across the entire app, covering both dynamic transforms that insert underscores and static hardcoded label strings.

Purpose: Jessica sees clean, readable labels ("FLOOR PLAN", "WALL SURFACE", "ADD PRODUCT") instead of code-style underscore formatting ("FLOOR_PLAN", "WALL_SURFACE", "ADD_PRODUCT").
Output: ~129 label edits across 30 files. Build passes, no visual underscores remain in any UI surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-label-cleanup/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dynamic transforms and data-layer labels</name>
  <files>
    src/components/RoomTabs.tsx,
    src/components/SidebarProductPicker.tsx,
    src/components/ProductLibrary.tsx,
    src/components/PropertiesPanel.tsx,
    src/data/roomTemplates.ts,
    src/canvas/fabricSync.ts
  </files>
  <action>
Fix the 4 dynamic transforms that actively insert underscores into every product/room name displayed. These are the highest-impact changes (LABEL-02).

1. **src/components/RoomTabs.tsx** (~line 32): Change `room.name.toUpperCase().replace(/\s/g, "_")` to `room.name.toUpperCase()`. Remove the `.replace()` call entirely.

2. **src/components/SidebarProductPicker.tsx** (~line 50): Change `{p.name.toUpperCase().replace(/\s/g, "_")}` to `{p.name.toUpperCase()}`.

3. **src/components/ProductLibrary.tsx** (~line 159): Change `{p.name.toUpperCase().replace(/\s/g, "_")}` to `{p.name.toUpperCase()}`.

4. **src/components/PropertiesPanel.tsx** (~line 134): Change `{product?.name?.toUpperCase().replace(/\s/g, "_") ?? "PRODUCT"}` to `{product?.name?.toUpperCase() ?? "PRODUCT"}`.

Then fix the data-layer and canvas labels:

5. **src/data/roomTemplates.ts** (~line 38): Change `label: "LIVING_ROOM . 16 x 20 ft"` to `label: "LIVING ROOM . 16 x 20 ft"`. Only this one template has an underscore; the others (BEDROOM, KITCHEN, BLANK) are single words.

6. **src/canvas/fabricSync.ts** (~line 842): Change `"MISSING_PRODUCT"` to `"MISSING PRODUCT"`.

**DO NOT CHANGE:**
- Any `data-testid` attributes
- Any Material Symbols icon names (e.g., `upload_file`, `grid_view`)
- Any CSS class names or store keys
- Any `.toUpperCase()` calls that do NOT have `.replace(/\s/g, "_")` chained — those are already correct
  </action>
  <verify>
    <automated>cd /Users/micahbank/room-cad-renderer && grep -rn '\.replace(/\\s/g, "_")' src/ && echo "FAIL: dynamic transforms still exist" || echo "PASS: no underscore transforms remain"</automated>
  </verify>
  <done>All 4 dynamic .replace(/\s/g, "_") transforms removed. LIVING_ROOM template label and MISSING_PRODUCT canvas label fixed. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all static underscore labels across component files</name>
  <files>
    src/App.tsx,
    src/components/TemplatePickerDialog.tsx,
    src/components/WelcomeScreen.tsx,
    src/components/AddRoomDialog.tsx,
    src/components/Sidebar.tsx,
    src/components/Toolbar.tsx,
    src/components/StatusBar.tsx,
    src/components/ProductForm.tsx,
    src/components/AddProductModal.tsx,
    src/components/ProductLibrary.tsx,
    src/components/SidebarProductPicker.tsx,
    src/components/PropertiesPanel.tsx,
    src/components/RoomSettings.tsx,
    src/components/FramedArtLibrary.tsx,
    src/components/WallSurfacePanel.tsx,
    src/components/CeilingPaintSection.tsx,
    src/components/PaintSection.tsx,
    src/components/FloorMaterialPicker.tsx,
    src/components/CustomElementsPanel.tsx,
    src/components/WainscotPopover.tsx,
    src/components/WainscotLibrary.tsx,
    src/components/SwatchPicker.tsx,
    src/components/HelpModal.tsx,
    src/components/onboarding/OnboardingOverlay.tsx,
    src/components/help/helpContent.tsx,
    src/components/help/helpIndex.ts,
    src/components/help/HelpSearch.tsx,
    src/components/onboarding/onboardingSteps.ts,
    src/three/ThreeViewport.tsx
  </files>
  <action>
Replace all static underscore labels with space-separated versions across ~28 files (LABEL-01). Use the complete inventory in 23-RESEARCH.md as the exact checklist — every "Before → After" row in that file is a required edit.

Work through each file systematically. The full list per file is in the research doc. Key examples by file:

- **App.tsx**: `SHOW_SIDEBAR` → `SHOW SIDEBAR`, `NATURAL_OAK` → `NATURAL OAK`, `BRUSHED_STEEL` → `BRUSHED STEEL`, `RECENT_IMPORTS` → `RECENT IMPORTS`, `BUILDING_SCENE...` → `BUILDING SCENE...`
- **TemplatePickerDialog.tsx**: `BLANK_ROOM` → `BLANK ROOM`, `LIVING_ROOM` → `LIVING ROOM`, `Choose_A_Template` → `Choose A Template`, `UPLOAD_IMAGE` → `UPLOAD IMAGE`, `REMOVE_IMAGE` → `REMOVE IMAGE`, `ESC_TO_CLOSE` → `ESC TO CLOSE`
- **WelcomeScreen.tsx**: `OBSIDIAN_CAD` → `OBSIDIAN CAD`, `DESIGN_YOUR_SPACE` → `DESIGN YOUR SPACE`, `CREATE_FLOOR_PLAN` → `CREATE FLOOR PLAN`, `UPLOAD_FLOOR_PLAN` → `UPLOAD FLOOR PLAN`, `SYSTEM_STATUS: READY` → `SYSTEM STATUS: READY`
- **Sidebar.tsx**: `COLLAPSE_SIDEBAR` → `COLLAPSE SIDEBAR`, `ROOM_CONFIG` → `ROOM CONFIG`, `SYSTEM_STATS` → `SYSTEM STATS`, `SQ_FT` → `SQ FT`, `FLOOR_MATERIAL` → `FLOOR MATERIAL`, `3_INCH` → `3 INCH`, `6_INCH` → `6 INCH`, `1_FOOT` → `1 FOOT`, `PRODUCT_LIBRARY` → `PRODUCT LIBRARY`
- **StatusBar.tsx**: Fix the template literal `{activeTool.toUpperCase()}_TOOL` → `{activeTool.toUpperCase()} TOOL`, plus `WALK_MODE` → `WALK MODE`, `ORBIT_MODE` → `ORBIT MODE`
- **WallSurfacePanel.tsx**: 11 labels including `WALL_SURFACE` → `WALL SURFACE`, `SIDE_{s}` → `SIDE {s}`, `COPY_TO_SIDE_{...}` → `COPY TO SIDE {...}`, `CROWN_MOLDING` → `CROWN MOLDING`, `WALL_ART` → `WALL ART`
- **SwatchPicker.tsx**: 11 labels including `RECENTLY_USED` → `RECENTLY USED`, `HUE_FILTER` → `HUE FILTER`, `F&B_CATALOG` → `F&B CATALOG`, `MY_COLORS` → `MY COLORS`
- **WainscotLibrary.tsx**: 9 labels including `WAINSCOT_LIBRARY` → `WAINSCOT LIBRARY`, dimension labels `PANEL_W` → `PANEL W`, etc.
- **Help content** (helpContent.tsx, helpIndex.ts, HelpSearch.tsx, onboardingSteps.ts): All underscore labels in help text, search index, and onboarding steps

Refer to the FULL research file for the complete per-file inventory. Every row listed there must be changed.

**CRITICAL — DO NOT CHANGE any of the following:**
- `data-testid="ADD_ROOM"` or `data-testid="ROOM_TABS"` — these are test identifiers
- `data-onboarding` attribute values
- Material Symbols icon names inside `<span className="material-symbols-outlined">`: `upload_file`, `grid_view`, `add_box`, `cloud_upload`, `image_not_supported`
- CSS class names: `glass-panel`, `cad-grid-bg`, `ghost-border`, `accent-glow`, etc.
- Store keys, variable names, import paths
- Template literal ID prefixes like `room_${uid()}`, `prod_${uid()}`, `pp_${uid()}`
- `PRODUCT_CATEGORIES` array values

After all edits, run `npm run build` to verify TypeScript compilation and bundling succeed.
  </action>
  <verify>
    <automated>cd /Users/micahbank/room-cad-renderer && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All ~120 static underscore labels replaced with spaces across 28 files. Build passes. No code identifiers, CSS classes, icon names, data-testid attributes, or store keys were modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed all underscores from user-facing labels across the entire app — 4 dynamic transforms eliminated, ~120 static labels updated, 1 data-layer label and 1 canvas label fixed. Total: ~129 edits across 30 files.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Check the **Welcome Screen**: should read "OBSIDIAN CAD", "DESIGN YOUR SPACE", "CREATE FLOOR PLAN", "UPLOAD FLOOR PLAN", "SYSTEM STATUS: READY"
    3. Create or open a project. Check the **Toolbar**: "FLOOR PLAN", "2D PLAN", "3D VIEW" — no underscores
    4. Check the **Sidebar**: "ROOM CONFIG", "SYSTEM STATS", "SQ FT", "FLOOR MATERIAL", "PRODUCT LIBRARY" — all spaces
    5. Open the **Product Library** (full panel): "YOUR LIBRARY", "PRODUCT REGISTRY", "ADD PRODUCT", "SEARCH ASSETS..." — all spaces
    6. Add a product with a multi-word name (e.g., "Velvet Sofa"). Verify it displays as "VELVET SOFA" (with space), NOT "VELVET_SOFA"
    7. Check the **Status Bar** at bottom: should show "SELECT TOOL" or "WALL TOOL" (with space before TOOL)
    8. Switch to **3D VIEW**: check "WALK MODE" label (no underscore)
    9. Open **Room Settings**: "WIDTH FT", "LENGTH FT", "HEIGHT FT"
    10. Open the **Help Modal**: "GETTING STARTED", "LIBRARY & 2D", "3D & WALK & ROOMS" — check section labels and help text
    11. Spot-check any wall surface panels, wainscot, ceiling paint, swatch picker panels for remaining underscores
    12. Verify Material Symbols icons still render correctly (the icon names like `upload_file` were NOT changed)
  </how-to-verify>
  <resume-signal>Type "approved" if all labels display cleanly with spaces, or describe any remaining underscores found</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn '\.replace(/\\s/g, "_")' src/` returns no results (all dynamic transforms removed)
- `npm run build` succeeds without errors
- Visual sweep confirms no underscore labels in any UI surface
- Material Symbols icons still render (icon names preserved)
- data-testid attributes unchanged
</verification>

<success_criteria>
1. Zero `.replace(/\s/g, "_")` calls remain in the codebase
2. All ~129 static and dynamic underscore labels display with spaces
3. `npm run build` passes
4. No code identifiers, CSS classes, icon names, or test attributes were modified
5. Human visual verification confirms clean labels across all views
</success_criteria>

<output>
After completion, create `.planning/phases/23-label-cleanup/23-01-SUMMARY.md`
</output>
