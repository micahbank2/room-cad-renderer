---
plan: "82-02"
phase: "82-right-panel-inspector-v1-21"
type: execute
wave: 2
autonomous: true
gap_closure: false
objective: "Add per-entity tab system (Phase 72 <Tabs> primitive) to each inspector per D-05. IA-04 surface."
files_modified:
  - src/components/inspectors/WallInspector.tsx
  - src/components/inspectors/ProductInspector.tsx
  - src/components/inspectors/CustomElementInspector.tsx
  - src/components/inspectors/CeilingInspector.tsx
  - tests/RightInspector.tabs.test.tsx
task_count: 3
requirements:
  - IA-04
depends_on:
  - "82-01"
must_haves:
  truths:
    - "Selecting a wall surfaces Geometry / Material / Openings tabs with Geometry active by default."
    - "Selecting a product surfaces Dimensions / Material / Rotation tabs with Dimensions active by default."
    - "Selecting a ceiling surfaces Geometry / Material tabs with Geometry active by default."
    - "Selecting a custom element surfaces Dimensions / Label / Material tabs with Dimensions active by default."
    - "Selecting a stair renders flat — no tab list (D-04)."
    - "Multi-select renders flat — no tab list (D-05)."
    - "Switching between entities resets activeTab to the first tab (D-03)."
    - "Phase 79 window preset chip row remains reachable (nested in Openings tab → expanded opening body — Plan 82-03 will lift it)."
  artifacts:
    - path: "src/components/inspectors/WallInspector.tsx"
      provides: "Tabs wrapper for Geometry / Material / Openings; SavedCameraButtons as trailing row."
    - path: "src/components/inspectors/ProductInspector.tsx"
      provides: "Tabs wrapper for Dimensions / Material / Rotation; SavedCameraButtons as trailing row."
    - path: "src/components/inspectors/CustomElementInspector.tsx"
      provides: "Tabs wrapper for Dimensions / Label / Material; SavedCameraButtons as trailing row."
    - path: "src/components/inspectors/CeilingInspector.tsx"
      provides: "Tabs wrapper for Geometry / Material; SavedCameraButtons as trailing row."
    - path: "tests/RightInspector.tabs.test.tsx"
      provides: "RED → GREEN unit specs asserting tab order, default tab, and D-03 reset on entity swap."
  key_links:
    - from: "src/components/inspectors/WallInspector.tsx"
      to: "src/components/ui/Tabs.tsx"
      via: "import Tabs / TabsList / TabsTrigger / TabsContent"
      pattern: "from \"@/components/ui/Tabs\""
    - from: "src/components/inspectors/ProductInspector.tsx"
      to: "src/components/ui/Tabs.tsx"
      via: "import Tabs / TabsList / TabsTrigger / TabsContent"
      pattern: "from \"@/components/ui/Tabs\""
    - from: "src/components/inspectors/CustomElementInspector.tsx"
      to: "src/components/ui/Tabs.tsx"
      via: "import Tabs / TabsList / TabsTrigger / TabsContent"
      pattern: "from \"@/components/ui/Tabs\""
    - from: "src/components/inspectors/CeilingInspector.tsx"
      to: "src/components/ui/Tabs.tsx"
      via: "import Tabs / TabsList / TabsTrigger / TabsContent"
      pattern: "from \"@/components/ui/Tabs\""
    - from: "tests/RightInspector.tabs.test.tsx"
      to: "src/components/RightInspector.tsx"
      via: "RTL render of RightInspector exercising tab order assertions"
      pattern: "RightInspector"
---

<objective>
Wrap each non-stair inspector's body in `<Tabs>` (from `src/components/ui/Tabs.tsx`, the Phase 72 primitive). Per-entity tab list comes from D-05:

- **Wall:** Geometry / Material / Openings
- **Product:** Dimensions / Material / Rotation
- **Custom element:** Dimensions / Label / Material
- **Ceiling:** Geometry / Material
- **Stair:** flat (D-04 — no tabs)
- **Bulk select:** untabbed (D-05 — verbatim)

D-03 tab reset on new selection: each inspector body is wrapped in `<div key={entityId}>` so React unmounts the old Tabs and mounts a fresh one with default activeTab. Simpler than `useEffect(() => setActiveTab(default), [entityId])` and semantically equivalent.

D-05 camera-buttons-as-trailing-row: the existing `<SavedCameraButtons>` block renders BELOW the tab body, NOT inside any tab. Always visible.

Purpose: deliver the IA-04 surface — when Jessica clicks a wall, she sees `Geometry / Material / Openings` as the first thing, with Geometry active. When she clicks a product, `Dimensions / Material / Rotation` with Dimensions active. Read-only Position rows the audit flagged as "Collapse by default" stay collapsed inside the relevant tab's PanelSection.

Output: 4 inspector files updated. 1 new RTL test asserting each entity's tabs render in the documented order. Phase 79 invariants untouched (Openings tab still mounts `<OpeningsSection>` verbatim; the window preset chip row stays inside the opening expanded body until Plan 82-03 lifts it).
</objective>

<context>
@.planning/phases/82-right-panel-inspector-v1-21/82-CONTEXT.md
@.planning/phases/82-right-panel-inspector-v1-21/82-RESEARCH.md
@./CLAUDE.md

@src/components/ui/Tabs.tsx
@src/components/inspectors/WallInspector.tsx
@src/components/inspectors/ProductInspector.tsx
@src/components/inspectors/CustomElementInspector.tsx
@src/components/inspectors/CeilingInspector.tsx

<interfaces>
Phase 72 <Tabs> primitive API (from src/components/ui/Tabs.tsx):

```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/Tabs";

<Tabs value={activeTab} onValueChange={setActiveTab}>
  <TabsList>
    <TabsTrigger value="geometry">Geometry</TabsTrigger>
    <TabsTrigger value="material">Material</TabsTrigger>
  </TabsList>
  <TabsContent value="geometry">...</TabsContent>
  <TabsContent value="material">...</TabsContent>
</Tabs>
```

- `<TabsContent>` returns null when inactive (DOM-free, no perf cost).
- Sliding pill animates via `layoutId={`tab-pill-${groupId}`}` — useId() ensures groupId is stable per Tabs instance.
- Reduced-motion aware (springTransition(reduced)).
- role=tablist / role=tab / role=tabpanel for a11y.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tab-system unit tests</name>
  <files>tests/RightInspector.tabs.test.tsx</files>
  <behavior>
    Per IA-04 Wave 0 gap: write the failing tests BEFORE adding tabs. Tests assert that for each entity type, the inspector renders the documented tab list in the documented order, with the documented default tab active.

    Test cases (each as a separate `it(...)`):

    1. **Wall tabs render in order:** select a wall → assert `getAllByRole("tab")` returns elements with text content `["Geometry", "Material", "Openings"]` in that order. Assert the first tab has `aria-selected="true"` (Geometry is default).

    2. **Product tabs render in order:** select a product → tabs are `["Dimensions", "Material", "Rotation"]`. Dimensions is default.

    3. **Custom element tabs render in order:** select a placed custom element → tabs are `["Dimensions", "Label", "Material"]`. Dimensions is default.

    4. **Ceiling tabs render in order:** select a ceiling → tabs are `["Geometry", "Material"]`. Geometry is default.

    5. **Stair has no tabs (D-04):** select a stair → `queryAllByRole("tab")` returns []. Assert StairSection inputs (e.g., Width, Rise, Run) are rendered directly.

    6. **Bulk select has no tabs (D-05):** select 2 walls → `queryAllByRole("tab")` returns []. Assert the bulk-actions header text "Bulk actions" is visible.

    7. **Tab reset on entity swap (D-03):** select wall A → click "Openings" tab → select wall B → assert `Geometry` is active (not `Openings`).

    Use the existing test harness pattern from `tests/PropertiesPanel.*.test.tsx`: mount `<RightInspector productLibrary={[]} viewMode="2d" />` inside a minimal cadStore/uiStore setup.

    These tests MUST fail at this point (no tabs in any inspector yet — Task 2/3 makes them pass).
  </behavior>
  <action>
    Create tests/RightInspector.tabs.test.tsx using the existing PropertiesPanel test patterns as a reference. Each `it()` block:
    1. Sets up cadStore with a single entity (wall / product / etc.).
    2. Sets up uiStore with that entity's id in selectedIds.
    3. Renders RightInspector.
    4. Queries by role="tab" or queryByText for tab labels.
    5. Asserts the order and the default-active tab.

    Run the suite — specs MUST fail with errors like "expected ['Geometry', 'Material', 'Openings'], received []" because Plan 82-01 inspectors render flat.

    Commit message: `test(82-02): add RED tab-system specs for RightInspector`.
  </action>
  <verify>
    <automated>npm run test -- tests/RightInspector.tabs.test.tsx</automated>

    Suite MUST be RED: each tab-presence assertion fails. Stair + bulk specs may pass (they assert tabs DON'T render — trivially true today).
  </verify>
  <done>
    - tests/RightInspector.tabs.test.tsx exists with 7 `it()` blocks.
    - Suite runs and fails for the tab-presence specs (RED).
    - Test commit lands on `gsd/phase-82-inspector` branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tabs to WallInspector, ProductInspector, CustomElementInspector, CeilingInspector</name>
  <files>src/components/inspectors/WallInspector.tsx, src/components/inspectors/ProductInspector.tsx, src/components/inspectors/CustomElementInspector.tsx, src/components/inspectors/CeilingInspector.tsx</files>
  <behavior>
    For each non-stair inspector, wrap the body in `<Tabs>` with the D-05 tab list. Per D-03, the outer wrapper `<div>` is keyed on entity id so React unmounts/remounts on selection change → activeTab snaps back to the default.

    **WallInspector** (Plan 82-01 lifted L365-421):
    - Tabs: Geometry (default) / Material / Openings.
    - **Geometry:** Dimensions PanelSection (Length / Thickness / Height EditableRows) + Position PanelSection (Start / End read-only Rows, `defaultOpen={false}`).
    - **Material:** `<WallSurfacePanel />` (wainscoting + crown molding).
    - **Openings:** `<OpeningsSection wall={wall} />`. (Phase 79 chip row stays nested inside OpeningEditor for this plan — Plan 82-03 lifts it.)
    - **Trailing row (outside tabs):** `<SavedCameraButtons kind="wall" ... />`.

    **ProductInspector** (lifted from L423-528 + L626-636):
    - Tabs: Dimensions (default) / Material / Rotation.
    - **Dimensions:** Dimensions PanelSection (Width / Depth / Height OR Size: Unset) + "Set dimensions (ft)" inputs for catalog products without dims + Reset size button when overrides exist + Position PanelSection (`defaultOpen={false}`).
    - **Material:** Material PanelSection (Category, library Material) + Finish PanelSection (Phase 69 per-placement MaterialPicker).
    - **Rotation:** rotation degrees Row + RotationPresetChips.
    - **Trailing row (outside tabs):** `<SavedCameraButtons kind="product" ... />`.

    **CustomElementInspector** (lifted from L530-624):
    - Tabs: Dimensions (default) / Label / Material.
    - **Dimensions:** Dimensions PanelSection + Position PanelSection (`defaultOpen={false}`) + Rotation PanelSection + Reset size button when overrides exist.
    - **Label:** `<LabelOverrideInput pce={pce} catalogName={ce.name} />`.
    - **Material:** face selector section + per-face MaterialPicker.
    - **Trailing row (outside tabs):** `<SavedCameraButtons kind="custom" ... />`.

    **CeilingInspector** (lifted from L311-363):
    - Tabs: Geometry (default) / Material.
    - **Geometry:** CeilingDimInput width + depth + Height read-only Row + Vertices read-only Row + Reset size when overrides exist.
    - **Material:** `<MaterialPicker surface="ceiling" .../>`.
    - **Trailing row (outside tabs):** `<SavedCameraButtons kind="ceiling" ... />`.

    Implementation pattern:

    ```tsx
    import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/Tabs";
    import { useState } from "react";

    export function WallInspector({ wall, viewMode }: Props) {
      const [activeTab, setActiveTab] = useState<"geometry" | "material" | "openings">("geometry");
      // ... existing store selectors ...

      return (
        // Key on wall.id so a new wall selection forces a fresh Tabs mount.
        // D-03: tab resets to first tab on every new selection.
        <div className="space-y-2" key={wall.id}>
          <div className="font-sans text-xs text-foreground">
            WALL SEGMENT {wall.id.slice(-4).toUpperCase()}
          </div>
          <Tabs value={activeTab} onValueChange={(v) => setActiveTab(v as typeof activeTab)}>
            <TabsList>
              <TabsTrigger value="geometry">Geometry</TabsTrigger>
              <TabsTrigger value="material">Material</TabsTrigger>
              <TabsTrigger value="openings">Openings</TabsTrigger>
            </TabsList>
            <TabsContent value="geometry">
              {/* PanelSection Dimensions + PanelSection Position */}
            </TabsContent>
            <TabsContent value="material">
              <WallSurfacePanel />
            </TabsContent>
            <TabsContent value="openings">
              <OpeningsSection wall={wall} />
            </TabsContent>
          </Tabs>
          <SavedCameraButtons kind="wall" id={wall.id} ... />
        </div>
      );
    }
    ```

    Tab values lowercase (`"geometry"`, `"material"`, ...). Tab labels mixed case per D-09 (`Geometry`, `Material`, ...).

    Constraints:
    - Do NOT alter any commit-path logic inside PanelSections being moved into tabs.
    - Phase 79 invariant: `<OpeningsSection>` mounts inside the `Openings` tab content unchanged.
    - The `activeFace` useState in CustomElementInspector stays at function scope (it's per-Material-tab state shared across faces, not tab-scoped).
    - The "Delete element" button is owned by RightInspector — per-inspector files do NOT render it.
  </behavior>
  <action>
    Edit each of the 4 inspector files. Use the WallInspector skeleton above as the pattern.

    Apply D-03 via `<div key={entityId}>` wrapping the inspector body. This is the cleanest reset mechanism — alternative `useEffect(() => setActiveTab(default), [entityId])` is semantically equivalent but more code.

    For each inspector:
    1. Add `useState<"...">("default")` for activeTab.
    2. Wrap the body in `<div key={entityId} className="space-y-2">`.
    3. Insert `<Tabs>` after the entity-name header.
    4. Render `<TabsList>` with the D-05 tab order.
    5. Render `<TabsContent>` for each tab, lifting the existing JSX into the right slot.
    6. Render `<SavedCameraButtons>` AFTER the `</Tabs>` close (trailing row).

    Run the RED tests from Task 1 after the 4 files are updated. They MUST go GREEN.

    Commit: `feat(82-02): wrap entity inspectors in Tabs per D-05`. Single commit covering all 4 inspector files is acceptable since they implement the same contract.
  </action>
  <verify>
    <automated>npm run test -- tests/RightInspector.tabs.test.tsx && npm run typecheck && npm run test</automated>

    Task 1's specs MUST now be GREEN. Full vitest suite passes.

    Manual smoke:
    1. `npm run dev`, click a wall → 3 tabs visible: Geometry / Material / Openings. Geometry active.
    2. Click Material tab → wainscot + crown molding pickers.
    3. Click Openings tab → openings list. Expand a window → preset chips still visible inside opening body.
    4. Click a product → Dimensions / Material / Rotation tabs.
    5. Click a ceiling → Geometry / Material tabs.
    6. Click a custom element → Dimensions / Label / Material tabs.
    7. Click a stair → no tabs.
    8. Select 2 walls → bulk actions, no tabs.
    9. Click wall A → Openings tab → click wall B → Geometry active (D-03).
  </verify>
  <done>
    - All 4 inspector files use `<Tabs>` with the D-05 tab list.
    - Stair stays flat. Bulk stays untabbed.
    - Phase 79 chip row still works (nested in OpeningEditor — not lifted yet).
    - Existing data-testids preserved.
    - All vitest tests pass including the new tab specs.
    - TypeScript clean.
    - Manual smoke passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cosmetic cleanup — remove redundant single-PanelSection wrapping inside tabs</name>
  <files>src/components/inspectors/WallInspector.tsx, src/components/inspectors/ProductInspector.tsx, src/components/inspectors/CustomElementInspector.tsx, src/components/inspectors/CeilingInspector.tsx</files>
  <behavior>
    Audit each tab's body. If a tab contains a SINGLE PanelSection whose label matches the tab label semantically (e.g., `Geometry` tab containing `Dimensions` section) → drop the PanelSection wrapper. If multiple PanelSections → keep both for sub-grouping.

    Position rows (read-only debug data per audit) MUST stay collapsed-by-default inside `<PanelSection defaultOpen={false}>` — preserves audit finding without removing data.

    Polish pass. No behavior change.
  </behavior>
  <action>
    For each tab in each inspector, decide per the rule:

    - WallInspector / Geometry: Dimensions + Position. Keep both PanelSections; Position stays `defaultOpen={false}`.
    - WallInspector / Material: only `<WallSurfacePanel />`. No PanelSection to drop.
    - WallInspector / Openings: `<OpeningsSection>`. No PanelSection to drop.
    - ProductInspector / Dimensions: Dimensions + Position + Reset button + set-dimensions block. Keep both PanelSections; Position `defaultOpen={false}`.
    - ProductInspector / Material: Material + Finish (semantically distinct). Keep both.
    - ProductInspector / Rotation: single Rotation PanelSection. **Drop the wrapper** — tab IS the rotation label.
    - CeilingInspector / Geometry: single Dimensions PanelSection. **Drop the wrapper**.

    Commit: `refactor(82-02): drop redundant single-PanelSection wrappers inside tabs`.
  </action>
  <verify>
    <automated>npm run test && npm run typecheck && npm run build</automated>

    Manual smoke: each tab body looks cleaner — no double-labeled chrome. Position rows remain collapsed by default. Tests pass.
  </verify>
  <done>
    - Each non-stair inspector reviewed for redundant PanelSection nesting.
    - Single-section tabs drop wrapper; multi-section tabs keep both.
    - Position rows remain collapsed-by-default.
    - All tests pass. Build clean.
  </done>
</task>

</tasks>

<verification>
**Overall phase 82-02 checks:**

1. `npm run typecheck` — no errors.
2. `npm run test` — full vitest suite passes, including tests/RightInspector.tabs.test.tsx (GREEN).
3. `npm run build` — production build succeeds.
4. **Manual visual diff:** open app, click each entity type, confirm tabs appear with D-05 order. Defaults match D-05.
5. **Tab reset (D-03):** click wall A → switch to Openings tab → click wall B → confirm Geometry is active.
6. **Phase 79 invariant preserved:** click wall → Openings tab → expand window opening → preset chips render → click "Picture" chip → `past.length` increments by exactly 1. `tests/windowTool.preset.test.tsx` still passes.
7. **Scope:** Plan 82-02 does NOT address window-opening sub-selection (that's Plan 82-03 — IA-05).
8. `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" verify key-links .planning/phases/82-right-panel-inspector-v1-21/82-02-PLAN.md` — passes (after Plan 82-01 lands the inspector files this plan edits).
</verification>

<rollback>
Each task is reversible via `git restore` on the modified files. The tab system is purely additive JSX wrapping; removing it returns inspectors to Plan 82-01's flat-scroll state.
</rollback>
