---
phase: 47-room-display-modes-display-01
plan: 03
type: execute
wave: 1
depends_on: [47-01]
files_modified:
  - src/components/Toolbar.tsx
autonomous: true
requirements: [DISPLAY-01]
must_haves:
  truths:
    - "Toolbar renders 3 segmented-control buttons (NORMAL / SOLO / EXPLODE) when viewMode === '3d' || viewMode === 'split'"
    - "Buttons are NOT rendered when viewMode === '2d' (D-01 gate matches Walk/Orbit + camera-preset precedent)"
    - "Each button has data-testid 'display-mode-{normal|solo|explode}', aria-label matching mode name (NORMAL/SOLO/EXPLODE), and aria-pressed reflecting active state"
    - "Active button styling uses D-09 verbatim classes: bg-accent/10, text-accent, border-accent/30"
    - "Inactive buttons use text-text-dim hover:text-accent-light + border-transparent"
    - "Icons are lucide-react LayoutGrid (NORMAL), Square (SOLO), Move3d (EXPLODE) — D-09 + D-33 (lucide-only for new chrome)"
    - "Tooltips render D-09 verbatim strings: 'All rooms render together' / 'Only the active room renders' / 'Rooms separated along X-axis'"
    - "Clicking a button calls useUIStore.setDisplayMode(mode) and updates aria-pressed instantly (D-07)"
    - "Zero arbitrary spacing values in the new code (D-34: per-file rule for Toolbar.tsx — zero p-[Npx]/m-[Npx]/gap-[Npx]/rounded-[Npx])"
    - "Vitest test at src/components/__tests__/Toolbar.displayMode.test.tsx passes"
    - "E2E test at e2e/display-mode-cycle.spec.ts passes (depends on Plan 02 for store + driver)"
---

<objective>
Wire the Toolbar UI for Phase 47. Adds the 3-button segmented control (NORMAL / SOLO / EXPLODE) that drives `useUIStore.setDisplayMode` (added in Plan 02). Turns Wave 0 component test + Playwright E2E spec GREEN.

Purpose: Surface the displayMode state in the user-visible toolbar — completes the user-facing slice of DISPLAY-01.
Output: 1 file modified — `src/components/Toolbar.tsx`.

Wave 1 parallelism: This plan and 47-02 BOTH depend on 47-01 (Wave 0 RED scaffolding) but NOT on each other. Plan 02 implements the state + 3D rendering; this plan implements the toolbar UI. The component test in this plan needs Plan 02's `setDisplayMode` to exist on uiStore — coordinate Wave 1 merge order or run plans serially if executors choose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-room-display-modes-display-01/47-CONTEXT.md
@.planning/phases/47-room-display-modes-display-01/47-RESEARCH.md
@.planning/phases/47-room-display-modes-display-01/47-VALIDATION.md
@.planning/phases/47-room-display-modes-display-01/47-01-PLAN.md
@.planning/phases/47-room-display-modes-display-01/47-02-PLAN.md
@src/components/Toolbar.tsx
@src/stores/uiStore.ts
@CLAUDE.md

<interfaces>
From src/stores/uiStore.ts (Plan 02 adds — REQUIRED for component to compile):
```typescript
displayMode: "normal" | "solo" | "explode";
setDisplayMode: (mode: "normal" | "solo" | "explode") => void;
```

D-09 button anatomy (verbatim from 47-CONTEXT.md):
| Mode    | Label   | Icon (lucide)    | Tooltip                              |
|---------|---------|------------------|--------------------------------------|
| normal  | NORMAL  | `LayoutGrid`     | "All rooms render together"          |
| solo    | SOLO    | `Square`         | "Only the active room renders"       |
| explode | EXPLODE | `Move3d`         | "Rooms separated along X-axis"       |

Active button class (D-09 verbatim): `bg-accent/10 text-accent border-accent/30`.
Inactive button class: `text-text-dim hover:text-accent-light border-transparent`.

Existing precedent (Toolbar.tsx lines 107–125 + 147–162):
- Walk/Orbit toggle gated on `viewMode === "3d" || viewMode === "split"` — same gate for displayMode buttons.
- Camera preset cluster (lines 147–162) is the active-state model. Reuse `aria-pressed`, `data-testid`, `flex items-center justify-center rounded-sm transition-colors duration-150` shell. NOTE: presets use `bg-accent/20`; D-09 specifies `bg-accent/10` for displayMode — this DIFFERENCE is intentional (distinguishes feature categories visually).

Placement: After camera-preset cluster (Toolbar.tsx ~line 167), before the document-title center slot (line 169). Left-to-right hierarchy: view tabs → walk/orbit → camera presets → **display mode** → [center: title] → [right: undo/save/export].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Toolbar 3-button segmented control + viewMode gate</name>
  <files>src/components/Toolbar.tsx</files>
  <read_first>
    - src/components/Toolbar.tsx (full file — find existing lucide-react imports, viewMode prop/store source, walk/orbit gate at line 107, camera preset cluster lines 130–167, the position immediately before line 169 where new buttons land)
    - src/components/__tests__/Toolbar.displayMode.test.tsx (the contract — make these pass)
    - src/stores/uiStore.ts (confirm displayMode + setDisplayMode added by Plan 02; subscriber pattern matches existing displayMode-adjacent fields like cameraMode)
    - .planning/phases/47-room-display-modes-display-01/47-CONTEXT.md (D-01, D-09 verbatim button anatomy)
    - .planning/phases/47-room-display-modes-display-01/47-RESEARCH.md § Focus Area 1 (Toolbar Button Anatomy — research-recommended structure with placement notes)
    - CLAUDE.md § Design System § D-34 spacing rule (Toolbar.tsx is in the per-file enforcement list — zero arbitrary `p-[Npx]`/`m-[Npx]`/`gap-[Npx]`/`rounded-[Npx]`)
  </read_first>
  <behavior>
    - Add lucide-react imports: `LayoutGrid`, `Square`, `Move3d`. If existing lucide import line exists, augment it; otherwise add a new import.
    - Add `DISPLAY_MODES` config constant at module scope (above the component) holding the 3 entries with `id`, `label`, `Icon`, `tooltip`.
    - Inside the Toolbar component:
      - Subscribe `displayMode = useUIStore(s => s.displayMode)` and `setDisplayMode = useUIStore(s => s.setDisplayMode)`.
      - Render the segmented control gated on `(viewMode === "3d" || viewMode === "split")`. Place the new block immediately AFTER the camera-preset cluster, BEFORE the document-title center slot.
      - Each button uses `data-testid="display-mode-{id}"`, `aria-label="{LABEL}"` (uppercase mode label per test expectations), `aria-pressed={isActive}`. Inner content: `<Icon size={14} strokeWidth={1.5} />` + `<span>{label}</span>`.
      - Active class: `bg-accent/10 text-accent border-accent/30` (D-09 verbatim).
      - Inactive class: `text-text-dim hover:text-accent-light border-transparent`.
      - Wrap each button in the existing `Tooltip` primitive from Toolbar.tsx (verify component name + import path — likely `@/components/ui/Tooltip` or similar; mirror existing camera-preset Tooltip usage). Pass tooltip text from config.
    - D-34 spacing constraint: use canonical Tailwind spacing utilities only. Acceptable: `gap-1`, `px-2`, `py-1`, `mr-6`, `rounded-sm`. NOT acceptable: `p-[8px]`, `gap-[6px]`, etc.
    - D-07: clicking a button calls `setDisplayMode(id)` synchronously — no debounce, no animation hook.
  </behavior>
  <action>
1. Open `src/components/Toolbar.tsx`. Find existing lucide-react import block (or any existing imports from `lucide-react`):
```bash
grep -n "from \"lucide-react\"" src/components/Toolbar.tsx
```
Augment to include the 3 new icons:
```typescript
import { /* existing icons, */ LayoutGrid, Square, Move3d } from "lucide-react";
```
If no lucide-react import exists yet (unlikely — Phase 33 added them), add a fresh import line.

2. Above the Toolbar component (after imports, before `export function Toolbar(...)` or `function ToolPalette(...)` — match the file's actual export pattern), add the config constant:

```typescript
/** Phase 47 D-09: display-mode segmented-control config. */
const DISPLAY_MODES = [
  { id: "normal" as const,  label: "NORMAL",  Icon: LayoutGrid, tooltip: "All rooms render together" },
  { id: "solo" as const,    label: "SOLO",    Icon: Square,     tooltip: "Only the active room renders" },
  { id: "explode" as const, label: "EXPLODE", Icon: Move3d,     tooltip: "Rooms separated along X-axis" },
];
```

3. Inside the Toolbar component body, add subscriptions next to existing useUIStore subscriptions (e.g. next to `cameraMode`/`pendingPresetRequest` subscriptions):

```typescript
const displayMode = useUIStore((s) => s.displayMode);
const setDisplayMode = useUIStore((s) => s.setDisplayMode);
```

4. In the JSX, locate the camera-preset cluster (lines ~130–167). Immediately AFTER its closing tag and BEFORE the document-title slot (~line 169), insert:

```tsx
{(viewMode === "3d" || viewMode === "split") && (
  <div
    className="flex items-center gap-1 mr-6"
    role="group"
    aria-label="Display mode"
  >
    {DISPLAY_MODES.map(({ id, label, Icon, tooltip }) => {
      const isActive = displayMode === id;
      return (
        <Tooltip key={id} content={tooltip} placement="bottom">
          <button
            data-testid={`display-mode-${id}`}
            onClick={() => setDisplayMode(id)}
            aria-label={label}
            aria-pressed={isActive}
            className={`flex items-center justify-center gap-1 px-2 py-1 rounded-sm font-mono text-sm transition-colors duration-150 border ${
              isActive
                ? "bg-accent/10 text-accent border-accent/30"
                : "text-text-dim hover:text-accent-light border-transparent"
            }`}
          >
            <Icon size={14} strokeWidth={1.5} />
            <span>{label}</span>
          </button>
        </Tooltip>
      );
    })}
  </div>
)}
```

**Notes on integration:**
- Verify `Tooltip` import already exists in Toolbar.tsx; if its props are `title` instead of `content`, or `position` instead of `placement`, match the existing usage. The verbatim tooltip strings ("All rooms render together", etc.) MUST appear in the rendered DOM (component test asserts via `document.body.innerHTML.toContain`).
- If the Toolbar uses `viewMode` from a prop (not store), keep using the prop. If from store, replace the gate accordingly.
- D-34 enforcement: confirm zero `p-[Npx]`/`m-[Npx]`/`gap-[Npx]`/`rounded-[Npx]` in the inserted block. The block uses only canonical utilities.

5. Run component test: `npm run test -- --run src/components/__tests__/Toolbar.displayMode.test.tsx`. All 9 assertions must pass. Common failure modes:
- If test uses `<Toolbar viewMode="3d" />` and the actual Toolbar takes viewMode from a different prop name → adjust the test wrapper helper, not the component (the test was a Plan 01 RED scaffold — minor calibration permitted as long as the assertion contract stays intact).
- If `aria-label="NORMAL"` test fails because Toolbar's existing buttons used different aria conventions → confirm new buttons use the literal label string as aria-label (D-09).

6. Run E2E (this plan + Plan 02 must both have shipped for E2E to pass): `npx playwright test e2e/display-mode-cycle.spec.ts`. All 3 tests must pass.

7. Regression: existing Toolbar tests (camera presets, walk/orbit, undo, save, export) continue to pass. Run: `npm run test -- --run src/components/__tests__`.

8. Spacing audit (per CLAUDE.md D-34): `grep -E '(p|m|gap|rounded)-\[' src/components/Toolbar.tsx | wc -l` returns 0. If non-zero, the violations are pre-existing — do NOT add new ones with this plan.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/__tests__/Toolbar.displayMode.test.tsx && grep -q "LayoutGrid.*Square.*Move3d\|Square.*LayoutGrid\|Move3d.*Square\|Move3d.*LayoutGrid" src/components/Toolbar.tsx && grep -q "DISPLAY_MODES" src/components/Toolbar.tsx && grep -q "All rooms render together" src/components/Toolbar.tsx && grep -q "Only the active room renders" src/components/Toolbar.tsx && grep -q "Rooms separated along X-axis" src/components/Toolbar.tsx && grep -q "bg-accent/10" src/components/Toolbar.tsx && grep -q "border-accent/30" src/components/Toolbar.tsx && grep -q "data-testid={\`display-mode-" src/components/Toolbar.tsx && grep -q "setDisplayMode(id)" src/components/Toolbar.tsx && grep -q "viewMode === \"3d\" || viewMode === \"split\"" src/components/Toolbar.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/__tests__/Toolbar.displayMode.test.tsx` exits 0 (all 9 tests pass)
    - `grep -c "LayoutGrid\|Square\|Move3d" src/components/Toolbar.tsx` returns ≥ 4 (1 import + 3 config refs)
    - `grep -c "DISPLAY_MODES" src/components/Toolbar.tsx` returns ≥ 2 (declaration + map)
    - `grep -c "All rooms render together" src/components/Toolbar.tsx` returns 1 (D-09 verbatim)
    - `grep -c "Only the active room renders" src/components/Toolbar.tsx` returns 1
    - `grep -c "Rooms separated along X-axis" src/components/Toolbar.tsx` returns 1
    - `grep -c "bg-accent/10" src/components/Toolbar.tsx` returns ≥ 1 (D-09 active class — distinct from preset bg-accent/20)
    - `grep -c "border-accent/30" src/components/Toolbar.tsx` returns ≥ 1 (D-09 active class — may also exist in preset cluster)
    - `grep -c "display-mode-normal\|display-mode-solo\|display-mode-explode\|display-mode-\\\${id}" src/components/Toolbar.tsx` returns ≥ 1
    - `grep -c "useUIStore.*displayMode\|useUIStore.*setDisplayMode" src/components/Toolbar.tsx` returns ≥ 2
    - `grep -E '(p|m|gap|rounded)-\[' src/components/Toolbar.tsx | wc -l` returns 0 (D-34 — zero arbitrary spacing in Toolbar.tsx)
    - `grep -c "material-symbols-outlined" src/components/Toolbar.tsx` is unchanged from baseline (no NEW Material Symbol icons added — D-33 enforces lucide-only for new chrome; existing 8-file Material Symbol allowlist preserved)
    - `npx tsc --noEmit` exits 0
    - `npx playwright test e2e/display-mode-cycle.spec.ts` exits 0 (all 3 tests pass; depends on Plan 02 also being merged)
    - Regression: `npm run test -- --run src/components/__tests__` shows pre-existing pass count + new Toolbar.displayMode test (no Toolbar regressions)
  </acceptance_criteria>
  <done>3-button segmented control rendered in Toolbar, gated on viewMode 3d/split. Buttons drive setDisplayMode synchronously (D-07). D-09 verbatim styling. D-34 spacing audit clean. Component test + E2E test GREEN.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/components/__tests__/Toolbar.displayMode.test.tsx` exits 0
- `npm run test -- --run src/components/__tests__` regression: pre-existing pass count + new test
- `npx tsc --noEmit` exits 0
- `npx playwright test e2e/display-mode-cycle.spec.ts` exits 0 (cycle test + persistence test + garbage-value test)
- `grep -E '(p|m|gap|rounded)-\[' src/components/Toolbar.tsx | wc -l` returns 0 (D-34)
- Visual smoke: `npm run dev` → switch to 3D view → 3 segmented buttons appear after camera-preset cluster → click EXPLODE → rooms separate along X-axis (Plan 02 wired this) → click SOLO → only active room visible → click NORMAL → all rooms re-converge.
- 2D-only view (`viewMode === "2d"`) does NOT render the buttons.
</verification>

<success_criteria>
DISPLAY-01 acceptance fully observable end-to-end with this plan + Plan 02:
- "Toolbar gains a NORMAL/SOLO/EXPLODE display-mode selector" ✓
- "Visible only when viewMode === '3d' || 'split'" ✓ (D-01 gate)
- "Three buttons grouped together" ✓ (segmented control with role="group")
- "Active button styling matches existing Toolbar pattern" ✓ (D-09 bg-accent/10 + text-accent + border-accent/30)
- "lucide-react icons LayoutGrid/Square/Move3d" ✓ (D-09 + D-33 lucide-only enforcement)
- "Tooltip text matches D-09 verbatim" ✓
- "Switching modes is INSTANT" ✓ (D-07 — no animation hook on click)
- "displayMode persists across reload" ✓ (Plan 02 localStorage; verified by E2E test 2)
- "displayMode in uiStore is single source of truth" ✓ (button click → setDisplayMode → all consumers re-render)

Phase 47 complete after this plan + Plan 02 + Plan 01 ship.
</success_criteria>

<output>
After completion, create `.planning/phases/47-room-display-modes-display-01/47-03-SUMMARY.md` capturing:
- File modified (1: src/components/Toolbar.tsx)
- Test results: Toolbar.displayMode.test.tsx + display-mode-cycle.spec.ts both GREEN
- D-34 spacing audit result (0 arbitrary values)
- D-33 icon policy audit result (no new Material Symbols added; lucide LayoutGrid/Square/Move3d added correctly)
- Visual smoke confirmation (3 buttons appear in 3D + split, hidden in 2D)
- Phase 47 acceptance status — DISPLAY-01 fully observable end-to-end
</output>
