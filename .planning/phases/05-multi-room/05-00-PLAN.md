---
phase: 05-multi-room
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/cadStore.multiRoom.test.ts
  - tests/snapshotMigration.test.ts
  - tests/roomTemplates.test.ts
autonomous: true
requirements: [ROOM-01, ROOM-02]

must_haves:
  truths:
    - "Three new test files exist and are collected by vitest"
    - "Test stubs describe all addRoom/switchRoom/renameRoom/removeRoom cases"
    - "Test stubs describe v1→v2 migration cases"
    - "Test stubs describe all four room templates"
    - "`npm test -- --run` exits 0 (stubs use it.todo, don't fail)"
  artifacts:
    - path: "tests/cadStore.multiRoom.test.ts"
      provides: "Stub test cases for room-management actions (ROOM-01)"
      contains: "describe(\"cadStore multi-room\""
    - path: "tests/snapshotMigration.test.ts"
      provides: "Stub test cases for v1→v2 snapshot migration"
      contains: "describe(\"migrateSnapshot\""
    - path: "tests/roomTemplates.test.ts"
      provides: "Stub test cases for room templates (ROOM-02)"
      contains: "describe(\"roomTemplates\""
  key_links:
    - from: "tests/cadStore.multiRoom.test.ts"
      to: "vitest collector"
      via: "it.todo placeholders"
      pattern: "it\\.todo\\("
---

<objective>
Create Wave 0 test stub files using `it.todo()` placeholders so Wave 1+ can fill in implementations against stable describe/it string anchors. Stubs MUST be vitest-collectable (compile cleanly) but MUST NOT fail the suite. Follow the Phase 4 Wave 0 precedent (stubs pinned via exact describe strings for import stability).

Purpose: Provide pre-agreed test shape so planner→executor handoff is mechanical. No test bodies yet — just `it.todo("…")` names matching the behaviors specified in 05-RESEARCH.md §"Phase Requirements → Test Map".

Output: Three new test files that pass `npm test -- --run` with all cases as todos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-multi-room/05-CONTEXT.md
@.planning/phases/05-multi-room/05-RESEARCH.md
@.planning/phases/05-multi-room/05-VALIDATION.md
@tests/walkCollision.test.ts
@tests/uiStore.test.ts

<interfaces>
<!-- Vitest test stub pattern (Phase 04 precedent) -->
```typescript
import { describe, it } from "vitest";

describe("feature name", () => {
  it.todo("behavior description");
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 Wave 0 stub test files with it.todo() placeholders</name>
  <files>tests/cadStore.multiRoom.test.ts, tests/snapshotMigration.test.ts, tests/roomTemplates.test.ts</files>
  <read_first>
    - tests/walkCollision.test.ts (Phase 4 Wave 0 stub precedent — confirms `it.todo()` pattern used in this repo)
    - tests/uiStore.test.ts (another Wave 0 precedent in repo)
    - .planning/phases/05-multi-room/05-RESEARCH.md §"Phase Requirements → Test Map" (exact test names expected)
  </read_first>
  <action>
    Create three new vitest stub files. Each file imports only `{ describe, it } from "vitest"` — NO imports from `src/` (Wave 1 hasn't created those modules yet).

    **File 1: `tests/cadStore.multiRoom.test.ts`**
    ```typescript
    import { describe, it } from "vitest";

    describe("cadStore multi-room actions", () => {
      it.todo("addRoom creates a RoomDoc and returns its id");
      it.todo("addRoom with template pre-populates perimeter walls");
      it.todo("switchRoom updates activeRoomId without pushing history");
      it.todo("renameRoom updates the room name in place");
      it.todo("removeRoom last-room guard refuses to delete final room");
      it.todo("removeRoom reassigns activeRoomId when active room is deleted");
      it.todo("addWall targets only the active room");
      it.todo("placedProducts are isolated across rooms");
      it.todo("undo addRoom removes the entire room");
    });
    ```

    **File 2: `tests/snapshotMigration.test.ts`**
    ```typescript
    import { describe, it } from "vitest";

    describe("migrateSnapshot", () => {
      it.todo("v1 to v2 wraps legacy snapshot into Main Room");
      it.todo("v2 passthrough returns identical snapshot unchanged");
      it.todo("empty/unknown input returns default single-room snapshot");
      it.todo("legacy walls and placedProducts carry over into room_main");
    });
    ```

    **File 3: `tests/roomTemplates.test.ts`**
    ```typescript
    import { describe, it } from "vitest";

    describe("roomTemplates", () => {
      it.todo("LIVING_ROOM produces 4 perimeter walls at 16x20 ft");
      it.todo("BEDROOM produces 4 perimeter walls at 12x14 ft");
      it.todo("KITCHEN produces 4 perimeter walls at 10x12 ft");
      it.todo("BLANK produces zero walls with 16x20x8 room dims");
      it.todo("each template wall has thickness 0.5 and openings []");
    });
    ```

    **Rules (per D-01..D-19):**
    - Describe strings are EXACT anchors — Wave 1/3 executors will replace `it.todo` with `it` + body, keeping the same string. Do not rename.
    - No imports from `src/` — stubs must not break if downstream modules don't exist yet.
    - No test bodies. No assertions. Just `it.todo(...)`.
    - Do NOT modify existing test files in this plan (tests/cadStore.test.ts and tests/useAutoSave.test.ts update happens in Plan 03).
  </action>
  <verify>
    <automated>npm test -- --run tests/cadStore.multiRoom.test.ts tests/snapshotMigration.test.ts tests/roomTemplates.test.ts 2>&1 | grep -E "(todo|passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it.todo" tests/cadStore.multiRoom.test.ts` returns 9
    - `grep -c "it.todo" tests/snapshotMigration.test.ts` returns 4
    - `grep -c "it.todo" tests/roomTemplates.test.ts` returns 5
    - `grep -l "from \"@/" tests/cadStore.multiRoom.test.ts tests/snapshotMigration.test.ts tests/roomTemplates.test.ts` returns nothing (no src imports)
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Three new test files created with it.todo stubs, vitest collects them without failures.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0
- All three files grep for `it.todo(` matches expected counts
- No stub file imports from `src/` or `@/`
</verification>

<success_criteria>
Three new test stub files exist, contain the required describe/it.todo anchors verbatim, and vitest collects them without failing any assertion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-room/05-00-SUMMARY.md`
</output>
