---
phase: 02-product-library
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/productStore.test.ts
  - tests/productHelpers.test.ts
  - tests/productSearch.test.ts
  - tests/AddProductModal.test.tsx
  - tests/SidebarProductPicker.test.tsx
autonomous: true
requirements: [LIB-03, LIB-04, LIB-05]
must_haves:
  truths:
    - "Test files exist at tests/ root for all Phase 2 units"
    - "All stub tests run (may be skipped/pending) — no import errors"
  artifacts:
    - path: "tests/productStore.test.ts"
      provides: "Test scaffold for productStore CRUD + persistence gate (LIB-03)"
    - path: "tests/productHelpers.test.ts"
      provides: "Test scaffold for effectiveDimensions + hasDimensions (LIB-04)"
    - path: "tests/productSearch.test.ts"
      provides: "Test scaffold for name substring filter (LIB-05)"
    - path: "tests/AddProductModal.test.tsx"
      provides: "Test scaffold for Skip dimensions toggle (LIB-04)"
    - path: "tests/SidebarProductPicker.test.tsx"
      provides: "Test scaffold for sidebar picker search + DRAG_MIME (LIB-05)"
  key_links:
    - from: "tests/*"
      to: "npm test -- --run"
      via: "vitest config from Phase 1"
      pattern: "vitest"
---

<objective>
Create test-file stubs required by Phase 2 VALIDATION.md so every implementation task downstream has an `<automated>` verification target.

Purpose: Satisfy Nyquist sampling — no code task in Phase 2 runs without a corresponding test file that can be executed immediately.
Output: 5 test files under `tests/` with skeleton `describe`/`it.todo` blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-product-library/02-VALIDATION.md
@.planning/phases/02-product-library/02-RESEARCH.md
@tests/useAutoSave.test.ts
@tests/SaveIndicator.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit-test stubs (productStore, productHelpers, productSearch)</name>
  <files>tests/productStore.test.ts, tests/productHelpers.test.ts, tests/productSearch.test.ts</files>
  <read_first>
    - .planning/phases/02-product-library/02-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/02-product-library/02-RESEARCH.md (§ Validation Architecture — Phase Requirements → Test Map)
    - tests/useAutoSave.test.ts (existing idb-keyval mocking pattern)
  </read_first>
  <action>
    Create three vitest stub files. Use `describe` + `it.todo` (or `it.skip`) so imports compile even before source files exist.

    1. **tests/productStore.test.ts** — stubs for LIB-03:
       ```typescript
       import { describe, it } from "vitest";

       describe("productStore (LIB-03)", () => {
         it.todo("load() hydrates products from idb-keyval room-cad-products key");
         it.todo("load() migrates legacy non-null width/depth/height to number|null");
         it.todo("addProduct appends a product to state");
         it.todo("removeProduct filters out by id");
         it.todo("updateProduct applies partial changes by id");
         it.todo("subscribe persists to idb-keyval only when loaded=true");
         it.todo("subscribe does NOT persist before load() resolves (guards empty-state overwrite)");
       });
       ```

    2. **tests/productHelpers.test.ts** — stubs for LIB-04:
       ```typescript
       import { describe, it } from "vitest";

       describe("effectiveDimensions (LIB-04)", () => {
         it.todo("returns product dims + isPlaceholder:false when all dims are numbers");
         it.todo("returns 2/2/2 + isPlaceholder:true when width is null");
         it.todo("returns 2/2/2 + isPlaceholder:true when depth is null");
         it.todo("returns 2/2/2 + isPlaceholder:true when height is null");
         it.todo("returns 2/2/2 + isPlaceholder:true when product is undefined (orphan)");
       });

       describe("hasDimensions (LIB-04)", () => {
         it.todo("returns true when all three dims are numbers");
         it.todo("returns false when any dim is null");
       });
       ```

    3. **tests/productSearch.test.ts** — stubs for LIB-05:
       ```typescript
       import { describe, it } from "vitest";

       describe("product name search (LIB-05)", () => {
         it.todo("case-insensitive substring match: 'EAMES' matches 'Eames Lounge Chair'");
         it.todo("case-insensitive substring match: 'lounge' matches 'Eames Lounge Chair'");
         it.todo("empty search string returns full product list");
         it.todo("whitespace-only search returns full list (trimmed)");
         it.todo("no match returns empty array");
       });
       ```

    Do NOT create source files this task — only stubs. Imports should be from `vitest` only.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(productStore|productHelpers|productSearch)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/productStore.test.ts` exists and contains string `"productStore (LIB-03)"`
    - `tests/productHelpers.test.ts` exists and contains string `"effectiveDimensions (LIB-04)"`
    - `tests/productSearch.test.ts` exists and contains string `"product name search (LIB-05)"`
    - `npm test -- --run` exits 0 (stubs pass as todo/skip)
    - grep finds at least 5 `it.todo` occurrences across the 3 new files
  </acceptance_criteria>
  <done>Three stub files exist, `npm test -- --run` passes, each file registers >= 5 todo tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create component-test stubs (AddProductModal, SidebarProductPicker)</name>
  <files>tests/AddProductModal.test.tsx, tests/SidebarProductPicker.test.tsx</files>
  <read_first>
    - .planning/phases/02-product-library/02-VALIDATION.md (Wave 0 Requirements)
    - tests/SaveIndicator.test.tsx (existing component-test pattern with @testing-library/react)
  </read_first>
  <action>
    Create two vitest + @testing-library/react stub files using `it.todo`.

    1. **tests/AddProductModal.test.tsx** — stubs for LIB-04:
       ```typescript
       import { describe, it } from "vitest";

       describe("AddProductModal Skip Dimensions (LIB-04)", () => {
         it.todo("renders SKIP_DIMENSIONS checkbox");
         it.todo("toggling Skip greys out width/depth/height inputs (opacity-40 + pointer-events-none)");
         it.todo("submit with skipDims=true calls onAdd with width:null, depth:null, height:null");
         it.todo("submit with skipDims=false calls onAdd with numeric width/depth/height");
       });
       ```

    2. **tests/SidebarProductPicker.test.tsx** — stubs for LIB-05:
       ```typescript
       import { describe, it } from "vitest";

       describe("SidebarProductPicker (LIB-05)", () => {
         it.todo("renders SEARCH... input");
         it.todo("typing 'eames' filters to products whose name contains Eames (case-insensitive)");
         it.todo("empty search shows all products");
         it.todo("dragstart on product row sets DRAG_MIME to the product id");
         it.todo("dragstart sets effectAllowed to 'copy'");
       });
       ```

    No source files created. Imports should be from `vitest` only.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(AddProductModal|SidebarProductPicker)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/AddProductModal.test.tsx` exists and contains `"AddProductModal Skip Dimensions (LIB-04)"`
    - `tests/SidebarProductPicker.test.tsx` exists and contains `"SidebarProductPicker (LIB-05)"`
    - `npm test -- --run` exits 0 with both new suites listed
    - grep finds at least 4 `it.todo` in each new file
  </acceptance_criteria>
  <done>Two component stub files exist, `npm test -- --run` passes, each file registers >= 4 todo tests.</done>
</task>

</tasks>

<verification>
Run full test suite: `npm test -- --run` — MUST pass with all new stubs included. No compile/import errors.
</verification>

<success_criteria>
- 5 new test files exist at tests/ root
- `npm test -- --run` passes (todo/skip tests register but do not fail)
- All Wave 0 gaps from VALIDATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-library/02-00-SUMMARY.md`
</output>
