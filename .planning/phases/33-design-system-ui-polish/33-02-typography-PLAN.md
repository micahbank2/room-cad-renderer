---
phase: 33-design-system-ui-polish
plan: 02
type: execute
wave: 1
depends_on: [00, 01]
files_modified:
  - src/components/Toolbar.tsx
  - src/components/Sidebar.tsx
  - src/components/PropertiesPanel.tsx
  - src/components/RoomSettings.tsx
  - src/components/ProjectManager.tsx
autonomous: true
requirements:
  - "GH #83"
must_haves:
  truths:
    - "Section headers in PropertiesPanel render as 'Position', 'Dimensions', 'Material', 'Rotation' (mixed-case, not UPPERCASE)"
    - "Sidebar panel headers render mixed-case ('Room config', 'Properties', 'Library')"
    - "Button labels in Toolbar render mixed-case ('Save', 'Load', 'New', 'Place in room')"
    - "Dynamic CAD identifiers remain UPPERCASE (WALL_SEGMENT_{id}, {PRODUCT_NAME})"
    - "Status strings remain UPPERCASE (SAVED, BUILDING_SCENE, READY)"
    - "Unit value labels remain UPPERCASE (LENGTH, WIDTH_FT, DEPTH_FT)"
    - "Typography shows 3 visually distinct levels (display 28px, base 13px, sm 11px) consumed via Tailwind utilities defined in Plan 01"
    - "Plan 02 does NOT modify src/index.css — Plan 01 owns that file (Wave 1 parallel-write hazard eliminated)"
  artifacts:
    - path: "src/components/PropertiesPanel.tsx"
      provides: "Mixed-case section headers using h2 role"
    - path: "src/components/Sidebar.tsx"
      provides: "Mixed-case panel headers using h1 role"
    - path: "src/components/Toolbar.tsx"
      provides: "Mixed-case button labels using label role"
  key_links:
    - from: "src/components/*"
      to: "src/index.css @theme tokens (owned by Plan 01)"
      via: "text-base / text-sm / text-display Tailwind utility classes generated by v4 from --text-* tokens"
      pattern: "text-(base|sm|display)"
---

<objective>
Ship GH #83 — implement the 5-tier type ramp (3 CSS tokens owned by Plan 01, 6 semantic roles, 2 weights) across Toolbar / Sidebar / PropertiesPanel / RoomSettings / ProjectManager. Shift section headers, panel headers, and button labels from blanket UPPERCASE to mixed-case per D-03/D-05. Preserve UPPERCASE for dynamic identifiers (D-04), status strings, and unit value labels.

**Wave 1 ownership fix (checker blocker 1):** Plan 02 no longer writes to `src/index.css`. The Tailwind v4 token-naming correction that was queued as Task 1 has been absorbed into Plan 01 Task 2, so Plan 01 exits Wave 1 with final `--text-*` + `--spacing-*` tokens committed. Plan 02 consumes those utilities via Tailwind classes only.

Purpose: Peer-grade readability (Pascal parity) while preserving the "CAD personality" of the obsidian theme for the categories that stay UPPERCASE.

Output: Section/panel headers read as mixed-case; button labels read as mixed-case; typography hierarchy has 3 visually distinct levels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-system-ui-polish/33-UI-SPEC.md
@.planning/phases/33-design-system-ui-polish/33-CONTEXT.md
@src/components/Toolbar.tsx
@src/components/Sidebar.tsx
@src/components/PropertiesPanel.tsx

<interfaces>
**Semantic roles (from UI-SPEC Typography table):**

| Role | Font | Size token (Plan 01) | Weight | Case | Example sites |
|------|------|----------------------|--------|------|---------------|
| display | Space Grotesk | `--text-display` (28px) | 500 | Mixed | `DESIGN_YOUR_SPACE` brand (WelcomeScreen — leave UPPERCASE, it's brand identity) |
| h1 | IBM Plex Mono | `--text-base` (13px) | 500 | **Mixed** | "Room config", "Properties", "Library" in Sidebar |
| h2 | IBM Plex Mono | `--text-sm` (11px) | 500 | **Mixed** | "Position", "Dimensions", "Material", "Rotation" in PropertiesPanel |
| label | IBM Plex Mono | `--text-sm` (11px) | 400 | **Mixed** | Button labels, tab labels, chip labels |
| body | Inter | `--text-base` (13px) | 400 | Mixed | Prose (empty-state bodies, help text) |
| value | IBM Plex Mono | `--text-sm` (11px) | 400 | As-is | Numeric field values — font-mono preserved for alignment |

**Tailwind v4 utility mapping (owned by Plan 01, consumed here):**
- `text-display` → font-size: 28px (from `--text-display`)
- `text-base` → 13px (from `--text-base` — may collide with Tailwind default; Plan 01 verified utility actually emits)
- `text-sm` → 11px (from `--text-sm`)

**Sites that STAY UPPERCASE (D-04):**
- Dynamic identifiers: `WALL_SEGMENT_{id}`, `.toUpperCase().replace(/\s/g, "_")` on product names
- Status strings: `SAVED`, `SAVING`, `BUILDING_SCENE`, `READY`, `SYSTEM_STATUS`
- Unit value labels in PropertiesPanel numeric-row leads: `LENGTH`, `WIDTH_FT`, `DEPTH_FT`, `HEIGHT`, `THICKNESS`

**Existing PropertiesPanel structure (from research — NO named sections yet):**
Currently a flat glass-panel. This plan INTRODUCES named section headers ("Position", "Dimensions", "Material", "Rotation") in mixed-case. Plan 04 (collapsible) wraps these in `<CollapsibleSection>`; Plan 02 just renders the headers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar + ProjectManager panel headers → mixed-case h1 role</name>
  <files>src/components/Sidebar.tsx, src/components/ProjectManager.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (full file — find all UPPERCASE strings that are panel headers)
    - src/components/ProjectManager.tsx (full file)
    - .planning/phases/33-design-system-ui-polish/33-UI-SPEC.md § "Typography" + § "Copywriting Contract"
    - src/index.css (READ-ONLY — confirm Plan 01 shipped `--text-base`/`--text-sm` tokens and Tailwind v4 exposes them as utilities)
  </read_first>
  <action>
    Find every UPPERCASE string literal in Sidebar.tsx that renders as a section/panel header — these are the h1 role per UI-SPEC. Replace with mixed-case:

    | UPPERCASE | Mixed-case |
    |-----------|------------|
    | `ROOM_CONFIG` | `Room config` |
    | `PROPERTIES` | `Properties` |
    | `LIBRARY` | `Library` |
    | `PROJECT` | `Project` |
    | `SYSTEM_STATS` | `System stats` |
    | `LAYERS` | `Layers` |
    | `SNAP` | `Snap` |

    Apply same transformation to ProjectManager.tsx headers (e.g., "SAVED_PROJECTS" → "Saved projects"). Leave status strings (e.g., inside a `SaveIndicator` / save-status) unchanged.

    Use className `font-mono text-base font-medium text-text-muted` (IBM Plex Mono, 13px from `--text-base` owned by Plan 01, weight 500) to implement the h1 role. If `text-base` utility conflicts with Tailwind default in v4, fall back to `text-[13px]` arbitrary value — but prefer the token utility.

    DO NOT change:
    - Dynamic identifier strings (any `.toUpperCase()` call)
    - Status badges (`SAVED`, `SAVING`, `SYSTEM_STATUS`)
    - Brand `OBSIDIAN CAD` in Toolbar (handled in Task 3)
    - Tool labels `SELECT`, `WALL`, `DOOR`, `WINDOW` (these are label role per D-04 — decided in Task 3)

    **IMPORTANT:** Many of these headers are likely inside inline `CollapsibleSection` (Sidebar.tsx:17-42 per research). Preserve the component structure; only change the string + className.

    **File ownership:** Do NOT touch `src/index.css`. Plan 01 owns that file. If you encounter a situation where a token name needs to change, STOP and escalate — do not fork Wave 1 ownership.
  </action>
  <verify>
    <automated>grep -E "\"Room config\"|\"Properties\"|\"Library\"" src/components/Sidebar.tsx &amp;&amp; ! grep -E "\"ROOM_CONFIG\"|\"LAYERS\"|\"SNAP\"" src/components/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"Room config\"" src/components/Sidebar.tsx` matches
    - `grep "\"Properties\"" src/components/Sidebar.tsx` matches
    - `grep "\"Library\"" src/components/Sidebar.tsx` matches
    - `grep "\"ROOM_CONFIG\"" src/components/Sidebar.tsx` returns nothing
    - `grep "\"LAYERS\"" src/components/Sidebar.tsx` returns nothing
    - Dynamic identifier strings (any `.toUpperCase(` calls) NOT touched
    - `src/index.css` NOT in git diff for this task
    - `npm test -- --run tests/phase33/typography.test.ts` passes the Sidebar assertion
  </acceptance_criteria>
  <done>Sidebar + ProjectManager panel headers are mixed-case h1 role.</done>
</task>

<task type="auto">
  <name>Task 2: PropertiesPanel section headers + unit-label preservation</name>
  <files>src/components/PropertiesPanel.tsx, src/components/RoomSettings.tsx</files>
  <read_first>
    - src/components/PropertiesPanel.tsx (full — identify all existing UPPERCASE strings + locate implicit groups for wall/product/custom-element/ceiling blocks)
    - src/components/RoomSettings.tsx
    - .planning/phases/33-design-system-ui-polish/33-RESEARCH.md § "Pitfall 5: PropertiesPanel does not have named sections yet"
    - .planning/phases/33-design-system-ui-polish/33-UI-SPEC.md § "Copywriting Contract"
  </read_first>
  <action>
    PropertiesPanel currently has NO explicit section headers. Per research Pitfall #5, INTRODUCE mixed-case section headers above each implicit group:

    1. Identify the existing sub-blocks in PropertiesPanel (wall block, product block, custom-element block, ceiling block). Each has an implicit group of numeric inputs (e.g., length/thickness, width/depth/height, position-x/y, rotation, material).

    2. Insert a section header `<h3>` (or `<div>` with h2-role class) ABOVE each group with a STABLE `id` prop (for Plan 04 CollapsibleSection to consume):
       - `id="position"` → label `Position`
       - `id="dimensions"` → label `Dimensions`
       - `id="rotation"` → label `Rotation`
       - `id="material"` → label `Material`

    3. Header className: `font-mono text-sm font-medium text-text-muted` (uses Plan 01's `--text-sm` token via Tailwind v4 utility — 11px, weight 500). Mixed-case labels per D-03.

    4. PRESERVE UPPERCASE on unit value labels per D-04 — any label like `LENGTH`, `WIDTH_FT`, `DEPTH_FT`, `HEIGHT`, `THICKNESS` inside the numeric input row LEFT column stays UPPERCASE with font-mono. These are unit identifiers, not section headers.

    5. RoomSettings.tsx: same treatment. Header labels like `ROOM_CONFIG`/`ROOM_DIMENSIONS` → mixed-case (per D-03). Unit labels (`WIDTH_FT`, `LENGTH_FT`, `HEIGHT`) stay UPPERCASE.

    6. Button labels in both files (e.g., `APPLY`, `RESET_SIZE`) shift to label role: mixed-case "Apply", "Reset size", font-mono, weight 400.

    **DO NOT wrap sections in `<CollapsibleSection>` yet** — Plan 04 does that. For now, stable `id="..."` prop on the header is enough.
  </action>
  <verify>
    <automated>grep -E "\"Position\"|\"Dimensions\"|\"Rotation\"|\"Material\"" src/components/PropertiesPanel.tsx &amp;&amp; grep -E "LENGTH|WIDTH_FT|DEPTH_FT" src/components/PropertiesPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"Position\"" src/components/PropertiesPanel.tsx` matches
    - `grep "\"Dimensions\"" src/components/PropertiesPanel.tsx` matches
    - `grep "\"Rotation\"" src/components/PropertiesPanel.tsx` matches
    - `grep "\"Material\"" src/components/PropertiesPanel.tsx` matches (at least one block uses this)
    - `grep "LENGTH\|WIDTH_FT\|DEPTH_FT" src/components/PropertiesPanel.tsx` still matches (preserved)
    - Headers have stable `id` attributes or data-section-id attributes
    - `src/index.css` NOT in git diff for this task
    - `tests/phase33/typography.test.ts` PropertiesPanel assertion is GREEN
  </acceptance_criteria>
  <done>PropertiesPanel has named mixed-case section headers with stable IDs; unit labels preserved UPPERCASE.</done>
</task>

<task type="auto">
  <name>Task 3: Toolbar mixed-case button labels + preserve brand/status/tool-icon labels</name>
  <files>src/components/Toolbar.tsx</files>
  <read_first>
    - src/components/Toolbar.tsx (full)
    - .planning/phases/33-design-system-ui-polish/33-UI-SPEC.md § "Copywriting Contract"
    - .planning/phases/33-design-system-ui-polish/33-CONTEXT.md D-03/D-04/D-05
  </read_first>
  <action>
    Apply label role (mixed-case, font-mono, weight 400) to button text labels in Toolbar.tsx:

    **SHIFT to mixed-case:**
    | Current | Mixed-case |
    |---------|------------|
    | `SAVE` / `SAVE_PROJECT` | `Save` |
    | `LOAD` / `LOAD_PROJECT` | `Load` |
    | `NEW` / `NEW_PROJECT` | `New` |
    | `EXPORT` | `Export` |
    | `UNDO` (tooltip text) | `Undo` |
    | `REDO` | `Redo` |
    | `GRID` toggle label | `Grid` |
    | `2D_PLAN`, `3D_VIEW`, `SPLIT`, `LIBRARY` (view mode labels) | `2D plan`, `3D view`, `Split`, `Library` |

    **PRESERVE UPPERCASE (per D-04):**
    - Tool labels `SELECT`, `WALL`, `DOOR`, `WINDOW` — these are tool-mode identifiers, stay UPPERCASE per D-04 reading ("Dynamic identifiers" — broad interpretation: tool mode IDs count). Use `tool-label` semantic.
    - Brand `OBSIDIAN CAD` — display role, stays UPPERCASE (brand identity)
    - Status strings in SaveStatus: `SAVED`, `SAVING`, `BUILDING_SCENE...`, `READY`

    Button className: `font-mono text-sm font-normal text-text-muted hover:text-text-primary` (label role: weight 400, 11px via Plan 01's `--text-sm` utility).

    **DO NOT touch** icon-only buttons (undo/redo with just an icon) — only text labels.
  </action>
  <verify>
    <automated>grep -E "\"Save\"|\"Load\"|\"New\"" src/components/Toolbar.tsx &amp;&amp; grep -E "\"SAVED\"|\"SAVING\"|OBSIDIAN" src/components/Toolbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"Save\"" src/components/Toolbar.tsx` matches (new button label)
    - `grep "\"Load\"" src/components/Toolbar.tsx` matches
    - `grep "\"New\"" src/components/Toolbar.tsx` matches
    - `grep "\"SAVED\"" src/components/Toolbar.tsx` still matches (status preserved)
    - `grep "OBSIDIAN" src/components/Toolbar.tsx` still matches (brand preserved)
    - Tool mode labels (SELECT, WALL, DOOR, WINDOW) still UPPERCASE (grep finds them)
    - `src/index.css` NOT in git diff for this task
  </acceptance_criteria>
  <done>Toolbar button text is mixed-case label role; brand/status/tool-IDs preserve UPPERCASE.</done>
</task>

</tasks>

<verification>
```bash
# Typography test passes
npm test -- --run tests/phase33/typography.test.ts

# Manual grep sweep — no unexpected broken UPPERCASE
grep -n "ROOM_CONFIG\|LAYERS\|SNAP" src/components/Sidebar.tsx   # expect zero
grep -n "\"Position\"\|\"Dimensions\"\|\"Rotation\"\|\"Material\"" src/components/PropertiesPanel.tsx   # expect matches

# Build compiles (Tailwind tokens resolve)
npm run build 2>&1 | tail -3

# Wave 1 ownership invariant: Plan 02 MUST NOT have touched src/index.css
# (checked by the orchestrator against the git diff for the plan's branch)
```
</verification>

<success_criteria>
- [ ] Sidebar panel headers mixed-case
- [ ] PropertiesPanel has 4 stable-ID section headers (Position, Dimensions, Rotation, Material) in mixed-case
- [ ] Toolbar button text mixed-case (Save/Load/New/Export/etc.)
- [ ] UPPERCASE preserved on: tool modes, brand, status, unit labels, dynamic identifiers
- [ ] `tests/phase33/typography.test.ts` GREEN
- [ ] `npm run build` succeeds
- [ ] `src/index.css` NOT modified by this plan (ownership invariant)
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-system-ui-polish/33-02-SUMMARY.md` documenting:
- All case shifts performed (by file)
- UPPERCASE sites preserved (with D-04 justification)
- Section header IDs available for Plan 04 consumption
- Confirmation: Plan 02 did not modify src/index.css (Plan 01 ownership preserved)
- Closes #83
</output>
</output>
