---
phase: 32-pbr-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/textures/wood-plank/albedo.jpg
  - public/textures/wood-plank/normal.jpg
  - public/textures/wood-plank/roughness.jpg
  - public/textures/wood-plank/LICENSE.txt
  - public/textures/concrete/albedo.jpg
  - public/textures/concrete/normal.jpg
  - public/textures/concrete/roughness.jpg
  - public/textures/concrete/LICENSE.txt
  - public/textures/plaster/albedo.jpg
  - public/textures/plaster/normal.jpg
  - public/textures/plaster/roughness.jpg
  - public/textures/plaster/LICENSE.txt
  - public/hdr/studio_small_09_1k.hdr
  - public/hdr/LICENSE.txt
  - src/data/surfaceMaterials.ts
autonomous: false
requirements:
  - VIZ-07
  - VIZ-09
user_setup: []

must_haves:
  truths:
    - "Three CC0 PBR texture sets exist on disk at correct resolutions under ~1.5 MB total"
    - "One neutral interior HDR environment exists on disk under ~500 KB"
    - "SurfaceMaterial type supports optional pbr field with three URL maps and repeat tile size"
    - "WOOD_PLANK, CONCRETE, PLASTER entries carry populated pbr blocks; PAINTED_DRYWALL does NOT"
    - "Tile sizes in the registry match D-20 exactly (WOOD_PLANK 0.5×4, CONCRETE 4×4, PLASTER 6×6 feet)"
  artifacts:
    - path: "public/textures/wood-plank/albedo.jpg"
      provides: "1024² sRGB albedo for wood plank"
    - path: "public/textures/wood-plank/normal.jpg"
      provides: "512² normal map for wood plank"
    - path: "public/textures/wood-plank/roughness.jpg"
      provides: "512² roughness map for wood plank"
    - path: "public/textures/concrete/albedo.jpg"
      provides: "1024² sRGB albedo for concrete"
    - path: "public/textures/concrete/normal.jpg"
      provides: "512² normal map for concrete"
    - path: "public/textures/concrete/roughness.jpg"
      provides: "512² roughness map for concrete"
    - path: "public/textures/plaster/albedo.jpg"
      provides: "1024² sRGB albedo for plaster"
    - path: "public/textures/plaster/normal.jpg"
      provides: "512² normal map for plaster"
    - path: "public/textures/plaster/roughness.jpg"
      provides: "512² roughness map for plaster"
    - path: "public/hdr/studio_small_09_1k.hdr"
      provides: "Neutral interior HDR environment (~≤500 KB) for IBL"
    - path: "src/data/surfaceMaterials.ts"
      provides: "SurfaceMaterial.pbr optional field + populated entries for WOOD_PLANK / CONCRETE / PLASTER"
      contains: "pbr?:"
  key_links:
    - from: "SURFACE_MATERIALS.WOOD_PLANK"
      to: "public/textures/wood-plank/{albedo,normal,roughness}.jpg"
      via: "pbr.albedo / pbr.normal / pbr.roughness absolute URLs starting with /textures/"
      pattern: "/textures/wood-plank/albedo"
    - from: "SURFACE_MATERIALS.CONCRETE"
      to: "public/textures/concrete/{albedo,normal,roughness}.jpg"
      via: "pbr field"
      pattern: "/textures/concrete/albedo"
    - from: "SURFACE_MATERIALS.PLASTER"
      to: "public/textures/plaster/{albedo,normal,roughness}.jpg"
      via: "pbr field"
      pattern: "/textures/plaster/albedo"
---

<objective>
Ship the static assets and the data-layer contract that every downstream Phase 32 task consumes. Source three CC0 PBR texture sets and one neutral HDR, commit them to `public/textures/` and `public/hdr/`, then extend `SurfaceMaterial` with an optional `pbr` field and populate it for WOOD_PLANK, CONCRETE, and PLASTER at the D-20 tile sizes.

Purpose: Satisfies VIZ-09 (bundled CC0 sets at correct resolution + license provenance) and lays the data rails for VIZ-07. Plans 02 and 03 load these assets through the optional `pbr` field — without this plan, they cannot wire anything.
Output: `public/textures/{wood-plank,concrete,plaster}/{albedo,normal,roughness}.jpg`, `public/hdr/studio_small_09_1k.hdr`, extended `SurfaceMaterial` type, populated registry entries, per-folder LICENSE.txt files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-pbr-foundation/32-CONTEXT.md
@src/data/surfaceMaterials.ts

<interfaces>
<!-- Current SurfaceMaterial type (extracted verbatim from src/data/surfaceMaterials.ts). -->
<!-- Extend — do not replace — by adding an optional `pbr` field. -->

```typescript
export type SurfaceTarget = "floor" | "ceiling" | "both";

export interface SurfaceMaterial {
  id: string;
  label: string;
  color: string;
  roughness: number;
  surface: SurfaceTarget;
  defaultScaleFt: number;
}
```

Current populated entries (unchanged — only PBR additions to the three named ones):
- WOOD_OAK, WOOD_WALNUT, TILE_WHITE, TILE_BLACK, CARPET, MARBLE, STONE — no change
- **CONCRETE** (surface: "both", defaultScaleFt: 4) — add `pbr`
- **PLASTER** (surface: "ceiling", defaultScaleFt: 4) — add `pbr`
- **WOOD_PLANK** (surface: "ceiling", defaultScaleFt: 0.5) — add `pbr`
- **PAINTED_DRYWALL** (surface: "ceiling", defaultScaleFt: 4) — leave unchanged (no `pbr`)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Source and commit CC0 texture sets + HDR</name>
  <files>public/textures/wood-plank/albedo.jpg, public/textures/wood-plank/normal.jpg, public/textures/wood-plank/roughness.jpg, public/textures/wood-plank/LICENSE.txt, public/textures/concrete/albedo.jpg, public/textures/concrete/normal.jpg, public/textures/concrete/roughness.jpg, public/textures/concrete/LICENSE.txt, public/textures/plaster/albedo.jpg, public/textures/plaster/normal.jpg, public/textures/plaster/roughness.jpg, public/textures/plaster/LICENSE.txt, public/hdr/studio_small_09_1k.hdr, public/hdr/LICENSE.txt</files>
  <read_first>
    - .planning/phases/32-pbr-foundation/32-CONTEXT.md (D-08, D-09, D-10, D-11, D-14 — taste + licensing rules)
    - .planning/REQUIREMENTS.md §VIZ-09 (resolution + bundle size acceptance)
    - .planning/ROADMAP.md §Phase 32 Success Criterion 5
  </read_first>
  <decision>
    Claude selects three CC0 texture sets and one neutral interior HDR per D-10/D-11, downloads them locally, resamples each to the locked resolutions (albedo 1024², normal 512², roughness 512²), and commits raw assets + per-folder LICENSE.txt to the repo. User reviews taste in PR preview (not at planning time). This task is a blocking checkpoint ONLY because the user explicitly reserved aesthetic sign-off to PR preview per D-11 — Claude runs the full mechanical sourcing and commits first, then surfaces the picks.
  </decision>
  <context>
    D-10 taste profile:
    - Wood: warm light oak (NOT walnut, NOT pine)
    - Concrete: smooth polished (NOT rough aggregate, NOT industrial)
    - Plaster: warm off-white (NOT bright white, NOT gray)
    - HDR: neutral interior studio class (Poly Haven `studio_small_09` or equivalent; soft, low-contrast)

    D-14 resolutions:
    - albedo: 1024×1024 JPG (~80–120 KB each after optimization)
    - normal: 512×512 JPG (~40–70 KB each)
    - roughness: 512×512 JPG (~30–60 KB each, grayscale acceptable)

    D-08 HDR budget: ≤ ~500 KB, .hdr format loadable via drei `<Environment files="…" />`.

    Suggested sources (Claude may use equivalents if listed ones are unavailable):
    - Poly Haven: https://polyhaven.com/textures — filter for CC0, "wood", "concrete", "plaster"
    - ambientCG: https://ambientcg.com/ — all CC0, filter by "Wood", "Concrete", "Plaster"
    - Poly Haven HDRIs: https://polyhaven.com/hdris — filter indoor, neutral

    Suggested specific starting points (planner research direction — substitute if inferior):
    - Wood: "wood_table_001" or "plywood_diff" — look for light oak tone, straight grain
    - Concrete: "concrete_layers_02" or "concrete_floor_001" — smooth polished preferred
    - Plaster: "plaster_001" or "plaster_brick_01" — warm off-white
    - HDR: "studio_small_09" 1K resolution (~300–500 KB)
  </context>
  <options>
    <option id="approved">
      <name>User approves taste selections in PR preview</name>
      <pros>Single round trip; swap-out per D-11 is a file replace</pros>
      <cons>None</cons>
    </option>
    <option id="swap">
      <name>User requests different assets</name>
      <pros>Nails taste before loader wiring</pros>
      <cons>Second commit required</cons>
    </option>
  </options>
  <action>
    Before raising the checkpoint, Claude MUST complete all mechanical work:

    1. Create directories:
       ```
       mkdir -p public/textures/wood-plank public/textures/concrete public/textures/plaster public/hdr
       ```

    2. For each of the three materials, source a CC0 set from Poly Haven or ambientCG matching D-10 taste. Download albedo/color, normal (OpenGL format — if DirectX only, invert green channel), and roughness maps. Resample using any CLI tool (sharp via npx, imagemagick, or node script) to:
       - albedo → 1024×1024 JPG, quality 82
       - normal → 512×512 JPG, quality 85 (normals tolerate less compression than albedo)
       - roughness → 512×512 JPG, quality 80 (grayscale collapse OK if source is grayscale)

    3. Write each set to `public/textures/{wood-plank|concrete|plaster}/{albedo|normal|roughness}.jpg`.

    4. Write per-folder LICENSE.txt with: source URL, asset name, license (CC0), author attribution if requested by source (CC0 does not require attribution but Poly Haven appreciates it).

    5. Source a neutral interior HDR (target: `studio_small_09` 1K or equivalent). Write to `public/hdr/studio_small_09_1k.hdr`. Write `public/hdr/LICENSE.txt` with source URL + CC0 notice.

    6. Verify total bundle delta:
       ```
       du -sb public/textures public/hdr
       ```
       Total MUST be between 1.2 MB and 2.2 MB (target ~1.5 MB textures + ~500 KB HDR = ~2.0 MB). If over, re-encode JPGs at lower quality.

    7. Verify dimensions for each map:
       ```
       file public/textures/wood-plank/albedo.jpg
       file public/textures/wood-plank/normal.jpg
       file public/textures/wood-plank/roughness.jpg
       ```
       Must report `1024x1024` for albedo and `512x512` for the other two. Repeat for concrete and plaster.

    8. Commit via:
       ```
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(32): source CC0 PBR texture sets + neutral HDR" --files public/textures public/hdr
       ```

    After commit, raise the checkpoint surfacing:
    - Three WOOD_PLANK preview thumbnails (or file paths if thumbnails impractical)
    - Three CONCRETE preview thumbnails
    - Three PLASTER preview thumbnails
    - HDR source + name
    - `du -sb public/textures public/hdr` total
    - Per-folder LICENSE.txt contents
  </action>
  <verify>
    <automated>test -f public/textures/wood-plank/albedo.jpg && test -f public/textures/wood-plank/normal.jpg && test -f public/textures/wood-plank/roughness.jpg && test -f public/textures/concrete/albedo.jpg && test -f public/textures/concrete/normal.jpg && test -f public/textures/concrete/roughness.jpg && test -f public/textures/plaster/albedo.jpg && test -f public/textures/plaster/normal.jpg && test -f public/textures/plaster/roughness.jpg && test -f public/hdr/studio_small_09_1k.hdr && test -f public/textures/wood-plank/LICENSE.txt && test -f public/textures/concrete/LICENSE.txt && test -f public/textures/plaster/LICENSE.txt && test -f public/hdr/LICENSE.txt</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/textures/` lists exactly three subdirectories: `wood-plank`, `concrete`, `plaster`
    - Each subdirectory contains exactly four files: `albedo.jpg`, `normal.jpg`, `roughness.jpg`, `LICENSE.txt`
    - `file public/textures/wood-plank/albedo.jpg` output contains the string `1024x1024`
    - `file public/textures/wood-plank/normal.jpg` output contains the string `512x512`
    - `file public/textures/wood-plank/roughness.jpg` output contains the string `512x512`
    - Same 1024/512/512 dimensional rule holds for `concrete/` and `plaster/`
    - `public/hdr/studio_small_09_1k.hdr` exists and is between 200000 and 700000 bytes
    - `du -sb public/textures` reports total bytes between 1000000 and 2200000
    - Each LICENSE.txt contains the string `CC0` and a source URL starting with `https://`
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept taste, or name replacement asset URLs to swap specific sets</resume-signal>
  <done>Nine texture files, one HDR file, four LICENSE.txt files committed. Sizes verified. Resolutions verified. User has approved taste in checkpoint.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend SurfaceMaterial with optional pbr field + populate three entries</name>
  <files>src/data/surfaceMaterials.ts</files>
  <read_first>
    - src/data/surfaceMaterials.ts (full file — current type definition + all 11 existing entries)
    - .planning/phases/32-pbr-foundation/32-CONTEXT.md (D-13 optional field pattern; D-20 tile sizes; D-04 shared repeat across all three maps)
    - .planning/REQUIREMENTS.md §VIZ-07 (Acceptance: D-2 optional pbr on SurfaceMaterial)
  </read_first>
  <action>
    Modify `src/data/surfaceMaterials.ts`:

    1. Immediately above the `export interface SurfaceMaterial` block, add:
       ```typescript
       /** Physically-based rendering maps for a surface material. Phase 32.
        *  All three URLs are served from `public/` (absolute paths starting with `/`).
        *  `tile.wFt` and `tile.lFt` are the real-world repeat size in feet; identical
        *  repeat applies to all three maps (D-04). */
       export interface PbrMaps {
         albedo: string;
         normal: string;
         roughness: string;
         tile: { wFt: number; lFt: number };
       }
       ```

    2. Extend `SurfaceMaterial`:
       ```typescript
       export interface SurfaceMaterial {
         id: string;
         label: string;
         color: string;
         roughness: number;
         surface: SurfaceTarget;
         defaultScaleFt: number;
         /** Optional PBR maps. Absence = flat-color render path (D-13). */
         pbr?: PbrMaps;
       }
       ```

    3. Populate `pbr` on the three named entries in `SURFACE_MATERIALS`:

       **WOOD_PLANK** — per D-20 6″×48″ planks = 0.5 ft × 4 ft:
       ```typescript
       pbr: {
         albedo: "/textures/wood-plank/albedo.jpg",
         normal: "/textures/wood-plank/normal.jpg",
         roughness: "/textures/wood-plank/roughness.jpg",
         tile: { wFt: 0.5, lFt: 4 },
       },
       ```

       **CONCRETE** — per D-20 4′×4′ tile = 4 ft × 4 ft:
       ```typescript
       pbr: {
         albedo: "/textures/concrete/albedo.jpg",
         normal: "/textures/concrete/normal.jpg",
         roughness: "/textures/concrete/roughness.jpg",
         tile: { wFt: 4, lFt: 4 },
       },
       ```

       **PLASTER** — per D-20 6′×6′ tile = 6 ft × 6 ft:
       ```typescript
       pbr: {
         albedo: "/textures/plaster/albedo.jpg",
         normal: "/textures/plaster/normal.jpg",
         roughness: "/textures/plaster/roughness.jpg",
         tile: { wFt: 6, lFt: 6 },
       },
       ```

    4. Do NOT add `pbr` to PAINTED_DRYWALL, WOOD_OAK, WOOD_WALNUT, TILE_WHITE, TILE_BLACK, CARPET, MARBLE, or STONE (D-13 + success criterion 2: these render unchanged).

    5. Leave existing `color`, `roughness`, `surface`, `defaultScaleFt` fields on all entries untouched — they remain the fallback render path per D-15.
  </action>
  <verify>
    <automated>grep -q "export interface PbrMaps" src/data/surfaceMaterials.ts && grep -q "pbr?: PbrMaps" src/data/surfaceMaterials.ts && grep -q "/textures/wood-plank/albedo.jpg" src/data/surfaceMaterials.ts && grep -q "/textures/concrete/albedo.jpg" src/data/surfaceMaterials.ts && grep -q "/textures/plaster/albedo.jpg" src/data/surfaceMaterials.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface PbrMaps" src/data/surfaceMaterials.ts` returns 1
    - `grep -c "pbr?: PbrMaps" src/data/surfaceMaterials.ts` returns 1 (field on SurfaceMaterial)
    - `grep -c "/textures/wood-plank/" src/data/surfaceMaterials.ts` returns 3 (albedo, normal, roughness)
    - `grep -c "/textures/concrete/" src/data/surfaceMaterials.ts` returns 3
    - `grep -c "/textures/plaster/" src/data/surfaceMaterials.ts` returns 3
    - `grep -c "wFt: 0.5" src/data/surfaceMaterials.ts` returns 1 (WOOD_PLANK plank width)
    - `grep -c "lFt: 4" src/data/surfaceMaterials.ts` returns 2 (WOOD_PLANK plank length + CONCRETE)
    - `grep -c "wFt: 6" src/data/surfaceMaterials.ts` returns 1 (PLASTER)
    - PAINTED_DRYWALL entry does NOT contain `pbr:` — verified by reading the entry block
    - `npx tsc --noEmit` exits 0 (no type errors)
    - All 11 existing material ids still present: `grep -c "^\s*[A-Z_]\+:\s*{" src/data/surfaceMaterials.ts` equals 11
  </acceptance_criteria>
  <done>PbrMaps type exported; SurfaceMaterial.pbr optional; three entries populated at D-20 tile sizes; eight others unchanged; TypeScript clean.</done>
</task>

</tasks>

<verification>
  <file_checks>
    - `ls public/textures/{wood-plank,concrete,plaster}/*.jpg` returns 9 files
    - `ls public/hdr/*.hdr` returns 1 file
    - `grep -c "pbr?:" src/data/surfaceMaterials.ts` returns 1
  </file_checks>
  <build_check>
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds (assets copied into dist/)
  </build_check>
</verification>

<success_criteria>
1. Three texture directories + nine maps + three LICENSE.txt files exist at correct resolutions
2. One HDR file + LICENSE.txt exist under ~500 KB
3. `PbrMaps` type exported; `SurfaceMaterial.pbr` optional
4. WOOD_PLANK, CONCRETE, PLASTER carry populated `pbr` blocks at D-20 tile sizes
5. PAINTED_DRYWALL + eight other entries unchanged
6. Build passes; no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-pbr-foundation/32-01-SUMMARY.md` documenting:
- Asset sources chosen (URLs from LICENSE.txt)
- Actual file sizes (`du -b`)
- PbrMaps type shape
- Which materials received pbr blocks
- Decisions implemented: D-10, D-11, D-13, D-14, D-20
- Decisions deferred: None (this plan is pure data)
</output>
