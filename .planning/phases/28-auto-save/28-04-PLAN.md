---
phase: 28-auto-save
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - .planning/phases/28-auto-save/28-VALIDATION.md
  - CLAUDE.md
autonomous: false
requirements: [SAVE-05, SAVE-06]

must_haves:
  truths:
    - "Per-Task Verification Map in 28-VALIDATION.md lists every task from Plans 01-03 with its automated command"
    - "Full vitest suite passes"
    - "Manual verification checklist confirms: wall edit → SAVING → SAVED cycle visible; continuous drag produces exactly one save; hard-refresh restores scene; blocked IndexedDB surfaces SAVE_FAILED and persists"
    - "nyquist_compliant flag flipped to true only if every task has an automated verify and all tests pass"
  artifacts:
    - path: ".planning/phases/28-auto-save/28-VALIDATION.md"
      provides: "Populated Per-Task Verification Map with sign-off"
      contains: "nyquist_compliant: true"
    - path: "CLAUDE.md"
      provides: "(Conditional) documentation update noting silent-restore + SAVE_FAILED behavior if materially new to reader"
  key_links:
    - from: ".planning/phases/28-auto-save/28-VALIDATION.md"
      to: "All Phase 28 task automated commands"
      via: "Table rows mapping task IDs to npx vitest commands"
      pattern: "npx vitest run"
---

<objective>
Close Phase 28 by populating the validation strategy with per-task coverage, running the full test suite, performing manual smoke tests that cover the visual/timing behaviors tests can't prove, and documenting the shipped behavior.

Purpose: Phase gate for `/gsd:verify-work`. Ensures Nyquist compliance (every task has automated verify), full regression is green, and Jessica's "never lose work" promise is validated end-to-end.
Output: Signed-off 28-VALIDATION.md with `nyquist_compliant: true`, optional CLAUDE.md addendum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-auto-save/28-CONTEXT.md
@.planning/phases/28-auto-save/28-RESEARCH.md
@.planning/phases/28-auto-save/28-VALIDATION.md
@.planning/phases/28-auto-save/28-01-PLAN.md
@.planning/phases/28-auto-save/28-02-PLAN.md
@.planning/phases/28-auto-save/28-03-PLAN.md
@.planning/phases/28-auto-save/28-01-SUMMARY.md
@.planning/phases/28-auto-save/28-02-SUMMARY.md
@.planning/phases/28-auto-save/28-03-SUMMARY.md

@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Populate 28-VALIDATION.md Per-Task Verification Map</name>
  <files>.planning/phases/28-auto-save/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-auto-save/28-VALIDATION.md (current skeleton with empty map)
    - .planning/phases/28-auto-save/28-01-PLAN.md (3 tasks)
    - .planning/phases/28-auto-save/28-02-PLAN.md (2 tasks)
    - .planning/phases/28-auto-save/28-03-PLAN.md (3 tasks)
    - 28-RESEARCH.md §"Phase Requirements → Test Map"
  </read_first>
  <action>
    Replace the placeholder row in the `## Per-Task Verification Map` table with one row per task from Plans 01, 02, and 03. Columns: Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status.

    Rows to add (use exact IDs — PLAN-TASK format):

    | 28-01-T1 | 01 | 0 | SAVE-05,SAVE-06 | infra | `npx vitest run tests/useAutoSave.test.ts` | Created Wave 0 | shipped |
    | 28-01-T2 | 01 | 0 | SAVE-05,SAVE-06 | unit (stubs) | `npx vitest run tests/useAutoSave.test.ts` | Extended Wave 0 | shipped |
    | 28-01-T3 | 01 | 0 | SAVE-05 | integration (stubs) | `npx vitest run tests/App.restore.test.tsx` | Created Wave 0 | shipped |
    | 28-02-T1 | 02 | 1 | SAVE-06 | unit | `npx vitest run tests/useAutoSave.test.ts -t "failed"` | yes | shipped |
    | 28-02-T2 | 02 | 1 | SAVE-05,SAVE-06 | unit | `npx vitest run tests/useAutoSave.test.ts` | yes | shipped |
    | 28-03-T1 | 03 | 2 | SAVE-05 | typecheck | `npx tsc --noEmit` | yes | shipped |
    | 28-03-T2 | 03 | 2 | SAVE-05 | unit | `npx vitest run tests/useAutoSave.test.ts` | yes | shipped |
    | 28-03-T3 | 03 | 2 | SAVE-05 | integration | `npx vitest run tests/App.restore.test.tsx` | yes | shipped |

    Do NOT yet flip `nyquist_compliant` in frontmatter — Task 4 does that after manual verifications pass. Do NOT sign off the `Validation Sign-Off` checklist yet either.
  </action>
  <verify>
    <automated>grep -c "28-01-T\|28-02-T\|28-03-T" .planning/phases/28-auto-save/28-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains '28-01-T1'`
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains '28-03-T3'`
    - Per-Task Verification Map contains at least 8 task rows
    - Each row has a non-empty Automated Command column
  </acceptance_criteria>
  <done>Per-Task Verification Map filled; every Plan 01-03 task mapped to a command</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full vitest suite and capture results</name>
  <files>(no code changes — execution + reporting only)</files>
  <read_first>
    - .planning/phases/28-auto-save/28-VALIDATION.md §"Sampling Rate" (full suite is the phase gate)
  </read_first>
  <action>
    Run `npx vitest run --reporter=dot`. Capture:
    - Total test files
    - Total tests passed / failed / skipped
    - Runtime

    If any test fails:
    1. Do NOT flip `nyquist_compliant` in Task 4
    2. Record the failing test name(s) and error excerpts in 28-04-SUMMARY.md
    3. Stop — the phase does not ship until green

    If all tests pass, proceed to Task 3.

    Do NOT attempt to fix failing tests in this plan. If a failure surfaces, surface it back to the orchestrator so Plans 01-03 can be amended via `--gaps`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - Output contains the phrase `Test Files` (vitest summary line)
    - All 10 Phase 28 stubs (6 useAutoSave + 4 App.restore) report as passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Full suite green; tsc clean; ready for manual smoke</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke verification (4 behaviors)</name>
  <what-built>
    Phase 28 auto-save hardening: SAVE_FAILED status, rename trigger, silent restore on mount, all flowing through the existing SAVE-04 toolbar surface.
  </what-built>
  <how-to-verify>
    1. **SAVE_FAILED visibility**
       - Run `npm run dev`, open the app
       - Open DevTools → Application → IndexedDB → Databases → right-click the `keyval-store` DB → "Delete database" or set the browser to Private Mode to block writes
       - Draw a wall (W tool, click two points)
       - Expected: Toolbar shows `SAVING...` then `SAVE_FAILED` in error color (text-error = salmon/red token)
       - Wait 5 seconds — `SAVE_FAILED` must PERSIST (not fade to idle)
       - Re-enable IndexedDB (exit private mode / allow storage) and draw another wall
       - Expected: status transitions failed → saving → saved → (after 2s) idle

    2. **Single save per drag**
       - Restore normal IndexedDB access
       - In DevTools → Application → IndexedDB, watch the `keyval-store` DB for write events
       - Place a product, then drag it for 3+ seconds continuously
       - Expected: Exactly ONE write to `room-cad-project-*` fires AFTER drag-end, NOT one per frame
       - Toolbar flashes SAVING... once, then SAVED

    3. **Hard-refresh restore**
       - Draw 2+ walls and place a product. Wait for SAVED to appear.
       - In DevTools, confirm `keyval-store` contains BOTH `room-cad-project-<id>` and `room-cad-last-project`
       - Hard refresh (Cmd+Shift+R / Ctrl+Shift+R)
       - Expected: Canvas opens directly to the saved scene — NO WelcomeScreen flash, walls + product visible
       - Scene must be pixel-identical to pre-refresh

    4. **Project rename triggers save**
       - Open Project Manager (via Toolbar)
       - Rename the active project from "Untitled Room" to "Jessica's Bedroom"
       - Expected: Toolbar shows SAVING... within ~2s, then SAVED
       - In IndexedDB, the `room-cad-project-<id>` record's `name` field reflects the new name

    Report result for EACH of the 4 behaviors: PASS or FAIL with notes.
  </how-to-verify>
  <resume-signal>
    Type "approved: all 4 passed" to proceed to Task 4. If any FAIL, describe which behavior and any error details — Phase 28 does not ship without all 4.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Flip nyquist_compliant and sign off VALIDATION.md</name>
  <files>.planning/phases/28-auto-save/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-auto-save/28-VALIDATION.md (post-Task-1 state)
    - Task 2 output (must have been green)
    - Task 3 checkpoint response (must have been "approved: all 4 passed")
  </read_first>
  <action>
    Only execute this task if Task 2 reported all tests passing AND Task 3 checkpoint was approved with all 4 manual behaviors passing.

    Edits to `.planning/phases/28-auto-save/28-VALIDATION.md`:

    1. Frontmatter:
       - Change `nyquist_compliant: false` → `nyquist_compliant: true`
       - Change `wave_0_complete: false` → `wave_0_complete: true`
       - Change `status: draft` → `status: signed`

    2. `Validation Sign-Off` section — check ALL boxes:
       - [x] All tasks have `<automated>` verify or Wave 0 dependencies
       - [x] Sampling continuity: no 3 consecutive tasks without automated verify
       - [x] Wave 0 covers all MISSING references
       - [x] No watch-mode flags
       - [x] Feedback latency < 20s
       - [x] `nyquist_compliant: true` set in frontmatter

    3. Change `**Approval:** pending` → `**Approval:** signed 2026-04-20`

    If either gate (Task 2 green, Task 3 approved) is not met: LEAVE the file in its current state and instead write a note in 28-04-SUMMARY.md describing which gate failed and what must be fixed before re-running this task.

    Additionally: Assess whether `CLAUDE.md` needs an update. Only modify CLAUDE.md if a reader coming fresh to the repo would be materially misled by its current auto-save description. If yes, add a 2-4 line note under the existing auto-save section covering: (a) SAVE_FAILED is persistent until next success, (b) silent restore via `room-cad-last-project` pointer on mount. If CLAUDE.md already reads correctly post-phase (likely, since the phase hardens existing behavior rather than inventing new), leave it alone.
  </action>
  <verify>
    <automated>grep "nyquist_compliant\|wave_0_complete\|status:" .planning/phases/28-auto-save/28-VALIDATION.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains 'nyquist_compliant: true'`
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains 'wave_0_complete: true'`
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains 'status: signed'`
    - `.planning/phases/28-auto-save/28-VALIDATION.md contains 'Approval:** signed'`
    - All 6 sign-off checkboxes marked `[x]`
  </acceptance_criteria>
  <done>Phase 28 validation signed; CLAUDE.md reviewed (and updated only if materially needed)</done>
</task>

</tasks>

<verification>
Final state check: `grep "nyquist_compliant: true" .planning/phases/28-auto-save/28-VALIDATION.md` returns a match. Full test suite still green via one last `npx vitest run`.
</verification>

<success_criteria>
- Per-Task Verification Map complete (8+ rows, every Plan 01-03 task mapped)
- `npx vitest run` exits 0 with all Phase 28 test stubs passing
- `npx tsc --noEmit` exits 0
- All 4 manual smoke behaviors verified: SAVE_FAILED persists, single-save-per-drag, hard-refresh restore, rename triggers save
- 28-VALIDATION.md signed (nyquist_compliant + wave_0_complete true, status signed, approval stamped)
- CLAUDE.md updated only if the phase materially changes documented behavior
</success_criteria>

<output>
After completion, create `.planning/phases/28-auto-save/28-04-SUMMARY.md` with: full-suite result, manual-verification results (per-behavior pass/fail), files touched count, and a Phase 28 wrap-up statement suitable for ROADMAP update.
</output>
