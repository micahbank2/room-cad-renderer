---
phase: 28-auto-save
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/useAutoSave.test.ts
  - tests/App.restore.test.tsx
  - vitest.config.ts
autonomous: true
requirements: [SAVE-05, SAVE-06]

must_haves:
  truths:
    - "Test harness supports fake timers and renderHook for useAutoSave"
    - "Failing/pending test stubs exist for: 2s debounce + single-save-per-drag, rename-trigger fires save, SAVE_FAILED persists, silent-restore on mount + WelcomeScreen fallback"
  artifacts:
    - path: "tests/useAutoSave.test.ts"
      provides: "Unit tests for debounce, rename trigger, ui-store no-trigger, save failure sets failed, failed does not fade, subsequent success clears failed"
    - path: "tests/App.restore.test.tsx"
      provides: "Integration tests for silent restore happy path, no-pointer fallthrough, stale-pointer fallthrough"
    - path: "vitest.config.ts"
      provides: "Vitest configuration (if not already present)"
  key_links:
    - from: "tests/useAutoSave.test.ts"
      to: "src/hooks/useAutoSave.ts"
      via: "renderHook import + vi.useFakeTimers"
      pattern: "renderHook.*useAutoSave"
    - from: "tests/App.restore.test.tsx"
      to: "src/App.tsx"
      via: "render(<App />) + idb-keyval pointer mock"
      pattern: "render.*App"
---

<objective>
Create Wave 0 test scaffolding for Phase 28 auto-save hardening. Every behavior that Plans 02-03 will implement must have a failing test stub in this plan so implementation drives tests to green.

Purpose: Enforce Nyquist compliance — every production task in Plans 02/03 must have a pre-existing automated verification command.
Output: Two test files + vitest config (if missing) with compilable but failing/pending stubs for all SAVE-05 and SAVE-06 acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-auto-save/28-CONTEXT.md
@.planning/phases/28-auto-save/28-RESEARCH.md
@.planning/phases/28-auto-save/28-VALIDATION.md

@tests/useAutoSave.test.ts
@src/hooks/useAutoSave.ts
@src/stores/projectStore.ts
@src/stores/cadStore.ts
@src/lib/serialization.ts
@src/App.tsx
@package.json

<interfaces>
<!-- Existing test pattern to mirror (from 28-RESEARCH.md §Validation Architecture). -->

From src/hooks/useAutoSave.ts:
```typescript
export const DEBOUNCE_MS = 2000;
export const FADE_MS = 2000;
export function useAutoSave(): void;
```

From src/stores/projectStore.ts (current):
```typescript
export type SaveStatus = "idle" | "saving" | "saved";  // Plan 02 adds "failed"
interface ProjectState {
  activeId: string | null;
  activeName: string;
  saveStatus: SaveStatus;
  setActive: (id: string, name: string) => void;
  setActiveName: (name: string) => void;
  clearActive: () => void;
  setSaveStatus: (s: SaveStatus) => void;
}
```

From src/lib/serialization.ts:
```typescript
export async function saveProject(id: string, name: string, snapshot: CADSnapshot): Promise<void>;
export async function loadProject(id: string): Promise<SavedProject | null>;
// Plan 03 adds: LAST_PROJECT_KEY, setLastProjectId, getLastProjectId
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm/install test deps + vitest.config.ts</name>
  <files>vitest.config.ts, package.json (only if deps missing)</files>
  <read_first>
    - package.json (check for vitest, @testing-library/react, @testing-library/jest-dom, happy-dom or jsdom)
    - vite.config.ts (may already contain inline test config)
    - tests/setup.ts (may already configure DOM env)
    - .planning/phases/28-auto-save/28-CONTEXT.md
    - .planning/phases/28-auto-save/28-RESEARCH.md §Environment Availability (confirms all deps already present)
  </read_first>
  <action>
    Per 28-RESEARCH.md §Environment Availability: vitest ^4.1.2, @testing-library/react, and happy-dom are all already installed. Do NOT run `npm install` unless a `grep` of package.json confirms a missing dep.

    Step 1: Read vite.config.ts. If it contains a `test: {...}` block already (it should per research), skip creating vitest.config.ts. Otherwise create a minimal vitest.config.ts at repo root:

    ```typescript
    import { defineConfig } from "vitest/config";
    import path from "path";
    export default defineConfig({
      test: {
        environment: "happy-dom",
        setupFiles: ["./tests/setup.ts"],
        globals: true,
      },
      resolve: {
        alias: { "@": path.resolve(__dirname, "./src") },
      },
    });
    ```

    Step 2: Verify `npx vitest run tests/useAutoSave.test.ts` can resolve `@/` imports without error (even if tests fail — this task only proves config is wired).

    Do NOT modify vite.config.ts if it already has a test block — vitest.config.ts (if needed) takes precedence.
  </action>
  <verify>
    <automated>npx vitest run tests/useAutoSave.test.ts --reporter=dot 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - Either `vite.config.ts contains 'test:'` OR `vitest.config.ts` exists at repo root
    - `npx vitest run tests/useAutoSave.test.ts` exits without ERR_MODULE_NOT_FOUND or "Cannot find module '@/...'" errors (failing tests are OK at this stage)
    - `package.json contains '"vitest"'` and `package.json contains '"@testing-library/react"'`
  </acceptance_criteria>
  <done>Test runner resolves `@/*` aliases and runs existing useAutoSave.test.ts to completion (pass or fail, no infra errors)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend tests/useAutoSave.test.ts with Phase 28 stubs</name>
  <files>tests/useAutoSave.test.ts</files>
  <read_first>
    - tests/useAutoSave.test.ts (existing 64 lines — preserve current tests, append new ones)
    - src/hooks/useAutoSave.ts
    - src/stores/projectStore.ts
    - src/stores/cadStore.ts
    - src/stores/uiStore.ts
    - .planning/phases/28-auto-save/28-CONTEXT.md (D-04, D-04a, D-05, D-05b)
    - .planning/phases/28-auto-save/28-RESEARCH.md §"Specific Assertions" and §"Code Examples"
  </read_first>
  <action>
    Append 6 new test cases to tests/useAutoSave.test.ts using the existing `vi.useFakeTimers()` + `renderHook(() => useAutoSave())` pattern already in that file. Preserve all existing tests verbatim.

    Add an import for `useProjectStore` and `useUIStore` at top if not already present. Mock `saveProject` per existing pattern (`vi.mock("@/lib/serialization", ...)`).

    Stubs to add (each uses `describe` / `it` with `.skip` OR a body that imports the not-yet-shipped `"failed"` status — so they fail until Plan 02/03 ships):

    1. `it("rename triggers save when activeId is non-null")` — call `useProjectStore.getState().setActive("proj_x", "A")`, advance 10ms, call `setActiveName("B")`, advance 2000ms + `await vi.runAllTimersAsync()`, assert `saveProject` was called with name `"B"`.

    2. `it("ui-store mutations do not trigger save")` — import `useUIStore`, call `setTool("wall")`, `toggleGrid()`, `setGridSnap(1)`, advance 3000ms, assert `saveProject` was NOT called.

    3. `it("save failure sets saveStatus to failed")` — mock `saveProject` to reject once, trigger a cadStore mutation (e.g., `useCADStore.getState().loadSnapshot({ version: 2, rooms: {...}, activeRoomId: "..." })` or use an existing helper), advance 2000ms, flush microtasks, assert `useProjectStore.getState().saveStatus === "failed"`.

    4. `it("failed status does not auto-fade")` — after setup from test 3, advance additional 5000ms (past FADE_MS), assert `saveStatus === "failed"` still.

    5. `it("subsequent successful save clears failed")` — after test 3 setup, make `saveProject` resolve, trigger another mutation, advance 2000ms + flush, assert `saveStatus === "saved"`, advance FADE_MS=2000ms more + flush, assert `saveStatus === "idle"`.

    6. `it("drag produces exactly one save at drag-end")` — simulate 50 rapid mutation calls on cadStore (all before mouse-up equivalent), advance 2000ms + flush, assert `saveProject.mock.calls.length === 1`.

    IMPORTANT: Tests 3-5 reference `"failed"` in the SaveStatus union — TypeScript will error until Plan 02 extends the union. Use `as any` cast on the equality check (e.g., `expect(useProjectStore.getState().saveStatus).toBe("failed" as any)`) so tests compile now and pass after Plan 02 ships.

    Do NOT implement the hook changes here. Tests must initially fail (red) — that's the point.
  </action>
  <verify>
    <automated>npx vitest run tests/useAutoSave.test.ts --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/useAutoSave.test.ts contains 'rename triggers save'`
    - `tests/useAutoSave.test.ts contains 'ui-store mutations do not trigger save'`
    - `tests/useAutoSave.test.ts contains 'save failure sets saveStatus to failed'`
    - `tests/useAutoSave.test.ts contains 'failed status does not auto-fade'`
    - `tests/useAutoSave.test.ts contains 'subsequent successful save clears failed'`
    - `tests/useAutoSave.test.ts contains 'drag produces exactly one save'`
    - File compiles (no TS errors when vitest runs)
    - At least 3 new tests FAIL (expected — they describe Plan 02 behavior not yet shipped)
  </acceptance_criteria>
  <done>6 new test cases present, file compiles, failing tests describe Plan 02 behavior</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create tests/App.restore.test.tsx with silent-restore stubs</name>
  <files>tests/App.restore.test.tsx</files>
  <read_first>
    - src/App.tsx (current lines 64-74 hydration — target for replacement in Plan 03)
    - src/lib/serialization.ts
    - src/components/WelcomeScreen.tsx (default fallback)
    - .planning/phases/28-auto-save/28-CONTEXT.md (D-02, D-02a, D-02b)
    - .planning/phases/28-auto-save/28-RESEARCH.md §"Silent restore with pointer"
  </read_first>
  <action>
    Create a new file `tests/App.restore.test.tsx`. Mock `idb-keyval` at module scope so `get("room-cad-last-project")` can be controlled per test. Mock `@/lib/serialization` to stub `loadProject`.

    Test cases (all initially FAILING — Plan 03 implements the behavior):

    1. `it("silent restore: valid pointer loads project and skips WelcomeScreen")` — mock `get("room-cad-last-project")` → `"proj_abc"`, mock `loadProject("proj_abc")` → `{ id, name, snapshot: {...} }` with at least one wall. Render `<App />`. Await flushed microtasks. Assert the canvas renders (NOT WelcomeScreen — look for absence of a unique WelcomeScreen testid/text like `"DESIGN_YOUR_SPACE"` or the presence of a Toolbar testid).

    2. `it("no pointer: falls through to WelcomeScreen")` — mock `get` → `undefined`. Render `<App />`. Await flush. Assert WelcomeScreen visible (query for the brand/CTA text).

    3. `it("stale pointer: loadProject returns null → falls through to WelcomeScreen")` — mock `get` → `"proj_stale"`, mock `loadProject` → `null`. Render `<App />`. Await flush. Assert WelcomeScreen visible.

    4. `it("loadProject throws: falls through to WelcomeScreen")` — mock `get` → `"proj_x"`, mock `loadProject` → `.mockRejectedValue(new Error("QuotaExceededError"))`. Render. Await flush. Assert WelcomeScreen visible AND no unhandled promise rejection was thrown.

    Use `@testing-library/react` `render` + `screen.findByText` / `queryByText`. If App.tsx imports lazy components that require complex mocking, use `vi.mock("@/three/ThreeViewport")` to stub.

    Do NOT implement any production code — these are stubs that will drive Plan 03 to completion.
  </action>
  <verify>
    <automated>npx vitest run tests/App.restore.test.tsx --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/App.restore.test.tsx` exists
    - `tests/App.restore.test.tsx contains 'room-cad-last-project'`
    - `tests/App.restore.test.tsx contains 'silent restore'`
    - `tests/App.restore.test.tsx contains 'no pointer'`
    - `tests/App.restore.test.tsx contains 'stale pointer'`
    - File compiles under vitest
    - At least 2 tests FAIL (describing Plan 03 behavior)
  </acceptance_criteria>
  <done>New test file with 4 silent-restore stubs, compiles, initial state is failing (red)</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/useAutoSave.test.ts tests/App.restore.test.tsx --reporter=dot`. Existing tests pass, new stubs fail (red state is correct — Plans 02/03 drive them to green).
</verification>

<success_criteria>
- vitest runs both test files without infra errors
- 6 new useAutoSave stubs + 4 new App.restore stubs present
- Failing tests precisely describe Phase 28 acceptance criteria from CONTEXT.md D-02..D-05
</success_criteria>

<output>
After completion, create `.planning/phases/28-auto-save/28-01-SUMMARY.md` listing the test IDs added, initial pass/fail counts, and any infra notes (e.g., vitest.config.ts created or pre-existing).
</output>
