---
phase: 03-3d-product-rendering
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/productTextureCache.test.ts
  - tests/floorTexture.test.ts
  - tests/exportFilename.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "`npm test -- --run` exits 0 with 3 new stub suites collected"
    - "All Phase 3 downstream plans can reference an existing test file path"
  artifacts:
    - path: "tests/productTextureCache.test.ts"
      provides: "stub suite for VIZ-04 texture cache (hit/miss, error fallback)"
    - path: "tests/floorTexture.test.ts"
      provides: "stub suite for VIZ-06 procedural floor (texture dims, tile-scale helper)"
    - path: "tests/exportFilename.test.ts"
      provides: "stub suite for SAVE-03 filename formatter"
  key_links:
    - from: "tests/productTextureCache.test.ts"
      to: "src/three/productTextureCache.ts"
      via: "import under test (will be created in plan 01)"
      pattern: "productTextureCache"
    - from: "tests/floorTexture.test.ts"
      to: "src/three/floorTexture.ts"
      via: "import under test (will be created in plan 02)"
      pattern: "floorTexture"
    - from: "tests/exportFilename.test.ts"
      to: "src/lib/export.ts"
      via: "import under test (extended in plan 03)"
      pattern: "formatExportFilename|exportFilename"
---

<objective>
Create Wave 0 stub test files for Phase 3 matching 03-VALIDATION.md task IDs. These stubs are populated by downstream plans (01, 02, 03) as they implement the real modules.

Purpose: Honor Nyquist validation contract — every downstream task must reference a real test file path. Vitest config already exists from Phase 1; no new framework install needed.
Output: 3 stub test files under `tests/` using `it.todo` entries that collect cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-3d-product-rendering/03-VALIDATION.md
@.planning/phases/03-3d-product-rendering/03-CONTEXT.md
@vitest.config.ts
@tests/setup.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 3 Phase 3 stub test files</name>
  <read_first>
    - /Users/micahbank/room-cad-renderer/.planning/phases/03-3d-product-rendering/03-VALIDATION.md (Wave 0 section + per-task verification map)
    - /Users/micahbank/room-cad-renderer/tests/productImageCache.test.ts (Phase 1 stub shape reference)
  </read_first>
  <files>tests/productTextureCache.test.ts, tests/floorTexture.test.ts, tests/exportFilename.test.ts</files>
  <action>
    Create each file with `describe` + `it.todo` stubs. Use `it.todo` (not `it.skip`) — vitest collects and reports them without failing.

    /Users/micahbank/room-cad-renderer/tests/productTextureCache.test.ts:
    ```ts
    import { describe, it } from "vitest";
    describe("productTextureCache (VIZ-04)", () => {
      it.todo("cache hit: returns same THREE.Texture instance on repeat getTexture(url) calls");
      it.todo("cache miss: calls TextureLoader.loadAsync on first call for a url");
      it.todo("error fallback: rejected loadAsync resolves getTexture to null without throwing");
      it.todo("colorSpace: cached texture has SRGBColorSpace set");
    });
    ```

    /Users/micahbank/room-cad-renderer/tests/floorTexture.test.ts:
    ```ts
    import { describe, it } from "vitest";
    describe("floorTexture (VIZ-06)", () => {
      it.todo("createFloorTexture: returns THREE.CanvasTexture with 512x512 source canvas");
      it.todo("getFloorTexture: module-level memoization returns same texture instance on repeat calls");
      it.todo("tileRepeatFor: room 16x12 at 4ft scale returns repeat (4, 3)");
      it.todo("tileRepeatFor: room 8x8 at 4ft scale returns repeat (2, 2)");
    });
    ```

    /Users/micahbank/room-cad-renderer/tests/exportFilename.test.ts:
    ```ts
    import { describe, it } from "vitest";
    describe("exportFilename (SAVE-03)", () => {
      it.todo("formatExportFilename: Date 2026-04-04T23:15 yields room-20260404-2315.png");
      it.todo("formatExportFilename: pads single-digit month/day/hour/minute with leading zeros");
      it.todo("formatExportFilename: uses local time components, not UTC");
    });
    ```
  </action>
  <verify>
    <automated>test -f tests/productTextureCache.test.ts && test -f tests/floorTexture.test.ts && test -f tests/exportFilename.test.ts && npm test -- --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/micahbank/room-cad-renderer/tests/productTextureCache.test.ts` succeeds
    - `test -f /Users/micahbank/room-cad-renderer/tests/floorTexture.test.ts` succeeds
    - `test -f /Users/micahbank/room-cad-renderer/tests/exportFilename.test.ts` succeeds
    - `grep -c "it.todo" tests/productTextureCache.test.ts` returns 4
    - `grep -c "it.todo" tests/floorTexture.test.ts` returns 4
    - `grep -c "it.todo" tests/exportFilename.test.ts` returns 3
    - `grep -q "VIZ-04" tests/productTextureCache.test.ts` succeeds
    - `grep -q "VIZ-06" tests/floorTexture.test.ts` succeeds
    - `grep -q "SAVE-03" tests/exportFilename.test.ts` succeeds
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>3 stub test files exist with `it.todo` entries; vitest run exits 0.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0
- All 3 new Phase 3 test files contain `describe` + `it.todo` entries
</verification>

<success_criteria>
Downstream Phase 3 plans can add real `it()` tests to these files matching the task IDs from 03-VALIDATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/03-3d-product-rendering/03-00-SUMMARY.md` describing the 3 stub files created.
</output>
