---
phase: 34
slug: user-uploaded-textures
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-22
---

# Phase 34 — UI Design Contract

> Visual and interaction contract for User-Uploaded Textures. Generated by gsd-ui-researcher.
> All decisions pre-populated from CONTEXT.md (D-01–D-12), RESEARCH.md, and CLAUDE.md Phase 33 design system.
> No user questions required — all contract fields answered by upstream artifacts.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Obsidian CAD theme) |
| Preset | not applicable |
| Component library | none — custom component set only |
| Icon library | lucide-react ONLY for all new UI chrome (D-33 policy). Material Symbols RESERVED to 8 existing allowlist files. New components (UploadTextureModal, MyTexturesList, DeleteTextureDialog) MUST NOT import material-symbols-outlined. |
| Font (mono) | IBM Plex Mono — all labels, values, identifiers, status strings |
| Font (body) | Inter — descriptive prose, helper text in modals |
| Font (display) | Space Grotesk — not used in Phase 34 UI surfaces (no hero headings in pickers or modals) |

Source: CLAUDE.md §Design System (Phase 33 — v1.7.5), D-33.

---

## Spacing Scale

Canonical scale from `src/index.css` `@theme {}` — D-34. No exceptions for Phase 34 surfaces.

| Token | Value | Tailwind utility | Usage in Phase 34 |
|-------|-------|-----------------|-------------------|
| xs | 4px | `p-1` / `gap-1` | Icon-to-label gaps inside LibraryCard, ⋮ menu item padding |
| sm | 8px | `p-2` / `gap-2` | Card internal padding, input field gap, drag-zone internal spacing |
| lg | 16px | `p-4` / `gap-4` | Modal section padding, grid gaps in MY TEXTURES list |
| xl | 24px | `p-6` / `gap-6` | Modal header/footer padding |
| 2xl | 32px | `p-8` / `gap-8` | Modal outer padding on wide viewports |

Exceptions: none. `p-3` / `m-3` / `gap-3` / any `p-[Npx]` arbitrary values are PROHIBITED in the following new files: `UploadTextureModal.tsx`, `MyTexturesList.tsx`, `DeleteTextureDialog.tsx`. Per D-34 checker rule.

Touch target minimum: 44px (height) on interactive icon buttons (⋮ menu trigger, X close, Upload tile). Achieved via `min-h-[44px]` on those buttons — this is the only approved arbitrary value exception. All icon sizes: 16px (`size-4` via lucide `size` prop).

---

## Typography

5-tier ramp, 2 weights, from CLAUDE.md D-03. Phase 34 uses 3 of the 5 tiers.

| Role | Size | Font | Weight | Line Height | Case | Usage in Phase 34 |
|------|------|------|--------|-------------|------|-------------------|
| Modal heading | 13px (`text-base`) | IBM Plex Mono | 500 | 1.2 | UPPERCASE + tracking-widest | "UPLOAD TEXTURE", "MY TEXTURES", "DELETE TEXTURE", "EDIT TEXTURE" |
| Label / field label | 11px (`text-sm`) | IBM Plex Mono | 500 | 1.4 | UPPERCASE + tracking-wide | "NAME", "TILE SIZE", "FILE", section headers in picker |
| Value / body / helper | 11px (`text-sm`) | IBM Plex Mono | 400 | 1.5 | Mixed-case | Field values, helper text, error messages, ref-count copy, empty state copy |
| Prose | 13px (`text-base`) | Inter | 400 | 1.5 | Sentence-case | Descriptive sentences in modal (e.g. "Drag and drop a photo, or click to browse."), delete confirm detail |

All user-facing label text that is a dynamic CAD identifier (texture name displayed in card, surface ID) renders `.toUpperCase()` — matching established CAD UI convention.

---

## Color

Obsidian CAD palette from `src/index.css`. 60/30/10 split:

| Role | Token / Hex | Tailwind utility | Usage |
|------|-------------|-----------------|-------|
| Dominant (60%) | `#12121d` `--color-obsidian-base` | `bg-obsidian-base` | Modal backdrop root; picker tab content area |
| Secondary (30%) | `#1f1e2a` `--color-obsidian-mid` | `bg-obsidian-mid` | Modal surface (`bg-obsidian-mid/90` with `backdrop-blur-xl`), card backgrounds in MY TEXTURES grid; drag-drop zone idle state |
| Tertiary surface | `#1b1a26` `--color-obsidian-low` | `bg-obsidian-low` | MY TEXTURES grid container background; picker tab panel background |
| Hover states | `#292935` `--color-obsidian-high` | `bg-obsidian-high` | LibraryCard hover, ⋮ menu item hover, Upload tile hover |
| Elevated | `#343440` `--color-obsidian-highest` | `bg-obsidian-highest` | ⋮ dropdown menu background |
| Accent (10%) | `#7c5bf0` `--color-accent` | `bg-accent` / `text-accent` | Reserved for: (1) selected LibraryCard ring, (2) active "MY TEXTURES" tab indicator, (3) Save button background, (4) drag-over zone border highlight, (5) Upload tile `+` icon on hover |
| Accent light | `#ccbeff` `--color-accent-light` | `text-accent-light` | Tile size value display in card, active tab label text |
| Destructive | `#ffb4ab` `--color-error` | `text-error` | Delete button text in confirm dialog; inline MIME/size error copy under file field |
| Text primary | `#e3e0f1` | `text-text-primary` | Texture name in card, modal heading, field input text |
| Text muted | `#cac3d7` | `text-text-muted` | Helper text, ref-count prose in delete dialog |
| Text dim | `#938ea0` | `text-text-dim` | Tile size label in card, field label secondary |
| Text ghost | `#484554` | `text-text-ghost` | Placeholder text in inputs, empty state supporting copy |
| Border default | `#484554/20` opacity | `border-outline-variant/20` | Modal border, card border, input border |
| Border drag-over | `#7c5bf0` (accent) | `border-accent` | Drop zone active state (dashed → solid accent) |

Accent reserved for ONLY the 5 elements listed above. Not for general hover states or informational text.

Destructive color (`--color-error`) is used ONLY for: delete confirm button text, inline error messages. Not for warnings.

---

## Component Inventory

New components to create in Phase 34. Each has its own visual contract:

### 1. UploadTextureModal (`src/components/UploadTextureModal.tsx`)

**Trigger:** The `+ UPLOAD TEXTURE` slot at the end of the MY TEXTURES grid, or the `+ UPLOAD` action in an empty MY TEXTURES state.

**Mode prop:** `mode: "create" | "edit"` — reuse for both upload and catalog edit (D-11). Edit mode pre-fills name + tile size; no drop zone shown in edit mode (file is fixed at upload time).

**Layout:** Fixed-position centered modal, `w-[520px]`. Same shape as `AddProductModal`: `fixed inset-0 z-50 flex items-center justify-center`.

**Backdrop:** `bg-obsidian-deepest/80 backdrop-blur-sm`. Click to dismiss = Cancel.

**Modal surface:** `bg-obsidian-mid/90 backdrop-blur-xl border border-outline-variant/20 rounded-sm shadow-2xl`.

**Header:** `p-6 pb-4`. Label: `UPLOAD TEXTURE` (create mode) / `EDIT TEXTURE` (edit mode). Font: IBM Plex Mono 13px 500 UPPERCASE tracking-widest, `text-text-primary`. Close button: lucide `X` icon, 16px, `text-text-ghost hover:text-text-primary`.

**Drop zone (create mode only):**
- Idle: `rounded-md border-2 border-dashed border-outline-variant/40 bg-obsidian-low` with `p-8` internal padding. Centered content: lucide `Upload` icon (24px, `text-text-dim`) + prose text "Drag and drop a photo, or click to browse." (Inter 13px, `text-text-muted`). Click opens file picker.
- Drag-over: `border-accent bg-accent/5` transition. Icon becomes `text-accent`.
- Preview loaded: Drop zone collapses; preview thumbnail appears instead (see below).

**File input:** `<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden">`. JS also validates `file.type`.

**Preview thumbnail (create mode, after file selected):**
- 160px × 120px (`w-40 h-30`) cover-fit square with `rounded-sm` and `border border-outline-variant/20`.
- Shows downscaled image via `URL.createObjectURL(blob)` after pipeline completes.
- "Change" link (11px, `text-accent`) below thumbnail to re-open file picker.

**Name input:**
- Label: `NAME` (IBM Plex Mono 11px 500 UPPERCASE `text-text-dim`).
- Input: `bg-obsidian-low border border-outline-variant/20 rounded-sm px-2 py-1 text-sm font-mono text-text-primary w-full placeholder:text-text-ghost`.
- Placeholder: `"e.g. Oak Floor"`
- Max length: 40 characters (matches `labelOverride` cap from Phase 31).

**Tile size input:**
- Label: `TILE SIZE` (same style as Name label). Helper: `"Real-world repeat (e.g. 2'6\")"` — Inter 11px `text-text-ghost`.
- Input: same style as Name. Placeholder: `"2'"`. Default pre-fill: `"2'"` (2.0 ft).
- Parsed via `validateInput` from `src/canvas/dimensionEditor.ts` on blur + on Save attempt.
- Invalid state: red underline `border-error` + inline error below: `"Enter a valid size like 2', 1'6\", or 0.5"` — IBM Plex Mono 11px `text-error`.

**Inline MIME error (create mode):** Rendered below the drop zone, NOT as a toast. Copy: `"Only JPEG, PNG, and WebP are supported."` — IBM Plex Mono 11px `text-error`.

**Inline oversize/decode error:** `"This file couldn't be processed. Try a different image."` — same style.

**Progress state:** Save button shows spinner (lucide `Loader2` 14px with `animate-spin`) + label `"Saving…"`. Button disabled during processing. Reduced-motion guard: when `useReducedMotion()` is true, no spin animation — show static `Loader2` icon.

**Footer:** `flex justify-end gap-2 p-6 pt-4`.
- Cancel: `rounded-sm px-4 py-1 font-mono text-sm text-text-muted hover:text-text-primary bg-obsidian-high hover:bg-obsidian-highest border border-outline-variant/20`.
- Save: `rounded-sm px-4 py-1 font-mono text-sm text-text-primary bg-accent hover:bg-accent/90 border-0`. Disabled state: `opacity-50 cursor-not-allowed`.
- Save disabled when: no file selected (create mode), name is empty, tile size is invalid, or processing is in flight.

**Toast on success:** `"Texture saved."` — uses existing `sonner` toast infrastructure. No details needed.

---

### 2. MyTexturesList (`src/components/MyTexturesList.tsx`)

**Purpose:** Shared sub-component consumed by FloorMaterialPicker, SurfaceMaterialPicker (ceiling), and WallSurfacePanel. Renders the MY TEXTURES tab content.

**Grid layout:** `grid grid-cols-3 gap-4 p-4` inside the tab panel.

**Ordering:** Most recently uploaded first (D-06). No sort UI.

**Upload slot:** Last tile in the grid. Rendered as a `LibraryCard`-shaped tile:
- Same dimensions as LibraryCard tiles.
- Background: `bg-obsidian-low border-2 border-dashed border-outline-variant/30 rounded-md`.
- Centered content: lucide `Plus` icon (20px, `text-text-ghost`) above label `"UPLOAD"` (IBM Plex Mono 11px 400 `text-text-ghost`).
- Hover: `border-accent/50 text-accent` on icon, `text-accent-light` on label. Cursor: `cursor-pointer`.
- Click opens UploadTextureModal in `mode="create"`.

**Texture card (per uploaded texture):**
- Reuses `LibraryCard` component with `thumbnail` = ObjectURL of the stored Blob.
- Card dimensions: match existing LibraryCard tile size in the picker grid.
- Bottom overlay: two lines — texture name (IBM Plex Mono 11px 500 UPPERCASE `text-text-primary`), tile size (IBM Plex Mono 11px 400 `text-accent-light` e.g. `"2'0"`).
- Selected state: `ring-2 ring-accent ring-offset-1 ring-offset-obsidian-low`.
- Click to apply: clicking card calls picker's `onSelect(userTextureId)` action. This is the primary apply gesture.
- ⋮ menu: `MoreHorizontal` lucide icon (14px), revealed on card hover (not always visible). Position: top-right corner of card, `absolute top-1 right-1`. Opens dropdown with two items:
  - `Edit` — opens UploadTextureModal in `mode="edit"` with current name + tile size pre-filled.
  - `Delete` — opens DeleteTextureDialog.
- ⋮ dropdown menu: `bg-obsidian-highest border border-outline-variant/20 rounded-sm shadow-lg`. Items: `px-2 py-1 font-mono text-[11px] text-text-muted hover:bg-obsidian-high hover:text-text-primary cursor-pointer`. Delete item: `text-error hover:text-error`.

**Empty state (no textures yet, MY TEXTURES tab active):**
- Replaces the grid entirely.
- Layout: centered column, `py-8 px-4 text-center`.
- No illustration (matches minimalist CAD aesthetic).
- Heading: `"NO CUSTOM TEXTURES"` — IBM Plex Mono 13px 500 UPPERCASE `text-text-dim`.
- Body: `"Upload a photo of a surface to use it on walls, floors, and ceilings."` — Inter 13px 400 `text-text-muted`.
- CTA: same Upload slot tile rendered below the prose (or a standalone `"+ UPLOAD TEXTURE"` text-link in `text-accent font-mono text-[11px]`).

---

### 3. DeleteTextureDialog (`src/components/DeleteTextureDialog.tsx`)

**Trigger:** "Delete" in the texture card ⋮ menu.

**Layout:** Centered modal, `w-[400px]`. Same modal shell as UploadTextureModal.

**Header:** `"DELETE TEXTURE"` — IBM Plex Mono 13px 500 UPPERCASE tracking-widest `text-text-primary`. No close button (use Cancel button).

**Body copy:**

When ref-count > 0:
```
Delete {TEXTURE_NAME}? {N} surface{s} in this project use it. They'll fall back to their base color.
```
Where `{TEXTURE_NAME}` = the texture's name rendered UPPERCASE in `text-text-primary`. `{N}` is the integer ref-count. `{s}` = "s" if N > 1, "" if N = 1. Body text font: Inter 13px `text-text-muted`.

When ref-count = 0:
```
Delete {TEXTURE_NAME}? This texture isn't used by any surface.
```

**Footer:** `flex justify-end gap-2`.
- Cancel: same Cancel style as UploadTextureModal.
- Delete: `rounded-sm px-4 py-1 font-mono text-sm text-error bg-obsidian-high hover:bg-error/10 border border-error/30`.

Source: D-07 (exact copy shape locked).

---

## Copywriting Contract

All copy is pre-locked by D-03a and D-07. No ambiguity.

| Element | Copy |
|---------|------|
| Modal title (create) | `UPLOAD TEXTURE` |
| Modal title (edit) | `EDIT TEXTURE` |
| Modal title (delete) | `DELETE TEXTURE` |
| Primary CTA (create) | `Save` |
| Primary CTA (edit) | `Save` |
| Destructive CTA | `Delete` |
| Cancel CTA | `Cancel` |
| Progress label (Save button) | `Saving…` |
| Empty state heading | `NO CUSTOM TEXTURES` |
| Empty state body | `Upload a photo of a surface to use it on walls, floors, and ceilings.` |
| MIME error (inline) | `Only JPEG, PNG, and WebP are supported.` |
| Decode/oversize error (inline) | `This file couldn't be processed. Try a different image.` |
| Tile size validation error | `Enter a valid size like 2', 1'6", or 0.5` |
| Name empty error | `Name is required.` |
| Success toast | `Texture saved.` |
| Delete confirm (N > 0) | `Delete {NAME}? {N} surface{s} in this project use it. They'll fall back to their base color.` |
| Delete confirm (N = 0) | `Delete {NAME}? This texture isn't used by any surface.` |
| Drop zone invite | `Drag and drop a photo, or click to browse.` |
| Tile size helper | `Real-world repeat (e.g. 2'6")` |
| Upload slot label | `UPLOAD` |
| MY TEXTURES tab label | `MY TEXTURES` |
| ⋮ menu — Edit | `Edit` |
| ⋮ menu — Delete | `Delete` |

Source: D-03a (MIME copy), D-07 (delete dialog copy). All other copy follows CAD theme tone (UPPERCASE for headers, sentence-case for prose).

---

## Interaction States

### Upload flow (create mode)

| State | Visual |
|-------|--------|
| Idle (no file) | Drop zone visible, Save disabled |
| Drag-over | Drop zone border → solid accent, bg-accent/5 |
| File selected (processing) | Drop zone replaced by thumbnail, Save shows spinner (`Saving…`), inputs locked |
| MIME rejected | Inline error under drop zone, drop zone resets to idle |
| Decode failure | Inline error under drop zone |
| Ready to save | Preview + filled name + valid tile size → Save enabled |
| Saving in flight | Save button spinner, all inputs + Cancel disabled |
| Save success | Modal closes, toast appears |

### MY TEXTURES tab states

| State | Visual |
|-------|--------|
| Empty | Empty state card (no grid) |
| Loading (hook mount) | Grid placeholder — 3 skeleton tiles matching LibraryCard dimensions: `bg-obsidian-highest animate-pulse rounded-md`. Reduced-motion guard: no pulse, just static `bg-obsidian-highest`. |
| Populated | Grid of texture cards + Upload slot tile at end |
| Card selected | `ring-2 ring-accent` |
| Card hover | ⋮ button revealed (`opacity-100`), hidden at rest (`opacity-0`) |
| ⋮ menu open | Dropdown positioned top-right of card |

### Delete dialog states

| State | Visual |
|-------|--------|
| Loading ref-count | Delete button shows `Loader2` spinner, disabled |
| Count resolved | Full copy rendered, Delete enabled |
| Deleting | Delete button spinner (`Deleting…`), both buttons disabled |
| Complete | Dialog closes, texture disappears from grid |

---

## Animation / Motion

All animations guard on `useReducedMotion()` from `src/hooks/useReducedMotion.ts` (D-39).

| Animation | Default | Reduced motion |
|-----------|---------|----------------|
| Modal open | Fade-in + subtle scale (`opacity-0 scale-95` → `opacity-100 scale-100`, 150ms ease-out) | Snap open (no transition) |
| Modal close | Fade-out + scale (`opacity-100 scale-100` → `opacity-0 scale-95`, 100ms ease-in) | Snap close |
| Drop zone drag-over | Border color + bg transition (100ms) | Snap |
| Skeleton loading pulse | `animate-pulse` | Static bg |
| Save button spinner | `animate-spin` | Static icon |
| ⋮ menu open | Fade-in (100ms) | Snap |

Implementation: use `transition-[opacity,transform]` Tailwind utilities, not custom keyframe CSS, to stay within the design system.

---

## Picker Integration Points

Three existing components receive MY TEXTURES tab. Visual contract for each is identical — only the `onSelect` callback differs.

| Picker | File | `onSelect` action |
|--------|------|------------------|
| FloorMaterialPicker | `src/components/FloorMaterialPicker.tsx` | `cadStore.setFloorMaterial({ kind: "user-texture", userTextureId, scaleFt: tileSizeFt, rotationDeg: 0 })` |
| SurfaceMaterialPicker (ceiling) | `src/components/SurfaceMaterialPicker.tsx` | `cadStore.updateCeiling(ceilingId, { userTextureId })` |
| WallSurfacePanel | `src/components/WallSurfacePanel.tsx` | Sets `wall.wallpaper.{A|B}.userTextureId` via appropriate store action |

The `CategoryTabs` `MY TEXTURES` tab entry: `{ id: "my-textures", label: "MY TEXTURES", count: textures.length }`. Count badge only shows when `textures.length > 0`.

---

## Spacing Scale

| Token | Value | Tailwind utility |
|-------|-------|-----------------|
| xs | 4px | `p-1` / `gap-1` |
| sm | 8px | `p-2` / `gap-2` |
| lg | 16px | `p-4` / `gap-4` |
| xl | 24px | `p-6` / `gap-6` |
| 2xl | 32px | `p-8` / `gap-8` |

Phase 34 does not add new spacing tokens. 12px (`p-3`) and arbitrary `p-[Npx]` values are PROHIBITED in new Phase 34 component files.

---

## Registry Safety

No third-party component registries used. No shadcn. All components are bespoke within the Obsidian CAD design system.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | n/a | not applicable |

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Upload entry point (picker-internal, no separate tab) | CONTEXT.md D-01 |
| Modal flow (no wizard) | CONTEXT.md D-02 |
| Single file only | CONTEXT.md D-03 |
| Inline errors + toast success | CONTEXT.md D-03a |
| MY TEXTURES CategoryTab | CONTEXT.md D-04 |
| Any texture to any surface | CONTEXT.md D-05 |
| Most-recent-first ordering | CONTEXT.md D-06 |
| Delete confirm with ref-count copy | CONTEXT.md D-07 |
| Silent orphan fallback | CONTEXT.md D-08 |
| Tile size editable post-upload via ⋮ menu | CONTEXT.md D-11 |
| No per-placement tile override | CONTEXT.md D-10 (deferred) |
| Icon library (lucide-react only for new chrome) | CLAUDE.md D-33 |
| Spacing tokens (4/8/16/24/32px only) | CLAUDE.md D-34, src/index.css |
| Radius (2/4/8px) | CLAUDE.md D-34, src/index.css |
| Typography ramp (13px/11px, mono/body) | CLAUDE.md D-03 |
| Obsidian color palette | src/index.css @theme |
| Reduced-motion guard | CLAUDE.md D-39 |
| Modal shell shape | AddProductModal.tsx reference |
| LibraryCard + CategoryTabs reuse | RESEARCH.md §D |
| Tile-size parser reuse (validateInput) | RESEARCH.md §F |
| `parseFeetInches` location | RESEARCH.md §F |

---

## Out of Scope (Deferred — Do Not Design)

- Per-placement tile-size override UI (D-10 deferred)
- Bulk upload queue / multi-file drop
- Per-surface orphan badge / indicator
- Surface-type tagging at upload
- Sort controls on MY TEXTURES list
- User-uploaded normal / roughness map fields

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
