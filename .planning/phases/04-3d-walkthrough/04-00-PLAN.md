---
phase: 04-3d-walkthrough
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/walkCollision.test.ts
  - tests/uiStore.test.ts
autonomous: true
requirements:
  - VIZ-05
must_haves:
  truths:
    - "tests/walkCollision.test.ts exists with it.todo stubs that vitest collects"
    - "tests/uiStore.test.ts exists with it.todo stubs for cameraMode state"
    - "npm test -- --run completes without collection errors"
  artifacts:
    - path: "tests/walkCollision.test.ts"
      provides: "Stubs for canMoveTo AABB + room bounds clamp"
      contains: "describe(\"walkCollision"
    - path: "tests/uiStore.test.ts"
      provides: "Stubs for cameraMode state machine"
      contains: "describe(\"uiStore cameraMode"
  key_links:
    - from: "tests/walkCollision.test.ts"
      to: "src/three/walkCollision.ts"
      via: "future import (not yet created)"
      pattern: "canMoveTo"
    - from: "tests/uiStore.test.ts"
      to: "src/stores/uiStore.ts"
      via: "future import of cameraMode actions"
      pattern: "cameraMode"
---

<objective>
Create Wave 0 test stubs for Phase 4 (3D Walkthrough). These files define the contract for walk-mode collision (canMoveTo) and camera-mode state (cameraMode/toggleCameraMode) using it.todo placeholders so vitest collects them cleanly before implementation lands in Wave 1.

Purpose: Per .planning/phases/04-3d-walkthrough/04-VALIDATION.md, Wave 0 stubs gate nyquist compliance and must exist before 04-01 fills them in.
Output: Two stub test files that vitest can collect without throwing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-3d-walkthrough/04-CONTEXT.md
@.planning/phases/04-3d-walkthrough/04-VALIDATION.md

<interfaces>
Existing test pattern (from tests/geometry.test.ts) — use `it.todo` for stubs per Phase 02 precedent:
```ts
import { describe, it, expect } from "vitest";
describe("name", () => {
  it.todo("behavior not yet implemented");
});
```

Wave 1 will import from (not yet created):
- `@/three/walkCollision` → `canMoveTo(from: {x,z}, to: {x,z}, walls: WallSegment[], room: Room, padding?: number): {x,z}`
- `@/stores/uiStore` → `useUIStore.getState().cameraMode | setCameraMode | toggleCameraMode`

Existing types (src/types/cad.ts):
```ts
export interface Room { width: number; length: number; wallHeight: number; }
export interface WallSegment { id: string; start: Point; end: Point; thickness: number; height: number; openings: Opening[]; }
export interface Point { x: number; y: number; }
```
Note: 2D canvas Point.y corresponds to Three.js z-axis in the walk-collision domain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create walkCollision + uiStore cameraMode test stubs</name>
  <files>tests/walkCollision.test.ts, tests/uiStore.test.ts</files>
  <read_first>
    - tests/geometry.test.ts (reference test style: vitest imports, describe/it)
    - tests/productStore.test.ts (reference for store tests with getState())
    - .planning/phases/04-3d-walkthrough/04-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create `tests/walkCollision.test.ts` with EXACTLY this content:

    ```ts
    import { describe, it } from "vitest";

    describe("walkCollision canMoveTo", () => {
      it.todo("returns target point when no walls block the path");
      it.todo("blocks movement when target crosses a wall segment AABB with default 1ft padding");
      it.todo("slides along wall when approaching at an angle (returns partial movement)");
      it.todo("clamps target to room bounds (0 <= x <= room.width, 0 <= z <= room.length)");
      it.todo("ignores product and opening collisions (walls only)");
      it.todo("custom padding parameter overrides the 1ft default");
    });

    describe("walkCollision room bounds clamp", () => {
      it.todo("clamps x to [0, room.width]");
      it.todo("clamps z to [0, room.length]");
      it.todo("leaves valid in-bounds points unchanged");
    });
    ```

    Create `tests/uiStore.test.ts` with EXACTLY this content:

    ```ts
    import { describe, it } from "vitest";

    describe("uiStore cameraMode state", () => {
      it.todo("default cameraMode is 'orbit'");
      it.todo("setCameraMode('walk') sets cameraMode to 'walk'");
      it.todo("setCameraMode('orbit') sets cameraMode to 'orbit'");
      it.todo("toggleCameraMode flips orbit -> walk");
      it.todo("toggleCameraMode flips walk -> orbit");
    });
    ```

    Both files use `it.todo` (not `it.skip`) per Phase 02 convention (see STATE.md: "Wave 0 stubs use it.todo so vitest collects without failing").
  </action>
  <verify>
    <automated>npm test -- --run tests/walkCollision.test.ts tests/uiStore.test.ts 2>&1 | grep -E "(todo|passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/walkCollision.test.ts` succeeds
    - `test -f tests/uiStore.test.ts` succeeds
    - `grep -c "it.todo" tests/walkCollision.test.ts` returns 9
    - `grep -c "it.todo" tests/uiStore.test.ts` returns 5
    - `grep "describe(\"walkCollision canMoveTo\"" tests/walkCollision.test.ts` matches
    - `grep "describe(\"uiStore cameraMode state\"" tests/uiStore.test.ts` matches
    - `npm test -- --run` exits 0 (all todos counted, no failures)
  </acceptance_criteria>
  <done>
    Both test files exist with exact stub contents. vitest collects them and reports them as todos. No test failures introduced. Wave 1 can now import `@/three/walkCollision` and `@/stores/uiStore` cameraMode APIs against these contracts.
  </done>
</task>

</tasks>

<verification>
Run `npm test -- --run` — all tests (existing + new todos) should pass/todo. No collection errors.
</verification>

<success_criteria>
- tests/walkCollision.test.ts exists with 9 it.todo stubs in 2 describe blocks
- tests/uiStore.test.ts exists with 5 it.todo stubs in 1 describe block
- `npm test -- --run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-3d-walkthrough/04-00-SUMMARY.md`
</output>
