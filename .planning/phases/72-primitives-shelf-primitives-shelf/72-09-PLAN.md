---
phase: 72-primitives-shelf
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AddRoomDialog.tsx
  - src/components/DeleteTextureDialog.tsx
autonomous: true
gap_closure: true
requirements:
  - M5
must_haves:
  truths:
    - "AddRoomDialog uses Dialog primitive (no manual fixed-inset-0 overlay div)"
    - "DeleteTextureDialog uses Dialog primitive (no manual fixed-inset-0 overlay div)"
    - "Both dialogs retain their controlled open prop interface — callers are unchanged"
    - "Escape key closes both dialogs (handled by Radix automatically)"
    - "Focus trap works inside both dialogs (handled by Radix automatically)"
  artifacts:
    - path: src/components/AddRoomDialog.tsx
      provides: "Add room dialog using Dialog primitive"
      contains: "DialogContent"
    - path: src/components/DeleteTextureDialog.tsx
      provides: "Delete texture confirm dialog using Dialog primitive"
      contains: "DialogContent"
  key_links:
    - from: src/components/AddRoomDialog.tsx
      to: src/components/ui/Dialog.tsx
      via: "import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui'"
      pattern: "DialogContent"
    - from: src/components/DeleteTextureDialog.tsx
      to: src/components/ui/Dialog.tsx
      via: "import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui'"
      pattern: "DialogContent"
---

<objective>
Migrate AddRoomDialog.tsx and DeleteTextureDialog.tsx from manual overlay divs to the Dialog primitive so M5 passes.

Purpose: Satisfies the verification gap — at least one dialog site must use the Dialog primitive.
Output: Both files use Dialog/DialogContent/DialogHeader/DialogTitle; callers unchanged; Radix handles focus trap, Escape key, and backdrop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/AddRoomDialog.tsx
@src/components/DeleteTextureDialog.tsx
@src/components/ui/Dialog.tsx
@src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AddRoomDialog to Dialog primitive</name>
  <files>src/components/AddRoomDialog.tsx</files>
  <action>
Replace the manual overlay pattern in AddRoomDialog.tsx with the Dialog primitive.

Import: `import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui';`

Before (manual pattern to remove):
```tsx
if (!open) return null;
return (
  <div className="fixed inset-0 z-50 flex items-center justify-center bg-background/60 backdrop-blur-sm">
    <div className="bg-popover border border-border/60 p-6 w-[480px] font-sans">
      ...
    </div>
  </div>
);
```

After (Dialog primitive pattern):
```tsx
return (
  <Dialog open={open} onOpenChange={(o) => { if (!o) onClose(); }}>
    <DialogContent className="w-[480px]">
      <DialogHeader>
        <DialogTitle>ADD ROOM</DialogTitle>
      </DialogHeader>
      {/* existing form content — move it here unchanged */}
    </DialogContent>
  </Dialog>
);
```

Rules:
- Remove the `if (!open) return null` guard — Dialog primitive handles visibility via the `open` prop.
- Remove the manual `fixed inset-0` overlay div and the inner `bg-popover border` content div.
- Keep the `open` and `onClose` props on the component signature unchanged — callers (App.tsx or TerritoryPlanner) are not modified.
- Keep all existing form fields, state, validation, and submit logic inside DialogContent unchanged.
- The Dialog primitive's Radix implementation handles: focus trap, Escape key (fires onOpenChange(false)), backdrop with blur, spring entry animation.
- Use `DialogHeader` + `DialogTitle` for the title row (matches D-05 shadcn-inspired API).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "AddRoomDialog" || echo "No AddRoomDialog type errors"</automated>
  </verify>
  <done>
    - AddRoomDialog.tsx imports Dialog, DialogContent, DialogHeader, DialogTitle from @/components/ui
    - No manual fixed-inset-0 overlay div remains
    - No if(!open) return null guard remains
    - open/onClose prop interface is unchanged
    - TypeScript compiles with no errors in this file
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate DeleteTextureDialog to Dialog primitive</name>
  <files>src/components/DeleteTextureDialog.tsx</files>
  <action>
Replace the manual overlay pattern in DeleteTextureDialog.tsx with the Dialog primitive.

Import: `import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui';`

Apply the same transformation as Task 1:
- Remove `if (!open) return null` guard
- Remove manual `fixed inset-0` overlay div and inner content div
- Wrap content in `<Dialog open={open} onOpenChange={(o) => { if (!o) onClose(); }}>`
- Place body inside `<DialogContent>`
- Use `<DialogHeader><DialogTitle>` for the confirm title (e.g., "Delete Texture" or whatever the existing heading reads)
- Keep all existing confirm/cancel button handlers unchanged
- Keep the controlled `open` + `onClose` prop interface unchanged so callers don't change

The Dialog primitive handles Escape key to dismiss — do NOT add a separate keydown handler for Escape unless one was already there for other keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "DeleteTextureDialog" || echo "No DeleteTextureDialog type errors"</automated>
  </verify>
  <done>
    - DeleteTextureDialog.tsx imports Dialog primitive components from @/components/ui
    - No manual fixed-inset-0 overlay div remains
    - No if(!open) return null guard remains
    - open/onClose prop interface is unchanged
    - TypeScript compiles with no errors in this file
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors in either file
- Both files contain `DialogContent` (grep confirms)
- Neither file contains `fixed inset-0` (manual overlay removed)
- Neither file contains `if (!open) return null` guard
- Callers of AddRoomDialog and DeleteTextureDialog are unchanged (no prop interface change)
</verification>

<success_criteria>
M5 is satisfied: at least one app dialog uses the Dialog primitive. Both AddRoomDialog and DeleteTextureDialog use Dialog/DialogContent/DialogHeader/DialogTitle. Radix handles focus trap, Escape key, and backdrop automatically. Caller interfaces are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-09-SUMMARY.md`
</output>
