---
phase: 72-primitives-shelf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/cn.ts
  - src/lib/motion.ts
  - src/components/ui/index.ts
  - tests/primitives/cn.test.ts
autonomous: true
requirements: [PRIMITIVES-SHELF]

must_haves:
  truths:
    - "cn() merges className strings and resolves Tailwind conflicts"
    - "SPRING_SNAPPY and SPRING_NONE presets are importable from src/lib/motion.ts"
    - "springTransition(reduced) returns instant config when reduced=true"
    - "All 8 npm packages install without conflict"
  artifacts:
    - path: "src/lib/cn.ts"
      provides: "className merge utility"
      exports: ["cn"]
    - path: "src/lib/motion.ts"
      provides: "Spring presets + reduced-motion helper"
      exports: ["SPRING_SNAPPY", "SPRING_NONE", "springTransition"]
    - path: "src/components/ui/index.ts"
      provides: "Barrel export skeleton for all primitives"
    - path: "tests/primitives/cn.test.ts"
      provides: "cn() unit tests"
  key_links:
    - from: "src/lib/cn.ts"
      to: "clsx + tailwind-merge"
      via: "import"
      pattern: "twMerge\\(clsx"
    - from: "src/lib/motion.ts"
      to: "motion/react"
      via: "type import"
      pattern: "import type.*Transition.*motion/react"
---

<objective>
Install all Phase 72 npm dependencies and create the two foundation utility files (cn.ts, motion.ts) plus the barrel export skeleton. Every subsequent plan imports from these.

Purpose: Foundation layer — no primitive can be built without cn() and spring presets.
Output: package.json updated, cn.ts, motion.ts, ui/index.ts skeleton, cn.test.ts passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-CONTEXT.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies + create cn.ts and motion.ts</name>
  <files>package.json, package-lock.json, src/lib/cn.ts, src/lib/motion.ts, src/components/ui/index.ts</files>
  <action>
1. Install all 8 packages (per D-04, D-01, D-15, D-17):
   ```
   npm install class-variance-authority clsx tailwind-merge motion @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip @radix-ui/react-slot
   ```

2. Create `src/lib/cn.ts` (per D-07):
   ```typescript
   import { clsx, type ClassValue } from "clsx";
   import { twMerge } from "tailwind-merge";
   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```

3. Create `src/lib/motion.ts` (per D-03):
   - Export `SPRING_SNAPPY: Transition` = `{ type: "spring", stiffness: 400, damping: 30 }`
   - Export `SPRING_NONE: Transition` = `{ duration: 0 }`
   - Export `springTransition(reduced: boolean): Transition` returning SPRING_NONE when reduced=true, SPRING_SNAPPY otherwise
   - Import `type Transition` from `motion/react`

4. Create `src/components/ui/index.ts` barrel skeleton:
   ```typescript
   // Phase 72 primitive barrel — each primitive re-exports from here.
   // Populated by Plans 02-05 as primitives ship.
   export {} // placeholder until first primitive lands
   ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 packages in package.json dependencies; cn.ts exports cn(); motion.ts exports SPRING_SNAPPY, SPRING_NONE, springTransition; barrel skeleton exists; project builds cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cn() unit tests</name>
  <files>tests/primitives/cn.test.ts</files>
  <behavior>
    - Test 1: cn("foo", "bar") returns "foo bar"
    - Test 2: cn("bg-red-500", "bg-blue-500") resolves to "bg-blue-500" (tw-merge conflict resolution)
    - Test 3: cn(undefined, null, false, "visible") returns "visible" (falsy filtering)
    - Test 4: cn("p-4", "p-2") resolves to "p-2" (last wins)
    - Test 5: cn("text-foreground", "text-muted-foreground") resolves correctly
  </behavior>
  <action>
Create `tests/primitives/cn.test.ts`. Import cn from `@/lib/cn`. Write the 5 test cases above. Run RED first (file doesn't exist yet — but cn.ts was created in Task 1, so tests should pass GREEN immediately). This is a verification-style TDD: write tests to lock the contract.
  </action>
  <verify>
    <automated>npx vitest run tests/primitives/cn.test.ts -x</automated>
  </verify>
  <done>All 5 cn() tests pass. Contract locked: className merging + Tailwind conflict resolution works.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run tests/primitives/cn.test.ts -x` passes
- `import { cn } from "@/lib/cn"` resolves
- `import { SPRING_SNAPPY, springTransition } from "@/lib/motion"` resolves
</verification>

<success_criteria>
All 8 npm packages installed. cn() and springTransition() importable and tested. Barrel export skeleton at src/components/ui/index.ts. Project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-01-SUMMARY.md`
</output>
