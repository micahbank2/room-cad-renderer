---
phase: 72-primitives-shelf
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/index.ts
  - tests/primitives/button.test.tsx
autonomous: true
requirements: [PRIMITIVES-SHELF]

must_haves:
  truths:
    - "Button renders all 6 variants (default/destructive/outline/secondary/ghost/link)"
    - "Button renders all 6 sizes (default/sm/lg/icon/icon-sm/icon-lg)"
    - "Button defaults type to 'button' (not 'submit')"
    - "Button active prop adds visual active state"
    - "Button asChild delegates rendering to child via Radix Slot"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Button primitive with cva variants"
      exports: ["Button", "buttonVariants", "ButtonProps"]
    - path: "tests/primitives/button.test.tsx"
      provides: "Button unit tests"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/lib/cn.ts"
      via: "import cn"
      pattern: "import.*cn.*from.*@/lib/cn"
    - from: "src/components/ui/Button.tsx"
      to: "class-variance-authority"
      via: "cva import"
      pattern: "import.*cva.*from.*class-variance-authority"
---

<objective>
Build the Button primitive — the most-used primitive across the entire app. cva-driven variant/size matrix with active state support and asChild pattern.

Purpose: Button is the foundation for ~130 raw button sites across 41 files. Phase 72 migrates ~40 of them in Plans 06-07.
Output: Button.tsx with full variant matrix, barrel-exported, unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-CONTEXT.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-RESEARCH.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Button primitive + tests</name>
  <files>src/components/ui/Button.tsx, src/components/ui/index.ts, tests/primitives/button.test.tsx</files>
  <behavior>
    - Test 1: renders a button element by default
    - Test 2: defaults type="button" (Pitfall 6 — prevents accidental form submission)
    - Test 3: renders all 6 variants with distinct class sets
    - Test 4: renders all 6 sizes with correct height/width classes
    - Test 5: active prop adds data-active attribute and active styling
    - Test 6: asChild renders child element instead of button
    - Test 7: className prop merges with variant classes (via cn())
    - Test 8: forwards ref to the button element
    - Test 9: disabled prop applies disabled styling
  </behavior>
  <action>
1. Create `tests/primitives/button.test.tsx` with the 9 test cases above. Use `@testing-library/react` render + screen queries. RED first.

2. Create `src/components/ui/Button.tsx` (per D-08, D-09, D-05):
   - Import `cva, type VariantProps` from `class-variance-authority`
   - Import `Slot` from `@radix-ui/react-slot`
   - Import `cn` from `@/lib/cn`
   - Define `buttonVariants` with cva:
     - Base: `inline-flex items-center justify-center gap-2 whitespace-nowrap font-sans text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 rounded-smooth-md`
     - Variants per D-08:
       - default: `bg-primary text-primary-foreground hover:bg-primary/90`
       - destructive: `bg-destructive text-destructive-foreground hover:bg-destructive/90`
       - outline: `border border-border bg-background hover:bg-accent hover:text-accent-foreground`
       - secondary: `bg-secondary text-secondary-foreground hover:bg-secondary/80`
       - ghost: `hover:bg-accent hover:text-accent-foreground`
       - link: `text-primary underline-offset-4 hover:underline`
     - Sizes per D-08:
       - default: `h-9 px-4 py-2`
       - sm: `h-7 px-3 text-xs`
       - lg: `h-10 px-8`
       - icon: `h-9 w-9`
       - icon-sm: `h-7 w-7`
       - icon-lg: `h-10 w-10`
     - defaultVariants: variant=default, size=default
   - Interface `ButtonProps` extends `ButtonHTMLAttributes<HTMLButtonElement>` + `VariantProps<typeof buttonVariants>` + `{ asChild?: boolean; active?: boolean }`
   - `forwardRef` component:
     - CRITICAL: Default `type` to `"button"` — spread props AFTER so explicit `type="submit"` overrides
     - Use `Slot` when asChild=true, `"button"` otherwise
     - Apply `cn(buttonVariants({ variant, size }), active && "bg-accent/20 ring-1 ring-ring", className)`
     - Set `data-active={active || undefined}`
   - Export `Button`, `buttonVariants`, and `ButtonProps` type

3. Update `src/components/ui/index.ts`: add `export { Button, buttonVariants, type ButtonProps } from "./Button";`

4. Run tests GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/primitives/button.test.tsx -x</automated>
  </verify>
  <done>Button renders all 6 variants and 6 sizes. type defaults to "button". active prop adds visual state. asChild delegates to child. All 9 tests pass. Barrel-exported.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/primitives/button.test.tsx -x` passes
- `npm run build` succeeds
- Button is importable: `import { Button } from "@/components/ui"`
</verification>

<success_criteria>
Button primitive fully functional with all variant/size combinations, type="button" default, active state, asChild support. 9 unit tests pass. Barrel-exported.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-02-SUMMARY.md`
</output>
