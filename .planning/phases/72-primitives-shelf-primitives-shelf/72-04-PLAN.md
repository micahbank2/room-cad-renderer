---
phase: 72-primitives-shelf
plan: 04
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/components/ui/Dialog.tsx
  - src/components/ui/index.ts
  - tests/primitives/dialog.test.tsx
autonomous: true
requirements: [PRIMITIVES-SHELF]

must_haves:
  truths:
    - "Dialog renders with Radix focus trap and Escape-to-close"
    - "Dialog overlay has backdrop blur"
    - "Dialog content spring-animates from scale(0.95) + opacity(0)"
    - "Reduced-motion users get instant snap"
  artifacts:
    - path: "src/components/ui/Dialog.tsx"
      provides: "Dialog primitive with Radix + motion/react"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogTitle", "DialogDescription", "DialogClose"]
    - path: "tests/primitives/dialog.test.tsx"
      provides: "Dialog unit tests"
  key_links:
    - from: "src/components/ui/Dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "Radix primitives"
      pattern: "DialogPrimitive"
    - from: "src/components/ui/Dialog.tsx"
      to: "motion/react"
      via: "spring entry animation"
      pattern: "motion\\.div"
---

<objective>
Build the Dialog primitive — Radix-based for accessibility (focus trap, Escape, aria) with motion/react spring entry animation. Used by all modal dialogs in Phases 75-76.

Purpose: Unified dialog treatment replaces ad-hoc modal patterns. Spring entry + backdrop blur creates consistent experience.
Output: Dialog.tsx with Radix + motion/react, unit-tested, barrel-exported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-CONTEXT.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-RESEARCH.md

<interfaces>
From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/lib/motion.ts:
```typescript
export function springTransition(reduced: boolean): Transition;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dialog primitive + tests</name>
  <files>src/components/ui/Dialog.tsx, src/components/ui/index.ts, tests/primitives/dialog.test.tsx</files>
  <behavior>
    - Test 1: DialogContent renders when Dialog is open
    - Test 2: DialogContent does NOT render when Dialog is closed
    - Test 3: Escape key closes the dialog
    - Test 4: DialogTitle renders with correct aria role
    - Test 5: Overlay has backdrop-blur class
    - Test 6: DialogClose button closes the dialog
    - Test 7: className prop on DialogContent is merged (not replaced)
  </behavior>
  <action>
1. Create `tests/primitives/dialog.test.tsx` with 7 test cases. Use `@testing-library/react` render + fireEvent. RED first.

2. Create `src/components/ui/Dialog.tsx` (per D-15, D-16):
   - Import `* as DialogPrimitive` from `@radix-ui/react-dialog`
   - Import `motion, AnimatePresence` from `motion/react`
   - Import `useReducedMotion` from `@/hooks/useReducedMotion`
   - Import `springTransition` from `@/lib/motion`
   - Import `cn` from `@/lib/cn`

   Re-export Radix parts directly:
   - `Dialog = DialogPrimitive.Root`
   - `DialogTrigger = DialogPrimitive.Trigger`
   - `DialogClose = DialogPrimitive.Close`
   - `DialogTitle` = styled wrapper of `DialogPrimitive.Title` with `font-sans text-lg font-semibold text-foreground`
   - `DialogDescription` = styled wrapper of `DialogPrimitive.Description` with `text-sm text-muted-foreground`

   DialogContent component (per D-16):
   - Use `forwardRef`
   - Accept `className` + rest props + `children`
   - Render inside `DialogPrimitive.Portal`:
     - Overlay: `<DialogPrimitive.Overlay className="fixed inset-0 z-50 bg-background/80 backdrop-blur-sm" />`
     - Content: `<DialogPrimitive.Content asChild>` wrapping a `<motion.div>`:
       - `initial={{ opacity: 0, scale: 0.95 }}`
       - `animate={{ opacity: 1, scale: 1 }}`
       - `exit={{ opacity: 0, scale: 0.95 }}`
       - `transition={springTransition(reduced)}`
       - `className={cn("fixed left-1/2 top-1/2 z-50 -translate-x-1/2 -translate-y-1/2 w-full max-w-lg bg-card rounded-smooth-lg border border-border shadow-lg p-6", className)}`

   NOTE: Radix Dialog manages its own open/close state — no AnimatePresence needed around the portal because Radix handles mount/unmount. The motion.div provides entry animation. Exit animation is best-effort (Radix unmounts the portal on close).

3. Update `src/components/ui/index.ts`: add Dialog exports.

4. Run tests GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/primitives/dialog.test.tsx -x</automated>
  </verify>
  <done>Dialog renders with Radix focus trap. Overlay has backdrop blur. Content spring-animates. 7 tests pass. Barrel-exported.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/primitives/dialog.test.tsx -x` passes
- `npm run build` succeeds
- Dialog importable from barrel
</verification>

<success_criteria>
Dialog primitive wraps Radix for accessibility + motion/react for spring entry. Focus trap and Escape work. Overlay has backdrop blur. Reduced-motion snaps. 7 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-04-SUMMARY.md`
</output>
