---
phase: 72-primitives-shelf
plan: 06
type: execute
wave: 3
depends_on: ["72-02"]
files_modified:
  - src/components/Toolbar.tsx
autonomous: true
requirements: [PRIMITIVES-SHELF]

must_haves:
  truths:
    - "All ~20 raw <button> elements in Toolbar.tsx use the Button primitive"
    - "Tool palette buttons use variant='ghost' size='icon' with active prop"
    - "View tab buttons use variant='ghost' size='sm'"
    - "Export button uses variant='outline'"
    - "Undo/Redo buttons use variant='ghost' size='icon'"
    - "All existing data-testid, aria-label, and onClick handlers preserved"
  artifacts:
    - path: "src/components/Toolbar.tsx"
      provides: "Toolbar migrated to Button primitive"
  key_links:
    - from: "src/components/Toolbar.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import { Button }"
      pattern: "import.*Button.*from.*@/components/ui"
---

<objective>
Migrate all ~20 raw `<button>` elements in Toolbar.tsx to the new Button primitive. This is the highest-density migration target in the codebase.

Purpose: Toolbar is the most-seen UI surface. Migrating it to Button creates immediate visual consistency and validates the variant/size matrix at scale.
Output: Toolbar.tsx with zero raw `<button>` elements (except those inside third-party components), all using Button primitive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-CONTEXT.md
@.planning/phases/72-primitives-shelf-primitives-shelf/72-RESEARCH.md
@src/components/Toolbar.tsx

<interfaces>
From src/components/ui/Button.tsx:
```typescript
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
  active?: boolean;
}
export { Button, buttonVariants };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Toolbar.tsx buttons to Button primitive</name>
  <files>src/components/Toolbar.tsx</files>
  <action>
Import `{ Button }` from `@/components/ui/Button`.

Migrate each button site in Toolbar.tsx. Mapping guide:

**Header bar (Toolbar component):**
1. Brand button ("Room CAD Renderer") → `<Button variant="ghost" size="sm" className="font-bold tracking-[0.1em] mr-6">`
2. Floor plan button → `<Button variant="ghost" size="sm">`
3. View mode tabs (2D, 3D, Library, Split — 4 buttons) → `<Button variant="ghost" size="sm">` with active styling via conditional className (these are tab-like but migrate to proper Tabs in Phase 74)
4. Walk/Orbit toggle → `<Button variant="ghost" size="sm">`
5. Camera preset buttons (4) → `<Button variant="ghost" size="icon">` with `active={isActive}`
6. Display mode buttons (Normal/Solo/Explode — 3) → `<Button variant="ghost" size="sm">` with `active={isActive}`
7. Cutaway toggle → `<Button variant="ghost" size="sm">` with `active={cutawayMode === "auto"}`
8. Undo button → `<Button variant="ghost" size="icon">`
9. Redo button → `<Button variant="ghost" size="icon">`
10. Export button → `<Button variant="outline" size="sm">`
11. Help button → `<Button variant="ghost" size="icon">`
12. Settings button → `<Button variant="ghost" size="icon">`

**Tool palette (ToolPalette component):**
13. Tool buttons (select/wall/door/window/ceiling/stair — 6) → `<Button variant="ghost" size="icon" active={activeTool === t.id}>`
14. Wall cutouts trigger → `<Button variant="ghost" size="icon" active={isCutoutTool}>`
15. Measure button → `<Button variant="ghost" size="icon" active={activeTool === "measure"}>`
16. Label button → `<Button variant="ghost" size="icon" active={activeTool === "label"}>`
17. Grid toggle → `<Button variant="ghost" size="icon">`
18. Zoom in → `<Button variant="ghost" size="icon">`
19. Zoom out → `<Button variant="ghost" size="icon">`
20. Fit to view → `<Button variant="ghost" size="icon">`

**CRITICAL preservation rules:**
- Keep ALL `data-testid` attributes exactly as-is
- Keep ALL `data-onboarding` attributes exactly as-is
- Keep ALL `aria-label` and `aria-pressed` attributes
- Keep ALL `onClick` handlers unchanged
- Keep ALL `disabled` props
- Keep ALL `ref` assignments (wallCutoutsTriggerRef)
- Button primitive defaults `type="button"` so explicit `type="button"` can be removed

**Styling migration:**
- Replace inline className strings with Button variant/size props
- Active state: use `active` prop instead of conditional `bg-primary text-primary-foreground shadow-[...]` classes
- The tool palette active state `shadow-[0_0_15px_rgba(124,91,240,0.3)]` moves into the Button active styling (or keep as className override via `cn()`)
- Disabled state styling handled by Button's `disabled:opacity-50 disabled:pointer-events-none`

**Do NOT touch:**
- `ToolbarSaveStatus` internal structure (status badges, not buttons)
- User avatar div (not a button)
- Divider elements
- The `<WallCutoutsDropdown>` component
- `<InlineEditableText>` component
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run tests/phase33/ -x 2>&1 | tail -10</automated>
  </verify>
  <done>Zero raw `<button>` elements in Toolbar.tsx (except inside child components). All ~20 buttons use Button primitive. All data-testid, aria-label, onClick handlers preserved. Build passes. Existing tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run tests/phase33/ -x` passes (no regressions)
- `grep -c '<button' src/components/Toolbar.tsx` returns 0 (or only within JSX string content)
- All existing Toolbar data-testid attributes still present
</verification>

<success_criteria>
Toolbar.tsx fully migrated to Button primitive. All ~20 button sites use consistent variant/size. Active tool states use the active prop. All existing test IDs, aria attributes, and event handlers preserved. No visual regression.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-06-SUMMARY.md`
</output>
