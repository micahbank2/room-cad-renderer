---
phase: 72-primitives-shelf
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RoomTabs.tsx
autonomous: true
gap_closure: true
requirements:
  - M4
must_haves:
  truths:
    - "RoomTabs renders using the Tabs primitive (no manual div/border-b tab strip)"
    - "Active tab contains InlineEditableText composed inside TabsTrigger"
    - "Switching rooms still calls useCADStore.getState().switchRoom(id)"
    - "data-testid='ROOM_TABS' remains on the outer wrapper"
    - "ADD ROOM button remains outside TabsList"
    - "Delete (×) button per tab still fires with e.stopPropagation()"
    - "layoutId pill animation slides under the active tab"
  artifacts:
    - path: src/components/RoomTabs.tsx
      provides: "Room tab bar using Tabs/TabsList/TabsTrigger primitives"
      contains: "Tabs"
  key_links:
    - from: src/components/RoomTabs.tsx
      to: src/components/ui/Tabs.tsx
      via: "import { Tabs, TabsList, TabsTrigger } from '@/components/ui'"
      pattern: "TabsTrigger"
---

<objective>
Migrate RoomTabs.tsx from its custom div/border-b tab strip to the Tabs primitive so M4 passes.

Purpose: Satisfies the verification gap — at least one tab site must use the Tabs primitive.
Output: Updated RoomTabs.tsx using Tabs/TabsList/TabsTrigger; all existing behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/RoomTabs.tsx
@src/components/ui/Tabs.tsx
@src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RoomTabs to Tabs primitive</name>
  <files>src/components/RoomTabs.tsx</files>
  <action>
Replace the manual div/border-b tab strip in RoomTabs.tsx with the Tabs primitive.

Import: `import { Tabs, TabsList, TabsTrigger } from '@/components/ui';`

Structure:
```tsx
<div data-testid="ROOM_TABS" className="flex items-center ...existing wrapper classes...">
  <Tabs value={activeRoomId} onValueChange={(id) => useCADStore.getState().switchRoom(id)}>
    <TabsList>
      {rooms.map(room => {
        const isActive = room.id === activeRoomId;
        return (
          <TabsTrigger key={room.id} value={room.id}>
            {isActive
              ? <InlineEditableText ... />
              : room.name}
            {canDelete && (
              <button
                onClick={(e) => { e.stopPropagation(); handleDelete(room.id); }}
                className="ml-1 opacity-0 group-hover:opacity-100 ..."
              >×</button>
            )}
          </TabsTrigger>
        );
      })}
    </TabsList>
  </Tabs>
  {/* ADD ROOM button stays OUTSIDE TabsList — it controls creation, not tab navigation */}
  <button onClick={handleAddRoom} ...>+ ADD ROOM</button>
</div>
```

Rules:
- Keep `data-testid="ROOM_TABS"` on the outermost div wrapper (NOT on the Tabs component itself, so the testid location is unchanged).
- Keep `onValueChange` calling `useCADStore.getState().switchRoom(id)` — do NOT add a separate onClick to TabsTrigger.
- The layoutId pill from Tabs.tsx handles the slide animation automatically — no manual animation needed.
- Remove all manual `border-b-2 border-accent-light` active tab styling (the Tabs primitive handles active state).
- The delete button `e.stopPropagation()` prevents the tab switch from firing when clicking ×.
- Do NOT import TabsContent — RoomTabs controls the canvas, not page sections, so no content panels are needed.
- Preserve any hover-reveal logic for the × button using the existing className approach.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "RoomTabs" || echo "No RoomTabs type errors"</automated>
  </verify>
  <done>
    - RoomTabs.tsx imports from @/components/ui (Tabs, TabsList, TabsTrigger)
    - No manual div border-b tab strip remains
    - data-testid="ROOM_TABS" present on outer wrapper
    - ADD ROOM button is outside TabsList
    - TypeScript compiles with no errors in RoomTabs.tsx
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors in RoomTabs.tsx
- RoomTabs.tsx contains `TabsTrigger` (grep confirms import and usage)
- `data-testid="ROOM_TABS"` still present in file
- No manual `border-b-2 border-accent-light` active tab pattern remains in RoomTabs.tsx
</verification>

<success_criteria>
M4 is satisfied: at least one app component uses the Tabs primitive. RoomTabs renders room tabs via Tabs/TabsList/TabsTrigger. All existing behavior (switch room, inline edit active tab, delete tab, add room) is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/72-primitives-shelf-primitives-shelf/72-08-SUMMARY.md`
</output>
