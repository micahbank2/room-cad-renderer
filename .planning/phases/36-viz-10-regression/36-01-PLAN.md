---
phase: 36-viz-10-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - playwright.config.ts
  - .gitignore
  - src/three/ThreeViewport.tsx
  - src/three/userTextureCache.ts
  - src/three/wallpaperTextureCache.ts
  - src/three/wallArtTextureCache.ts
  - src/hooks/useUserTexture.ts
  - src/components/Toolbar.tsx
  - tests/e2e/playwright-helpers/setTestCamera.ts
  - tests/e2e/playwright-helpers/uploadTexture.ts
  - tests/e2e/playwright-helpers/toggleViewMode.ts
  - tests/e2e/playwright-helpers/settle.ts
  - tests/e2e/playwright-helpers/lifecycleEvents.ts
  - tests/e2e/fixtures/sample-wallpaper.jpg
  - tests/e2e/fixtures/sample-wallart.png
  - tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts
  - tests/e2e/specs/wallart-2d-3d-toggle.spec.ts
  - tests/e2e/specs/floor-user-texture-toggle.spec.ts
  - tests/e2e/specs/ceiling-user-texture-toggle.spec.ts
  - tests/e2e/README.md
  - .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
autonomous: true
requirements: [VIZ-10]
requirements_addressed: [VIZ-10]

must_haves:
  truths:
    - "Playwright harness can be run locally with `npm run test:e2e`"
    - "Harness reproduces the VIZ-10 regression (fails pre-fix) on uploaded-image wallpaper + wallArt across 5 toggle cycles"
    - "window.__textureLifecycleEvents[] captures load/bind/unmount/dispose events from all 4 cache modules"
    - "ROOT-CAUSE.md names the actual cause with inline log evidence — not speculation"
    - "ROOT-CAUSE.md cross-references every Phase 32 candidate (1-4) as confirmed/refuted/inconclusive with log evidence"
    - "ROOT-CAUSE.md triages each of 3 Phase 32 defensive-code pieces individually (keep/simplify/remove with justification)"
    - "No production source logic changed (instrumentation only — test-mode gated)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Two Playwright projects (chromium-dev + chromium-preview) + webServer orchestration"
    - path: "tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts"
      provides: "5-cycle wallpaper toggle with pixel-diff"
    - path: "tests/e2e/specs/wallart-2d-3d-toggle.spec.ts"
      provides: "5-cycle wallArt toggle with pixel-diff"
    - path: "tests/e2e/specs/floor-user-texture-toggle.spec.ts"
      provides: "2-cycle floor smoke"
    - path: "tests/e2e/specs/ceiling-user-texture-toggle.spec.ts"
      provides: "2-cycle ceiling smoke"
    - path: ".planning/phases/36-viz-10-regression/ROOT-CAUSE.md"
      provides: "Named cause + evidence + Phase 32 candidate cross-reference + defensive-code triage"
      contains: "## Candidate Cause 1"
  key_links:
    - from: "tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts"
      to: "window.__driveTextureUpload (src/components/UploadTextureModal.tsx:460)"
      via: "page.evaluate fixture upload"
      pattern: "__driveTextureUpload"
    - from: "tests/e2e/specs/*.spec.ts"
      to: "window.__setTestCamera (src/three/ThreeViewport.tsx)"
      via: "page.evaluate camera pose"
      pattern: "__setTestCamera"
    - from: "src/three/userTextureCache.ts"
      to: "window.__textureLifecycleEvents"
      via: "load/resolve/fail tap"
      pattern: "__textureLifecycleEvents"
---

<objective>
Install Playwright, build the VIZ-10 instrumentation harness, capture texture-lifecycle evidence across 5-cycle toggle scenarios on 4 surfaces (wallpaper/wallArt/floor/ceiling), and author ROOT-CAUSE.md naming the actual cause with inline evidence. NO PRODUCTION FIX in this plan — this plan only produces the harness and the root-cause document.

Purpose: Enforce root-cause discipline (CONTEXT R-01). Plan 36-02's fix does not begin until this plan's PR merges with ROOT-CAUSE.md in place.

Output: Playwright config + 4 spec files + 5 helpers + 2 fixtures + lifecycle taps in 4 cache modules + `__setTestCamera` helper + ROOT-CAUSE.md + npm scripts + CI-ready config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-viz-10-regression/36-CONTEXT.md
@.planning/phases/36-viz-10-regression/36-RESEARCH.md
@.planning/phases/36-viz-10-regression/36-VALIDATION.md
@.planning/phases/32-pbr-foundation/32-07-SUMMARY.md

<interfaces>
<!-- Test-driver conventions the executor must follow (verbatim from Phase 31 / 34) -->

Phase 31 install pattern (src/canvas/tools/selectTool.ts:1567-1643):
```ts
useEffect(() => {
  if (import.meta.env.MODE !== "test" || typeof window === "undefined") return;
  (window as any).__driveSomething = (...) => { /* ... */ };
  return () => { delete (window as any).__driveSomething; };
}, []);
```

Phase 34 upload driver (src/components/UploadTextureModal.tsx:460):
```ts
window.__driveTextureUpload(file: File, name: string, tileSizeFt: number): Promise<string>
// Returns the user-texture ID once persisted in IndexedDB + cache hot.
```

Phase 32 candidate causes (32-07-SUMMARY.md:38-42 — copy VERBATIM into ROOT-CAUSE.md §1):
1. HTMLImageElement backing a data URL is tied to the old WebGLRenderer's context and silently fails re-upload...
2. meshStandardMaterial in R3F receives <primitive attach="map"> child but doesn't re-link the uniform...
3. Image decode happens once off the event loop and the decoded pixel data is only bound to the first WebGL texture upload...
4. Something unrelated to textures — the 300x150 canvas size hints that R3F <Canvas> may not be mounting into a sized container...

Phase 32 defensive code (triage each individually in ROOT-CAUSE.md §4):
1. Non-disposing userTextureCache Map — src/three/userTextureCache.ts:31
2. `<primitive attach="map" object={tex} dispose={null} />` — WallMesh.tsx:136/182/268/288, FloorMesh.tsx:102/127, CeilingMesh.tsx:110
3. Static regression test — tests/wallMeshDisposeContract.test.ts (76 lines)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Install Playwright + create config + update vitest exclude + package.json scripts + .gitignore</name>
  <files>package.json, vitest.config.ts, playwright.config.ts, .gitignore</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md (full — §Standard Stack, §Architecture Patterns, §Wave 0 Requirements)
    - package.json (existing scripts section lines 6-12)
    - vitest.config.ts (existing exclude array)
    - .gitignore (check existing patterns)
  </read_first>
  <action>
    1. Install Playwright: `npm install --save-dev @playwright/test` then `npx playwright install --with-deps chromium`.

    2. Create `playwright.config.ts` at repo root with the exact structure from 36-RESEARCH.md §Pattern 1 (verbatim, including comments):
       - `testDir: "./tests/e2e/specs"`
       - `fullyParallel: false` (texture caches are module-level singletons)
       - `retries: process.env.CI ? 1 : 0`
       - `reporter: [["html", { open: "never" }], ["list"]]`
       - `use: { viewport: { width: 1280, height: 720 }, deviceScaleFactor: 1, trace: "retain-on-failure" }`
       - `expect: { toHaveScreenshot: { maxDiffPixelRatio: 0.01 } }`
       - TWO projects: `chromium-dev` and `chromium-preview` (both using `devices["Desktop Chrome"]`)
       - `webServer` array with ONE entry for now: `{ command: "npm run dev -- --mode test --port 5173", port: 5173, reuseExistingServer: !process.env.CI, timeout: 60_000 }`
       - Add comment: "Plan 36-02 wires the preview webServer + activates chromium-preview project."

       CRITICAL: `--mode test` is non-negotiable (36-RESEARCH Pitfall 1). Without it, `import.meta.env.MODE === "test"` gates never activate and `window.__setTestCamera` throws.

    3. Update `package.json` scripts (preserve existing):
       - Add `"test:e2e": "playwright test"`
       - Add `"test:e2e:debug": "playwright test --headed --debug"`

    4. Update `vitest.config.ts` — add `"tests/e2e/**"` to the `test.exclude` array so vitest ignores Playwright specs (Pitfall 3).

    5. Update `.gitignore`:
       - Add `playwright-report/`
       - Add `test-results/`
       - Do NOT add `*-snapshots/` — golden images ARE committed (the contract).

    6. Verify install: `npx playwright --version` prints a version ≥1.45.
  </action>
  <verify>
    <automated>
      npx playwright --version
      && node -e "require('./playwright.config.ts')" || echo "TS config — will validate via playwright test --list below"
      && npx playwright test --list 2>&1 | grep -q "Listing tests"
      && grep -q '"test:e2e"' package.json
      && grep -q '"test:e2e:debug"' package.json
      && grep -q 'tests/e2e/' vitest.config.ts
      && grep -q 'playwright-report' .gitignore
      && grep -q 'maxDiffPixelRatio: 0.01' playwright.config.ts
      && grep -q 'npm run dev -- --mode test' playwright.config.ts
      && grep -q 'chromium-dev' playwright.config.ts
      && grep -q 'chromium-preview' playwright.config.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright --version` prints ≥1.45
    - `playwright.config.ts` contains `chromium-dev` AND `chromium-preview` project names
    - `playwright.config.ts` webServer command contains `--mode test`
    - `playwright.config.ts` contains `maxDiffPixelRatio: 0.01`
    - `package.json` contains both `test:e2e` and `test:e2e:debug` scripts
    - `vitest.config.ts` excludes `tests/e2e/**` (grep confirms)
    - `.gitignore` contains `playwright-report/` and `test-results/` but does NOT list `*-snapshots/`
    - Running `npx playwright test --list` does not error (may list 0 tests — that's expected before specs exist)
  </acceptance_criteria>
  <done>Playwright installed, config lays out both projects, webServer runs `npm run dev -- --mode test`, vitest no longer picks up e2e files. No production source touched yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (Wave 1): Install texture-lifecycle instrumentation + __setTestCamera helper + data-testid on view-mode buttons</name>
  <files>src/three/ThreeViewport.tsx, src/three/userTextureCache.ts, src/three/wallpaperTextureCache.ts, src/three/wallArtTextureCache.ts, src/hooks/useUserTexture.ts, src/components/Toolbar.tsx</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md §Pattern 2, §Pattern 3, §Runtime State Inventory, §Key File Paths
    - src/three/ThreeViewport.tsx (full — understand Scene component + orbitControlsRef at line 51-110, Canvas at line 225)
    - src/three/userTextureCache.ts (full — tap points at line 40, 62)
    - src/three/wallpaperTextureCache.ts (tap point ~line 22)
    - src/three/wallArtTextureCache.ts (tap point ~line 11)
    - src/hooks/useUserTexture.ts (tap point ~line 22)
    - src/components/Toolbar.tsx (view-mode buttons at lines 19, 74)
    - src/canvas/tools/selectTool.ts:1567-1643 (install/cleanup convention)
    - src/components/UploadTextureModal.tsx:460 (__driveTextureUpload reference)
  </read_first>
  <action>
    All changes gated by `import.meta.env.MODE === "test"` — zero production-path behavior change.

    1. **Event shape contract** — define in each cache file (not imported shared, but identical shape for simplicity):
       ```ts
       type LifecycleEvent = {
         t: number;            // performance.now()
         event: string;        // e.g., "userTex:load-start", "viewport-mount"
         id?: string;          // texture id / wall id / "n/a"
         uuid?: string;        // THREE.Texture.uuid after load-resolve
         context?: Record<string, unknown>;
       };
       // Initialize on first write:
       // (window as any).__textureLifecycleEvents ??= [];
       ```
       The buffer is NEVER cleared between cycles (36-RESEARCH Anti-Patterns). ROOT-CAUSE.md needs the full sequence.

    2. **src/three/ThreeViewport.tsx** — add TWO test-mode hooks inside `Scene()`:
       a. `__setTestCamera({ position, target })` — per 36-RESEARCH Pattern 2 (verbatim code block), uses existing `orbitControlsRef.current.object.position` + `ctrl.target`.
       b. `viewport-mount` / `viewport-unmount` lifecycle events in a useEffect with empty deps — log on mount, log on cleanup.

       Both hooks install/cleanup via `useEffect` returning a cleanup fn that deletes the window property (Phase 31 convention).

    3. **src/three/userTextureCache.ts** — add tap calls:
       - `userTex:load-start` when IDB read begins (around line 40)
       - `userTex:load-resolve` (include `tex.uuid`) when `tex.needsUpdate = true` is set
       - `userTex:load-fail` on catch
       - `userTex:cache-clear` inside `clearUserTextureCache` (~line 62)

    4. **src/three/wallpaperTextureCache.ts** — same 3 taps (`wallpaperTex:load-start` / `-resolve` with uuid / `-fail`) around the existing load site (~line 22).

    5. **src/three/wallArtTextureCache.ts** — same 3 taps (`wallArtTex:load-start` / `-resolve` with uuid / `-fail`) around the existing load site (~line 11).

    6. **src/hooks/useUserTexture.ts** — add `hook-mount(id)`, `hook-unmount(id)`, `hook-resolve(id, uuid)` taps in the existing useEffect/useState resolver chain (~line 22).

    7. **src/components/Toolbar.tsx** — add `data-testid="view-mode-2d"`, `data-testid="view-mode-3d"`, `data-testid="view-mode-split"` to the view-mode buttons (Research Wave 0 Gaps bullet). These attributes are harmless in production (pure DOM attrs).

    8. Run `npm test -- --run` to confirm ALL existing unit tests still pass — instrumentation must not break any Phase 34 test.
  </action>
  <verify>
    <automated>
      grep -n '__setTestCamera' src/three/ThreeViewport.tsx
      && grep -n 'import.meta.env.MODE' src/three/ThreeViewport.tsx
      && grep -n '__textureLifecycleEvents' src/three/ThreeViewport.tsx
      && grep -n '__textureLifecycleEvents' src/three/userTextureCache.ts
      && grep -n '__textureLifecycleEvents' src/three/wallpaperTextureCache.ts
      && grep -n '__textureLifecycleEvents' src/three/wallArtTextureCache.ts
      && grep -n '__textureLifecycleEvents' src/hooks/useUserTexture.ts
      && grep -n 'userTex:load-resolve' src/three/userTextureCache.ts
      && grep -n 'data-testid="view-mode-2d"' src/components/Toolbar.tsx
      && grep -n 'data-testid="view-mode-3d"' src/components/Toolbar.tsx
      && npm test -- --run
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep __setTestCamera src/three/ThreeViewport.tsx` returns a match AND the match is inside an `if (import.meta.env.MODE ... "test")` guard
    - `__textureLifecycleEvents` is referenced in all 5 files: ThreeViewport.tsx, userTextureCache.ts, wallpaperTextureCache.ts, wallArtTextureCache.ts, useUserTexture.ts
    - Each cache module logs at least `load-start` / `load-resolve` (with uuid) / `load-fail`
    - Toolbar has `data-testid="view-mode-2d"` AND `data-testid="view-mode-3d"`
    - `npm test -- --run` exits 0 — NO unit test regressions from instrumentation
    - No cleanup of `__textureLifecycleEvents` between mounts (grep should NOT find `__textureLifecycleEvents = []` or `.length = 0` in any tap site outside initialization)
  </acceptance_criteria>
  <done>Instrumentation complete, all gates test-mode only, all 74+ vitest tests still green, view-mode buttons have stable selectors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (Wave 1): Write Playwright helpers + 4 spec files + fixtures</name>
  <files>tests/e2e/playwright-helpers/setTestCamera.ts, tests/e2e/playwright-helpers/uploadTexture.ts, tests/e2e/playwright-helpers/toggleViewMode.ts, tests/e2e/playwright-helpers/settle.ts, tests/e2e/playwright-helpers/lifecycleEvents.ts, tests/e2e/fixtures/sample-wallpaper.jpg, tests/e2e/fixtures/sample-wallart.png, tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts, tests/e2e/specs/wallart-2d-3d-toggle.spec.ts, tests/e2e/specs/floor-user-texture-toggle.spec.ts, tests/e2e/specs/ceiling-user-texture-toggle.spec.ts, tests/e2e/README.md</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md §Code Examples, §Anti-Patterns, §Pitfall 2, §Pitfall 5
    - src/components/Toolbar.tsx (after Task 2 — confirm data-testid values)
    - src/components/UploadTextureModal.tsx:460 (__driveTextureUpload signature)
    - src/three/ThreeViewport.tsx (after Task 2 — confirm __setTestCamera is installed)
    - src/App.tsx:333-360 (viewMode-driven conditional mount)
  </read_first>
  <action>
    1. **Fixtures** — commit SMALL real image files (~10KB each):
       - `tests/e2e/fixtures/sample-wallpaper.jpg` — any small real JPEG (not a bundled Vite asset — the spec uploads it via data-URL to exercise the IDB/createObjectURL path per 36-CONTEXT §specifics line 126).
       - `tests/e2e/fixtures/sample-wallart.png` — any small real PNG.
       (If existing sample images exist in `src/assets/`, copy them; otherwise use a tiny generated JPEG/PNG.)

    2. **Helpers** in `tests/e2e/playwright-helpers/` (small, composable):
       - `setTestCamera.ts`: `export async function setTestCamera(page, pose: { position: [number,number,number]; target: [number,number,number] })` — wraps `page.evaluate(p => window.__setTestCamera(p), pose)`.
       - `uploadTexture.ts`: `export async function uploadTexture(page, fixturePath: string, name: string, tileSizeFt: number): Promise<string>` — reads fixture bytes, base64-encodes, calls `window.__driveTextureUpload` via `page.evaluate`. Returns the texture id.
       - `toggleViewMode.ts`: `export async function toggleViewMode(page, mode: "2d" | "3d" | "split")` — `await page.click('[data-testid="view-mode-' + mode + '"]')`.
       - `settle.ts`: `export async function settle(page)` — `await page.waitForTimeout(200); await page.evaluate(() => new Promise(r => requestAnimationFrame(() => requestAnimationFrame(r))));` (D-02 lock).
       - `lifecycleEvents.ts`: `export async function getLifecycleEvents(page)` — `return await page.evaluate(() => (window as any).__textureLifecycleEvents ?? []);` (reads at END only — 36-RESEARCH Anti-Patterns).

    3. **Spec files** (all 4, based on the skeleton in 36-RESEARCH §Code Examples):

       Common preamble for all specs:
       - `test.beforeEach` clears IDB: `await page.evaluate(() => indexedDB.deleteDatabase("room-cad-user-textures"))` + `page.reload()`.
       - Fixed camera pose via `setTestCamera` BEFORE every `toHaveScreenshot` call (D-03).
       - Settle (`settle(page)`) before every screenshot.
       - End of test: dump lifecycle events via `console.log(JSON.stringify(events, null, 2))` — captured in Playwright trace for ROOT-CAUSE.md evidence.

       `wallpaper-2d-3d-toggle.spec.ts`:
       - 5 toggle cycles. Screenshot each 3D mount as `wallpaper-cycle-{1..5}.png`.
       - Each screenshot uses `{ maxDiffPixelRatio: 0.01 }`.
       - Assertion: screenshots 2-5 match screenshot 1 (Playwright's `toHaveScreenshot` with a stable baseline name per cycle — all 5 will regenerate on first `--update-snapshots` run).

       `wallart-2d-3d-toggle.spec.ts`: same structure, but apply the texture as wallArt instead of wallpaper. 5 cycles.

       `floor-user-texture-toggle.spec.ts`: 2 cycles. Apply user texture as floor finish.

       `ceiling-user-texture-toggle.spec.ts`: 2 cycles. Apply user texture as ceiling finish.

       The exact call-sequence to apply a texture to each surface should be added via `page.evaluate` direct store action (reuse `useCADStore.getState().updateRoom(...)` or the relevant surface-specific action) rather than click-through the picker — simpler and more deterministic. Claude's discretion on exact call shape.

    4. **tests/e2e/README.md** — document the baseline-update workflow per 36-RESEARCH Pitfall 5:
       > "To update golden images, run `npm run test:e2e -- --update-snapshots` ONLY after confirming the visual change is intentional. Commit updated `*-snapshots/*.png` files in the same PR as the source change so reviewers see the diff."
       Also note: fake-indexeddb is NOT used here — Playwright uses real browser IDB.

    5. **Run the harness** — do NOT yet have baselines. First pass generates them:
       ```
       npm run test:e2e -- --update-snapshots
       ```
       This MUST produce a failure report showing the regression pattern (cycles 2-5 differ from cycle 1 beyond 1%). The failure IS THE POINT — it's the proof of reproduction required before Plan 36-01 merges.

       Capture:
       - Full Playwright trace (stored in `test-results/`)
       - Lifecycle events JSON from console output
       - Screenshot diffs

       These feed Task 4.

       If `--update-snapshots` somehow passes on cycle 1 but the subsequent runs catch cycles 2-5 differing — that's normal baseline-set behavior. The follow-up `npm run test:e2e` run WITHOUT `--update-snapshots` is the actual proof-of-reproduction.
  </action>
  <verify>
    <automated>
      test -f tests/e2e/fixtures/sample-wallpaper.jpg
      && test -f tests/e2e/fixtures/sample-wallart.png
      && test -f tests/e2e/playwright-helpers/setTestCamera.ts
      && test -f tests/e2e/playwright-helpers/uploadTexture.ts
      && test -f tests/e2e/playwright-helpers/toggleViewMode.ts
      && test -f tests/e2e/playwright-helpers/settle.ts
      && test -f tests/e2e/playwright-helpers/lifecycleEvents.ts
      && test -f tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts
      && test -f tests/e2e/specs/wallart-2d-3d-toggle.spec.ts
      && test -f tests/e2e/specs/floor-user-texture-toggle.spec.ts
      && test -f tests/e2e/specs/ceiling-user-texture-toggle.spec.ts
      && test -f tests/e2e/README.md
      && grep -q '__driveTextureUpload' tests/e2e/playwright-helpers/uploadTexture.ts
      && grep -q '__setTestCamera' tests/e2e/playwright-helpers/setTestCamera.ts
      && grep -q 'requestAnimationFrame' tests/e2e/playwright-helpers/settle.ts
      && grep -q 'maxDiffPixelRatio' tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts
      && grep -c 'cycle' tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts | awk '$1 >= 5 { exit 0 } { exit 1 }'
      && npx playwright test --list tests/e2e/specs/
    </automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist at listed paths (verified by `test -f`)
    - `uploadTexture.ts` calls `__driveTextureUpload` (grep confirms)
    - `setTestCamera.ts` calls `__setTestCamera` (grep confirms)
    - `settle.ts` awaits `requestAnimationFrame` twice (grep confirms the word appears; visual inspection confirms it's wrapped in a Promise)
    - Each wallpaper/wallArt spec contains exactly 5 toggle cycles (grep `cycle-` appears ≥5 times, OR a `for (let cycle = 1; cycle <= 5)` loop exists)
    - Each floor/ceiling spec contains exactly 2 cycles
    - `npx playwright test --list` lists all 4 spec files in the output
    - Running `npm run test:e2e` (after `--update-snapshots`) WITHOUT the flag produces a FAILING report showing cycle-2+ pixel diff > 1% — this is expected proof-of-reproduction (captured for ROOT-CAUSE.md)
  </acceptance_criteria>
  <done>Harness runs end-to-end, reproduces VIZ-10 regression (FAILS as designed), lifecycle evidence captured for ROOT-CAUSE.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (Wave 2): Write ROOT-CAUSE.md with inline harness evidence + Phase 32 cross-reference + defensive-code triage</name>
  <files>.planning/phases/36-viz-10-regression/ROOT-CAUSE.md</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/36-CONTEXT.md §Root-Cause Discipline (R-01 through R-04)
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md §Phase 32 Still-Plausible Candidate Causes, §Phase 32 Defensive Code Inventory
    - .planning/phases/32-pbr-foundation/32-07-SUMMARY.md (lines 36-43 — COPY VERBATIM into §1)
    - Playwright trace output + lifecycle-events JSON from Task 3's harness run
    - src/three/userTextureCache.ts:31
    - src/three/WallMesh.tsx:136,182,268,288
    - src/three/FloorMesh.tsx:102,127
    - src/three/CeilingMesh.tsx:110
    - tests/wallMeshDisposeContract.test.ts
  </read_first>
  <action>
    Write `.planning/phases/36-viz-10-regression/ROOT-CAUSE.md` with EXACTLY these 4 sections (R-02):

    **Section 1 — "Phase 32 Candidate Causes (from 32-07-SUMMARY.md:36-43)":**
    Copy all 4 candidate causes VERBATIM from `.planning/phases/32-pbr-foundation/32-07-SUMMARY.md` lines 38-42. Include the source line reference. Each candidate gets a status bar: `Status: CONFIRMED | REFUTED | INCONCLUSIVE`.

    **Section 2 — "Actual Cause":**
    Name the cause identified by the harness. REQUIRED evidence INLINE (not linked — R-02 specifies inline embed for audit standalone value):
    - Relevant excerpts from `window.__textureLifecycleEvents[]` (paste the JSON inline, trimmed to the ~20-50 events most diagnostic)
    - `tex.uuid` sequence across mounts (same? different? null?)
    - Screenshot diff observation (describe what the pixels show — e.g., "material renders black on mounts 2-5; mount 1 renders correct texture")
    - WebGL state observations, if relevant (from DevTools or inspection)
    - Cite which tap points produced the evidence (file:line)

    If ALL 4 Phase 32 candidates are proven WRONG, ALSO add a sub-section "Actual cause not previously considered" (R-03) with the same evidence standard.

    If the harness CANNOT reproduce the regression, document the full attempt (what was tried, what evidence was captured, why it falls short) per R-04. In this case, §4 must recommend "keep all Phase 32 defensive code" with justification.

    **Section 3 — "Evidence Ruling Out Other Candidates":**
    For each of the other 3 (or all 4 if §2 is a 5th cause), explain with log evidence WHY it's not the cause. Reference specific lifecycle events.

    **Section 4 — "Phase 32 Defensive-Code Triage":**
    For each of the 3 pieces (case-by-case per C-01):
    1. **Non-disposing `userTextureCache` Map** (`src/three/userTextureCache.ts:31`)
    2. **`<primitive attach="map" dispose={null} />`** at render sites (`WallMesh.tsx:136,182,268,288`; `FloorMesh.tsx:102,127`; `CeilingMesh.tsx:110`)
    3. **Static regression test** (`tests/wallMeshDisposeContract.test.ts`)

    Classify each as:
    - **KEEP** — evidence shows it IS load-bearing; Plan 36-02 must retain it (add a code comment pointing back to ROOT-CAUSE.md §4).
    - **SIMPLIFY** — evidence justifies partial relaxation (specify exact change); Plan 36-02 implements the simplification with a commit citing this doc.
    - **REMOVE** — evidence proves redundant; Plan 36-02 deletes with a commit citing this doc.

    NO BLANKET RULES. Each piece gets its own disposition with its own justification. Default bias: KEEP (per C-01).

    Also briefly note Phase 34 guards (userTextureCache.test.tsx, userTextureSnapshot.test.ts) — classify as KEEP (orthogonal unit-level contracts, independent of this investigation).

    **Frontmatter + Header:**
    ```markdown
    ---
    phase: 36-viz-10-regression
    type: root-cause
    written_during: Plan 36-01
    harness_status: reproduced | not-reproduced
    actual_cause_category: one-of-candidates-1-4 | fifth-cause | no-repro
    ---

    # ROOT-CAUSE.md — VIZ-10 Wallpaper/wallArt 2D↔3D Regression

    **Written:** <date>
    **Evidence source:** Playwright harness in tests/e2e/specs/ (Plan 36-01)
    **Rule:** This document is authored BEFORE any fix (CONTEXT R-01). Plan 36-02's fix must reference this document, not speculation.

    ---
    ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -q '## ' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -cE 'Candidate (Cause )?[1-4]|HTMLImageElement|meshStandardMaterial|ImageBitmap|300x150|300×150' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md | awk '$1 >= 4 { exit 0 } { exit 1 }'
      && grep -qE 'Actual Cause|actual cause' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'userTextureCache|userTextureCache\.ts' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'dispose=\{null\}|dispose=null|primitive attach' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'wallMeshDisposeContract|regression test' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'KEEP|SIMPLIFY|REMOVE|load-bearing|redundant' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE '__textureLifecycleEvents|lifecycle' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/36-viz-10-regression/ROOT-CAUSE.md`
    - Contains 4 top-level sections (grep for `## ` count ≥ 4)
    - References all 4 Phase 32 candidate causes by keyword (HTMLImageElement, meshStandardMaterial, ImageBitmap, 300×150 OR 300x150)
    - §2 names an actual cause OR explicitly documents no-repro per R-04
    - §4 triages each of 3 defensive pieces individually — grep finds mentions of `userTextureCache`, `dispose={null}` (or similar primitive-attach phrase), AND `wallMeshDisposeContract`
    - Each defensive piece has a KEEP / SIMPLIFY / REMOVE classification (grep finds at least one)
    - Document embeds lifecycle-event evidence INLINE (grep finds `__textureLifecycleEvents` or `lifecycle`)
    - Frontmatter present with `type: root-cause` and `harness_status`
  </acceptance_criteria>
  <done>ROOT-CAUSE.md written with inline harness evidence. Plan 36-02 can now proceed using this as its evidence base. If harness did not reproduce, document reflects no-repro outcome honestly (R-04 — valid terminal state).</done>
</task>

</tasks>

<verification>
**Plan-level verification** (run after all 4 tasks):

1. All Wave 0 infrastructure present:
   ```
   test -f playwright.config.ts
   test -f tests/e2e/README.md
   grep -q 'test:e2e' package.json
   grep -q 'tests/e2e' vitest.config.ts
   ```

2. All instrumentation test-mode gated:
   ```
   grep -l '__textureLifecycleEvents' src/three/*.ts src/hooks/*.ts | xargs -I {} grep -L 'import.meta.env.MODE' {} || echo "OK — all instrumented files check MODE"
   ```

3. Harness runs end-to-end (expected to FAIL pre-fix — failure IS proof of reproduction):
   ```
   npm run test:e2e -- --update-snapshots   # generate baselines
   npm run test:e2e                         # re-run: SHOULD FAIL on cycles 2-5 for wallpaper/wallArt
   ```

4. Unit tests still green:
   ```
   npm test -- --run
   ```

5. ROOT-CAUSE.md complete per acceptance criteria in Task 4.
</verification>

<success_criteria>
- Playwright ^1.45+ installed as devDep + Chromium binary
- `playwright.config.ts` with both `chromium-dev` (active) and `chromium-preview` (scaffolded, inactive) projects
- 4 spec files exercise wallpaper (5 cycles) + wallArt (5 cycles) + floor (2 cycles) + ceiling (2 cycles)
- 5 helpers + 2 fixtures committed
- 4 cache modules instrumented with lifecycle taps (test-mode gated)
- `window.__setTestCamera` helper installed in ThreeViewport (test-mode gated)
- `data-testid` attributes on Toolbar view-mode buttons
- `vitest.config.ts` excludes `tests/e2e/**`; npm scripts `test:e2e` + `test:e2e:debug` added
- `.gitignore` excludes `playwright-report/` + `test-results/` but commits `*-snapshots/`
- Harness reproduces VIZ-10 failure (fails pre-fix) — proof-of-reproduction captured
- `ROOT-CAUSE.md` written with 4 required sections per R-02 including inline evidence
- Phase 32 candidate causes (1-4) each classified confirmed/refuted/inconclusive
- Phase 32 defensive-code pieces (3) each triaged KEEP/SIMPLIFY/REMOVE individually
- Zero production code behavior change (instrumentation only, test-mode gated)
- All 74+ existing vitest tests still green
- GitHub Actions workflow NOT created in this plan (deferred to Plan 36-02 end per H-02)
</success_criteria>

<output>
After completion, create `.planning/phases/36-viz-10-regression/36-01-SUMMARY.md` documenting:
- Harness files created
- Lifecycle-event taps installed (file:line)
- Harness run outcome (reproduced / not-reproduced)
- ROOT-CAUSE.md summary: cause named + defensive-code triage decisions
- Files changed in production (instrumentation only, test-mode gated)
- Handoff to Plan 36-02: which defensive pieces to KEEP/SIMPLIFY/REMOVE per ROOT-CAUSE.md §4
</output>