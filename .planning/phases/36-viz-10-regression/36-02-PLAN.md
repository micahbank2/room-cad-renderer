---
phase: 36-viz-10-regression
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
  - playwright.config.ts
  - .github/workflows/e2e.yml
  # Production source files: determined by ROOT-CAUSE.md findings at Plan 36-01 merge — may include:
  # - src/three/userTextureCache.ts (if cause 1 or 2)
  # - src/three/WallMesh.tsx / FloorMesh.tsx / CeilingMesh.tsx (if cause 2 or 4)
  # - src/three/ThreeViewport.tsx (if cause 4 — canvas sizing)
  # - src/hooks/useUserTexture.ts (if cause 2 — needsUpdate)
  # - tests/wallMeshDisposeContract.test.ts (if defensive code gets SIMPLIFY/REMOVE per §4)
autonomous: true
requirements: [VIZ-10]
requirements_addressed: [VIZ-10]

must_haves:
  truths:
    - "Plan executor has READ ROOT-CAUSE.md before making any fix decision"
    - "Fix targets the specific cause named in ROOT-CAUSE.md §2 (not speculation)"
    - "Phase 32 defensive-code dispositions (KEEP/SIMPLIFY/REMOVE) from ROOT-CAUSE.md §4 are reflected in production code and test suite"
    - "After fix, Playwright harness all 4 surfaces pass with ≤1% pixel delta per ROADMAP success criterion #3"
    - "Retained harness runs on every PR via .github/workflows/e2e.yml (ROADMAP success criterion #4)"
    - "If ROOT-CAUSE.md documented no-repro (R-04), defensive code is kept intact and the harness is still retained as regression guard"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "CI regression guard on every PR, Chromium headless"
      contains: "pull_request"
    - path: "playwright.config.ts"
      provides: "Activated chromium-preview project + preview webServer entry"
      contains: "vite preview"
  key_links:
    - from: ".github/workflows/e2e.yml"
      to: "npm run test:e2e"
      via: "GitHub Actions job step"
      pattern: "test:e2e"
    - from: "fix commits"
      to: ".planning/phases/36-viz-10-regression/ROOT-CAUSE.md"
      via: "commit message citation"
      pattern: "ROOT-CAUSE"
---

<objective>
Implement the VIZ-10 fix informed by ROOT-CAUSE.md findings from Plan 36-01. Execute Phase 32 defensive-code triage decisions (KEEP/SIMPLIFY/REMOVE) per ROOT-CAUSE.md §4. Activate the production-preview Playwright project. Wire the retained harness into CI via GitHub Actions. Verify all 4 surfaces (wallpaper/wallArt/floor/ceiling) pass with ≤1% pixel delta across their cycle counts.

Purpose: Close VIZ-10 with evidence-based fix, retain the harness as a permanent regression guard.

Output: Fix commit(s) per ROOT-CAUSE.md findings + activated prod-preview project + CI workflow + green Playwright suite + updated goldens + closed VIZ-10 issue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-viz-10-regression/36-CONTEXT.md
@.planning/phases/36-viz-10-regression/36-RESEARCH.md
@.planning/phases/36-viz-10-regression/ROOT-CAUSE.md
@.planning/phases/36-viz-10-regression/36-01-SUMMARY.md

<interfaces>
<!-- Everything depends on what ROOT-CAUSE.md says. The executor MUST read it first. -->
<!-- The fix code, the test updates, and the defensive-code disposition flow from §2 and §4 of ROOT-CAUSE.md. -->

Anti-specification: Do NOT speculate about the fix here. The first task is a hard gate that reads ROOT-CAUSE.md and branches execution based on what it says.

Relevant source files (may or may not be touched depending on §2 finding):
- src/three/userTextureCache.ts — cache contract + lifecycle tap (instrumentation added in Plan 36-01)
- src/three/wallpaperTextureCache.ts / wallArtTextureCache.ts — legacy cache paths
- src/three/WallMesh.tsx, FloorMesh.tsx, CeilingMesh.tsx — `<primitive attach="map" dispose={null} />` sites
- src/three/ThreeViewport.tsx — R3F Canvas + Scene + (post-Plan-36-01) __setTestCamera + viewport-mount/unmount taps
- src/hooks/useUserTexture.ts — React consumer hook
- tests/wallMeshDisposeContract.test.ts — static contract test (may SIMPLIFY/REMOVE per §4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 (Wave 1): GATE — read ROOT-CAUSE.md, derive fix plan + defensive-code triage plan</name>
  <files>(no edits — produces a planning decision recorded in the task's git notes or PR description)</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/ROOT-CAUSE.md (FULL — this is the source of truth for every downstream decision in this plan)
    - .planning/phases/36-viz-10-regression/36-01-SUMMARY.md
    - .planning/phases/36-viz-10-regression/36-CONTEXT.md §Root-Cause Discipline (R-01 through R-04) and §Phase 32 Code + CI Guard (C-01)
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md §Phase 32 Defensive Code Inventory
  </read_first>
  <action>
    This task is a HARD GATE. Do not proceed to Task 2 until ROOT-CAUSE.md has been read and a fix plan derived.

    1. Verify ROOT-CAUSE.md exists:
       ```
       test -f .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
       ```
       If missing → STOP. Plan 36-01 did not ship. Escalate to the user.

    2. Read ROOT-CAUSE.md fully. Extract:
       - **§2 actual cause** — which of Phase 32 candidates 1-4, OR a 5th cause, OR no-repro?
       - **§4 defensive-code triage** — for each of 3 pieces (non-disposing cache, primitive-attach dispose={null}, static regression test): KEEP | SIMPLIFY | REMOVE?

    3. **Branch A — Cause identified (most likely case):** Derive concrete fix based on which cause §2 names:
       - Cause 1 (HTMLImageElement context binding) → fix likely adds `texture.source.needsUpdate = true` on every mount in `userTextureCache.ts` / `wallpaperTextureCache.ts` / `wallArtTextureCache.ts`.
       - Cause 2 (meshStandardMaterial uniform re-link) → fix likely adds `material.needsUpdate = true` handling in WallMesh/FloorMesh/CeilingMesh (via `useEffect` keyed on texture).
       - Cause 3 (ImageBitmap decode state) → fix likely switches loader to re-decode per mount (new `createImageBitmap(blob)` call).
       - Cause 4 (canvas sizing) → fix likely adds a `ResizeObserver` check or initial-size enforcement on the R3F `<Canvas>` parent container in `App.tsx` or `ThreeViewport.tsx`.
       - 5th cause → fix shape is whatever ROOT-CAUSE.md §2 evidence indicates.

    4. **Branch B — No-repro (R-04 terminal state):** Skip all fix tasks. Proceed directly to Task 4 (activate prod-preview + CI). Document in the final SUMMARY.md that the harness is retained as a permanent guard and all defensive code is kept (per §4 directive in ROOT-CAUSE.md).

    5. **Defensive-code triage execution list:** From §4, for each piece, record the action Task 3 must take:
       - KEEP → no code change; add a code comment `// Kept per .planning/phases/36-viz-10-regression/ROOT-CAUSE.md §4 — evidence: <summary>`
       - SIMPLIFY → specify exact change (e.g., "remove `dispose={null}` on FloorMesh.tsx:127 only, keep others")
       - REMOVE → specify exact deletion (e.g., "delete `tests/wallMeshDisposeContract.test.ts`")

    6. Record the derived plan in this task's commit message (or in the PR description) so reviewers can verify fidelity to ROOT-CAUSE.md. Example commit: `chore(36-02): gate check — ROOT-CAUSE.md §2 names cause 2 (meshStandardMaterial); §4 disposition: KEEP cache, SIMPLIFY dispose={null} at FloorMesh.tsx only, KEEP static test`.

    This task produces no file edits, but its output shapes Tasks 2 and 3.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'Actual Cause|actual cause' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
      && grep -qE 'KEEP|SIMPLIFY|REMOVE|keep|simplify|remove' .planning/phases/36-viz-10-regression/ROOT-CAUSE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36-viz-10-regression/ROOT-CAUSE.md` exists (verified by `test -f`)
    - ROOT-CAUSE.md has an §2 Actual Cause section (grep match)
    - ROOT-CAUSE.md has defensive-code triage decisions (grep finds KEEP/SIMPLIFY/REMOVE or equivalent)
    - Executor's commit message or PR description explicitly cites the cause named in §2 AND lists the 3 defensive-piece dispositions from §4 — NOT general speculation
    - Decision is ONE of: "fix cause X per §2" OR "no-repro per R-04, skip fix"
  </acceptance_criteria>
  <done>ROOT-CAUSE.md read; fix plan derived; defensive-code triage list recorded. Executor knows exactly which files to touch in Task 2 and Task 3 — no guessing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Implement the fix per ROOT-CAUSE.md §2</name>
  <files>
    (file list determined by Task 1 gate — typically 1-3 of:
    src/three/userTextureCache.ts,
    src/three/wallpaperTextureCache.ts,
    src/three/wallArtTextureCache.ts,
    src/three/WallMesh.tsx,
    src/three/FloorMesh.tsx,
    src/three/CeilingMesh.tsx,
    src/three/ThreeViewport.tsx,
    src/hooks/useUserTexture.ts)
  </files>
  <read_first>
    - .planning/phases/36-viz-10-regression/ROOT-CAUSE.md §2 (Actual Cause — evidence + cited tap points)
    - .planning/phases/36-viz-10-regression/36-01-SUMMARY.md
    - The specific source files named in §2's evidence (cited by file:line)
    - tests/userTextureCache.test.tsx (Phase 34 unit guard — the fix must NOT break these)
    - tests/userTextureSnapshot.test.ts (Phase 34 5-cycle stability guard — must still pass)
  </read_first>
  <behavior>
    Tests added or updated in this task MUST precede or accompany the implementation (tdd="true"). Exact test shape depends on §2:
    - If fix adds `needsUpdate` semantics → unit test asserts `texture.source.needsUpdate === true` on subsequent hook mounts OR `material.needsUpdate === true` on remount.
    - If fix re-decodes image → unit test asserts the decode function is called once per mount, not once per session.
    - If fix handles canvas sizing → unit test or integration test asserts canvas dimensions are non-300×150 on second mount.
    - If no-repro (Task 1 Branch B) → SKIP this task entirely.

    Baseline: Phase 34's unit tests (`tests/userTextureCache.test.tsx:122`, `tests/userTextureSnapshot.test.ts:168`) must still pass.
  </behavior>
  <action>
    **Branch B (no-repro):** Skip this task. Proceed to Task 3 for defensive-code triage (which in Branch B is KEEP-all).

    **Branch A (cause identified):**

    1. Write a failing unit test FIRST (Red):
       - Scope the test to the specific contract the fix will introduce.
       - Example for cause 2: "on second mount of useUserTexture with same id, the returned material's `map` references a texture with `needsUpdate === true`".
       - Example for cause 1: "after cache hit from cross-context remount, `tex.source.needsUpdate === true`".
       - Run `npm test -- --run <newTestPath>` — confirm RED.

    2. Implement the minimal fix in the files named by §2 evidence (Green):
       - Fix must be scoped to the smallest code path needed to address §2.
       - Preserve all test-mode instrumentation from Plan 36-01 — do not remove or alter `__textureLifecycleEvents` taps.
       - Preserve all Phase 34 unit-test guarantees — `userTextureCache` stays non-disposing unless §4 REMOVE directive explicitly says otherwise (handled in Task 3, not here).

    3. Run `npm test -- --run` — ALL unit tests green.

    4. Run `npm run test:e2e` — expect the 4-surface harness to PASS. If still failing:
       - Do NOT modify goldens yet (that hides a real regression).
       - Re-read ROOT-CAUSE.md §2 evidence. The fix may be incomplete. Iterate within this task.
       - If after 2-3 iterations the fix still doesn't produce green, the cause named in §2 may be wrong. Escalate — amend ROOT-CAUSE.md (this is valid per R-02 — the document is the authoritative evidence log) and re-derive fix in Task 1 gate terms.

    5. Once harness passes: regenerate goldens from the FIXED state:
       ```
       npm run test:e2e -- --update-snapshots
       ```
       Commit the new `tests/e2e/specs/*-snapshots/*.png` files in the same commit as the source fix (36-RESEARCH Pitfall 5 — reviewers must see the diff).

    6. Re-run `npm run test:e2e` WITHOUT `--update-snapshots` — must be GREEN.

    7. Commit message format: `fix(36-02): <one-line>` with body citing `ROOT-CAUSE.md §2 cause X`.
  </action>
  <verify>
    <automated>
      npm test -- --run
      && npm run test:e2e
    </automated>
  </verify>
  <acceptance_criteria>
    - If Branch A: unit test added that specifically asserts the fix contract; test is RED pre-fix, GREEN post-fix
    - `npm test -- --run` passes (all Phase 34 unit guards still green)
    - `npm run test:e2e` passes (all 4 surfaces, ≤1% pixel delta across all cycles — ROADMAP success criterion #3)
    - Updated goldens committed in same commit as source fix (`*-snapshots/*.png`)
    - Commit message body cites `ROOT-CAUSE.md §2` and the specific cause number/name
    - Fix touches only files whose line:range was cited by §2 evidence — no drive-by edits
    - `__textureLifecycleEvents` instrumentation from Plan 36-01 is still present (grep confirms)
  </acceptance_criteria>
  <done>VIZ-10 fixed. All 4 Playwright surfaces green. Phase 34 unit guards still green. Fix traceable to ROOT-CAUSE.md §2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (Wave 2): Execute Phase 32 defensive-code triage per ROOT-CAUSE.md §4</name>
  <files>
    (file list determined by Task 1 gate — subset of:
    src/three/userTextureCache.ts,
    src/three/WallMesh.tsx,
    src/three/FloorMesh.tsx,
    src/three/CeilingMesh.tsx,
    tests/wallMeshDisposeContract.test.ts,
    plus any new code comments in files marked KEEP)
  </files>
  <read_first>
    - .planning/phases/36-viz-10-regression/ROOT-CAUSE.md §4 (triage decisions — source of truth for this task)
    - Task 1 gate commit message (executor's derived disposition list)
    - src/three/userTextureCache.ts:31 (non-disposing cache)
    - src/three/WallMesh.tsx:136,182,268,288 (dispose={null} sites)
    - src/three/FloorMesh.tsx:102,127
    - src/three/CeilingMesh.tsx:110
    - tests/wallMeshDisposeContract.test.ts (full)
  </read_first>
  <action>
    Execute the 3 dispositions recorded in Task 1. NO BLANKET RULES — each piece gets individual handling.

    **Piece 1 — Non-disposing userTextureCache (`src/three/userTextureCache.ts`):**
    - If KEEP: Add a line comment above the cache Map declaration (line 31): `// Non-disposing per ROOT-CAUSE.md §4 — evidence: <one-line summary of why it's load-bearing>. Phase 32 rationale: 32-06-SUMMARY.md.`
    - If SIMPLIFY: Apply the specific change from §4 (e.g., "cap map size at N entries", "clear on project-switch only"). Write a unit test covering the new contract.
    - If REMOVE: Delete the non-disposing semantics (restore refcount disposal). Write a commit with message `refactor(36-02): restore userTextureCache refcount disposal per ROOT-CAUSE.md §4`. Update `tests/userTextureCache.test.tsx` expectations accordingly.

    **Piece 2 — `<primitive attach="map" dispose={null} />` at mesh render sites:**
    - For each of WallMesh.tsx:136/182/268/288, FloorMesh.tsx:102/127, CeilingMesh.tsx:110, apply §4's per-site decision. §4 may classify some KEEP and others SIMPLIFY — handle each individually.
    - KEEP: Add inline comment above the `<primitive>` line: `// dispose={null} kept per ROOT-CAUSE.md §4 — <one-line reason>`
    - SIMPLIFY: Replace `<primitive attach="map" object={tex} dispose={null} />` with `map={tex}` shorthand at the specific sites §4 names. Do NOT touch sites §4 says KEEP.
    - REMOVE: Same mechanic as SIMPLIFY but flagged explicitly.

    **Piece 3 — Static regression test (`tests/wallMeshDisposeContract.test.ts`):**
    - KEEP: Add a comment in the test file header: `// Retained per ROOT-CAUSE.md §4 — static contract for <reason>. See .planning/phases/36-viz-10-regression/ROOT-CAUSE.md.` Also add a reference to the Playwright harness: `// Runtime verification: tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts (and 3 others).`
    - SIMPLIFY: Edit per §4 directive (e.g., remove specific assertions that are now redundant, keep structural ones).
    - REMOVE: Delete the file. Commit message: `refactor(36-02): delete wallMeshDisposeContract static test per ROOT-CAUSE.md §4 — superseded by Playwright harness`.

    Run the full suite after each piece:
    ```
    npm test -- --run && npm run test:e2e
    ```

    Every disposition commit must cite `ROOT-CAUSE.md §4` in the commit body. This is non-negotiable — the audit trail from evidence → decision → code must be intact.
  </action>
  <verify>
    <automated>
      npm test -- --run
      && npm run test:e2e
      && grep -qE 'ROOT-CAUSE\.md|ROOT-CAUSE' src/three/userTextureCache.ts || true
    </automated>
  </verify>
  <acceptance_criteria>
    - All 3 Phase 32 defensive pieces handled per ROOT-CAUSE.md §4 disposition (no drift, no drive-by decisions)
    - Every KEEP piece has a code comment citing `ROOT-CAUSE.md §4`
    - Every SIMPLIFY or REMOVE piece has a commit message citing `ROOT-CAUSE.md §4`
    - `npm test -- --run` green — Phase 34 unit guards still valid (unless §4 REMOVE explicitly obsoleted them, in which case they were deleted/updated in this task)
    - `npm run test:e2e` green — Playwright harness still passes post-triage
    - `grep 'ROOT-CAUSE' src/three/ tests/ -r` shows the audit-trail comments exist in KEEP-marked files
  </acceptance_criteria>
  <done>Defensive-code triage executed. Each piece reflects its §4 disposition. Audit trail (code comments + commit messages) maps every change back to ROOT-CAUSE.md §4 evidence. Suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 (Wave 2): Activate production-preview Playwright project + create .github/workflows/e2e.yml CI guard</name>
  <files>playwright.config.ts, .github/workflows/e2e.yml</files>
  <read_first>
    - .planning/phases/36-viz-10-regression/36-CONTEXT.md §Phase 32 Code + CI Guard (C-02, C-03, C-04)
    - .planning/phases/36-viz-10-regression/36-RESEARCH.md §Architecture Patterns §Pattern 1, §Open Questions Q1 (production-preview + MODE=test)
    - playwright.config.ts (current state after Plan 36-01 Task 1 — has `chromium-preview` project scaffolded but no preview webServer)
    - package.json (scripts + engines)
    - .gitignore (confirm playwright-report/ + test-results/ already excluded)
  </read_first>
  <action>
    1. **Activate chromium-preview project in `playwright.config.ts`:**
       - Add a second entry to `webServer` array:
         ```ts
         {
           command: "npm run build && npx vite preview --mode test --port 4173 --strictPort",
           port: 4173,
           reuseExistingServer: !process.env.CI,
           timeout: 120_000,
         }
         ```
       - The `--mode test` on the preview step addresses 36-RESEARCH Open Question Q1 (propagate MODE=test through prod build). If minification eliminates test-only gates, the prod-preview project will reveal it.
       - Update `chromium-preview` project config to use port 4173 (via `baseURL` or `use.baseURL: "http://localhost:4173"`).
       - `chromium-dev` stays on port 5173.

    2. **Create `.github/workflows/e2e.yml`:**
       Exact specification from locked decisions (C-02, C-03, C-04):
       - Trigger: `on: [pull_request]` — every PR, no branch filter, no `paths:` filter (C-02).
       - Single job `e2e` running on `ubuntu-latest`.
       - Steps:
         1. `actions/checkout@v4`
         2. `actions/setup-node@v4` with node version matching local (pull from `package.json` engines if set; else use a reasonable default like 20 or 22).
         3. `npm ci`
         4. `npx playwright install --with-deps chromium` (C-03 — Chromium only, C-04 — Chromium-only browser matrix).
         5. `npm test -- --run` (vitest unit tests).
         6. `npm run test:e2e` (Playwright — both projects).
         7. Upload `playwright-report/` and `test-results/` as artifacts on failure (`if: failure()`).
       - Budget: ~90s per 36-CONTEXT C-02. The workflow includes no timeouts tighter than that.

       Example sketch (executor refines per GH Actions current best practices):
       ```yaml
       name: E2E Regression Harness
       on:
         pull_request:
       jobs:
         e2e:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: "22"
                 cache: "npm"
             - run: npm ci
             - run: npx playwright install --with-deps chromium
             - run: npm test -- --run
             - run: npm run test:e2e
             - if: failure()
               uses: actions/upload-artifact@v4
               with:
                 name: playwright-report
                 path: |
                   playwright-report/
                   test-results/
       ```

    3. **Local validation:**
       - Run `npm run test:e2e` — BOTH projects green (chromium-dev + chromium-preview).
       - If chromium-preview fails where chromium-dev passes: investigate per Open Question Q1 (minifier dropping test gates). Document finding in ROOT-CAUSE.md appendix — this is a valuable cross-check.

    4. **GitHub Actions validation:**
       - Push the branch, open PR, confirm the workflow triggers and passes.
       - If the PR-close auto-generates the workflow-dispatch link in GH, verify the artifact upload step works on a forced failure (optional — skip if green-first-try).

    5. Per project CLAUDE.md "GitHub Issues — Living System Rule":
       - Confirm a GH issue exists for VIZ-10 (search by label `VIZ-10` or by issue title).
       - The PR body for this plan MUST include `Fixes #<VIZ-10-issue-number>` so merge auto-closes the issue.
       - PR body also includes `Spec: .planning/phases/36-viz-10-regression/36-02-PLAN.md` per the project CLAUDE.md convention.
  </action>
  <verify>
    <automated>
      test -f .github/workflows/e2e.yml
      && grep -q 'pull_request' .github/workflows/e2e.yml
      && grep -q 'playwright install' .github/workflows/e2e.yml
      && grep -q 'chromium' .github/workflows/e2e.yml
      && grep -q 'test:e2e' .github/workflows/e2e.yml
      && grep -q 'vite preview' playwright.config.ts
      && grep -q 'port: 4173' playwright.config.ts
      && grep -c 'webServer' playwright.config.ts | awk '$1 >= 1 { exit 0 } { exit 1 }'
      && npm run test:e2e
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/e2e.yml` exists
    - Workflow triggers on `pull_request` (grep confirms)
    - Workflow runs `npx playwright install --with-deps chromium` (grep confirms — C-03, C-04)
    - Workflow runs `npm test -- --run` AND `npm run test:e2e`
    - Workflow uploads `playwright-report/` + `test-results/` on failure
    - No `paths:` filter in workflow (C-02 — grep `paths:` returns no match OR returns only `paths-ignore` for irrelevant patterns)
    - `playwright.config.ts` has TWO webServer entries — dev on 5173, preview on 4173
    - `playwright.config.ts` preview webServer uses `vite preview --mode test` (36-RESEARCH Open Question Q1)
    - Local `npm run test:e2e` passes BOTH chromium-dev AND chromium-preview
    - When PR is pushed, the new GH Actions workflow runs and passes (manual confirmation)
    - PR body includes `Fixes #<VIZ-10-issue-number>` and `Spec: .planning/phases/36-viz-10-regression/36-02-PLAN.md`
  </acceptance_criteria>
  <done>Both Playwright projects active. CI workflow in place. Every future PR runs the 4-surface harness in Chromium headless — VIZ-10 can no longer silently regress. ROADMAP success criterion #4 satisfied.</done>
</task>

</tasks>

<verification>
**Plan-level verification** (run after all 4 tasks):

1. Fix traceability to ROOT-CAUSE.md:
   ```
   git log --grep 'ROOT-CAUSE' --oneline
   # Every fix / triage commit should cite ROOT-CAUSE.md §2 or §4
   ```

2. All 4 ROADMAP success criteria:
   - #1 Harness exists: `test -f tests/e2e/specs/wallpaper-2d-3d-toggle.spec.ts` ✓
   - #2 ROOT-CAUSE.md written before fix: timestamp check — file exists from Plan 36-01 ✓
   - #3 Fix: wallpaper + wallArt 5-cycle + floor + ceiling 2-cycle ≤1% delta: `npm run test:e2e` green ✓
   - #4 Retained as CI guard: `.github/workflows/e2e.yml` on pull_request ✓
   - #5 Defensive code kept or simplified with justification: audit trail in git log + code comments ✓

3. Full suite:
   ```
   npm test -- --run && npm run test:e2e
   ```
   Both green.

4. Harness validates both projects:
   ```
   npm run test:e2e -- --project=chromium-dev
   npm run test:e2e -- --project=chromium-preview
   ```
   Both green.

5. Manual UAT per 36-VALIDATION.md:
   - Open app locally (`npm run dev`), upload a real wallpaper image, toggle 2D→3D five times, verify texture persists every cycle.
   - This is the final human-confidence check per 36-VALIDATION.md manual-only section.
</verification>

<success_criteria>
- Task 1 gate passed: ROOT-CAUSE.md read, fix plan + triage plan derived and recorded
- Fix implemented per ROOT-CAUSE.md §2 with matching unit test (TDD)
- Phase 32 defensive-code triage executed per ROOT-CAUSE.md §4 — each piece handled individually (KEEP has code comment, SIMPLIFY/REMOVE has commit citation)
- All 4 Playwright surfaces pass with ≤1% pixel delta (ROADMAP #3)
- `chromium-preview` Playwright project active alongside `chromium-dev`
- `.github/workflows/e2e.yml` runs on every PR, Chromium-only, no path filters (ROADMAP #4; C-02/03/04)
- All 74+ vitest tests still green (instrumentation from Plan 36-01 preserved; Phase 34 guards intact)
- PR body includes `Fixes #<VIZ-10-issue>` + `Spec: .planning/phases/36-viz-10-regression/36-02-PLAN.md` per project CLAUDE.md
- Audit trail: every code change (fix or triage) traceable via commit message to ROOT-CAUSE.md §2 or §4
- If Plan 36-01 documented no-repro (R-04): Task 2 skipped, Task 3 keeps all defensive code, Task 4 still activates prod-preview + CI — harness retained as permanent guard regardless of whether a fix landed
</success_criteria>

<output>
After completion, create `.planning/phases/36-viz-10-regression/36-02-SUMMARY.md` documenting:
- ROOT-CAUSE.md §2 cause name + fix commit SHA(s)
- Files modified + rationale cited from §2
- Defensive-code triage outcome per §4 piece-by-piece (with commit SHAs)
- Production-preview project activation + CI workflow path
- Playwright harness run output (both projects green)
- GitHub issue closure status for VIZ-10
- Handoff: VIZ-10 is closed; harness is permanent regression guard; future cache-migration phases MUST pass this harness before merge
</output>