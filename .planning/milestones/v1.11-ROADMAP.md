---
milestone: v1.11
name: Pascal Feature Set
shipped: 2026-04-26
phases: [45, 46, 47, 48]
requirements: [THUMB-01, TREE-01, DISPLAY-01, CAM-04]
audit_status: passed_with_carry_over
---

# v1.11 Pascal Feature Set — Milestone Archive

## Goal

Adopt the 4 strongest features from the Pascal Editor competitive audit. v1.9 Phase 39 deferred these as "speculative"; user explicitly committed to them during v1.10 scoping as the next direction.

## Stats

- 4 phases (45, 46, 47, 48)
- 13 plans across 9 waves
- 4 requirements shipped (4/4 v1.11 issues closed: [#77](https://github.com/micahbank2/room-cad-renderer/issues/77), [#78](https://github.com/micahbank2/room-cad-renderer/issues/78), [#79](https://github.com/micahbank2/room-cad-renderer/issues/79), [#80](https://github.com/micahbank2/room-cad-renderer/issues/80))
- 60+ commits, ~5 PRs (each phase shipped via its own PR + small fix-up commits)
- Shipped 2026-04-25 to 2026-04-26 (under 24-hour milestone)
- Audit: `passed_with_carry_over` (1 medium-severity cross-phase gap → Phase 999.4)

## Phases

### Phase 45: Auto-Generated Material Swatch Thumbnails (THUMB-01)
**Goal:** Material picker swatches render from the live PBR/material pipeline rather than hand-curated static images.
**Depends on:** Phase 32 PBR foundation (texture cache, color-space helper); existing `surfaceMaterials.ts` data + picker components.
**Requirements:** THUMB-01
**Plans:** 2/2 complete
- [x] 45-01-PLAN.md — Renderer + cache module (`swatchThumbnailGenerator.ts`) + Wave 0 vitest
- [x] 45-02-PLAN.md — `MaterialThumbnail` component + `SurfaceMaterialPicker` wiring + Wave 0 vitest

### Phase 46: Rooms Hierarchy Sidebar Tree (TREE-01)
**Goal:** Sidebar gains a Rooms tree — collapsible per-room, nested children (walls/ceilings/products/custom-elements), click-to-focus, per-node visibility toggle.
**Depends on:** Phase 45 (sequencing, not code); Phase 33 `CollapsibleSection` primitive; existing `cadStore.rooms` structure; `uiStore.selectedIds`.
**Requirements:** TREE-01
**Plans:** 4/4 complete
- [x] 46-01-PLAN.md — Wave 0 RED test scaffolding (8 test files + pure-function stubs)
- [x] 46-02-PLAN.md — Wave 1 pure logic + uiStore additions (treeBuilder, wallLabels, hiddenIds, pendingCameraTarget)
- [x] 46-03-PLAN.md — Wave 1 component family + Sidebar integration (RoomsTreePanel, TreeRow, focusDispatch)
- [x] 46-04-PLAN.md — Wave 2 ThreeViewport wiring (visibility filter + pendingCameraTarget consumer)

### Phase 47: Room Display Modes (DISPLAY-01)
**Goal:** Toolbar gains a NORMAL / SOLO / EXPLODE display-mode selector.
**Depends on:** Phase 46 (rooms tree provides "active room" semantic); existing `uiStore.cameraMode` pattern (mirror for `displayMode`).
**Requirements:** DISPLAY-01
**Plans:** 3/3 complete
- [x] 47-01-PLAN.md — Wave 0 RED scaffolding (4 test files + 2 source stubs at canonical paths)
- [x] 47-02-PLAN.md — Wave 1 uiStore.displayMode + multi-room ThreeViewport refactor (RoomGroup, computeRoomOffsets)
- [x] 47-03-PLAN.md — Wave 1 Toolbar 3-button segmented control (NORMAL/SOLO/EXPLODE)

**Architectural shift:** ThreeViewport's Scene refactored from single-room (active room only) to multi-room iteration via new `RoomGroup` wrapper. EXPLODE math: `cumulativeX += max(width, length) × 1.25` per room, X-axis only. SOLO filters to `activeRoomId`. NORMAL renders all. Switching is INSTANT (no tween). Phase 46 hiddenIds composes per-room inside RoomGroup. Persists to `gsd:displayMode` localStorage.

### Phase 48: Per-Node Saved Camera + Focus Action (CAM-04)
**Goal:** Each placed product / wall / ceiling / custom-element can have a bookmarked camera angle saved via PropertiesPanel button. Double-click in the rooms tree dispatches the saved pose through Phase 46's pendingCameraTarget bridge (easeInOutCubic + reduced-motion snap inherited).
**Depends on:** Phase 35 preset-tween infrastructure (easeInOutCubic); Phase 46 (`pendingCameraTarget` bridge + tree click-to-focus); Phase 28 (autosave debounce — saves persist via existing snapshot path).
**Requirements:** CAM-04
**Plans:** 3/3 complete
- [x] 48-01-PLAN.md — Wave 0 RED scaffolding (3 vitest + 1 playwright + 2 source stubs at canonical paths)
- [x] 48-02-PLAN.md — Wave 1 cadStore type additions + 5 NoHistory setters + uiStore getCameraCapture bridge + ThreeViewport install
- [x] 48-03-PLAN.md — Wave 2 PropertiesPanel Save/Clear + TreeRow Camera icon + double-click dispatch + focusOnSavedCamera + savedCameraDrivers fill

**Pattern reuse:** Mirrors Phase 25/31 `_NoHistory` setter pattern (5 actions: 4 `setSavedCameraOn*NoHistory` + 1 `clearSavedCameraNoHistory`). Reuses Phase 46 `pendingCameraTarget` infrastructure — easeInOutCubic + reduced-motion snap inherited. lucide-only icons (D-33), zero arbitrary spacing (D-34).

## Carry-over

### Phase 999.4 — Saved camera EXPLODE-mode offset correction (medium severity)

When user saves a camera in NORMAL/SOLO and double-clicks the same node while in EXPLODE, focus tween targets stale world-space coordinates. Tracked in [GH #127](https://github.com/micahbank2/room-cad-renderer/issues/127). Narrow trigger; deferred to v1.12+ pending real-use signal.

### Tech debt (low severity, all in audit)

- AUDIT-02: Type-assertion cast in `focusDispatch.ts:16-32` (defensive dead code post-Plan 02 merge)
- AUDIT-03: Phase 46 VERIFICATION.md line refs are stale after Phase 47 RoomGroup refactor (doc only)
- AUDIT-04: No Phase 35 preset-tween e2e regression spec (carried from v1.10)

## Outcome

The "ship the competitive audit's 4 strongest items" thesis was validated:
- All 4 requirements shipped without scope creep
- Architectural surprise from Phase 47 (single-room → multi-room rendering refactor) absorbed cleanly
- Test pollution patterns surfaced and were fixed in-flight (vi.spyOn + Zustand stale state)
- Each phase shipped through the same loop: discuss → plan → verify-plan → execute → push → preview-test → merge
- 60+ commits with TDD discipline preserved (Wave 0 RED scaffolds → Wave N implementations)

The cross-phase EXPLODE+saved-camera gap is the only design hole the audit surfaced, and it's narrow enough to safely defer.

**Closes the v1.11 milestone with all features shipped, integrated, and merged to main.**
