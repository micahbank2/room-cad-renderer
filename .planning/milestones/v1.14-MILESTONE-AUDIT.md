---
milestone: v1.14
name: Real 3D Models
audited: 2026-05-04
status: SHIP
phases: [55, 56, 57, 58]
requirements: [GLTF-UPLOAD-01, GLTF-RENDER-3D-01, GLTF-RENDER-2D-01, GLTF-INTEGRATION-01]
---

# v1.14 Real 3D Models — Milestone Audit

## Status: SHIP

The biggest single user-visible win in project history shipped end-to-end. All four requirements satisfied, all four phase verifications PASSED (5/5 + 10/10 + 7/7 + 10/10), all wires connect, all 12 GLTF e2e scenarios + 1 Phase 48 regression pass on chromium-preview, zero new vitest failures, zero regressions on Phase 31/48/53/54/56/57.

## Requirements Coverage

| ID | Phase | Status | Evidence |
|----|-------|--------|----------|
| GLTF-UPLOAD-01 | 55 | ✅ ACCEPTED | 5/5 truths verified; `gltfStore.ts` (9 exports, SHA-256 dedup mirroring Phase 32 pattern); AddProductModal extended with `.gltf`/`.glb` accept + 25MB cap; 6 unit + 3 component + 2 e2e GREEN; `Product.gltfId?` survives snapshot round-trip. |
| GLTF-RENDER-3D-01 | 56 | ✅ ACCEPTED | 10/10 truths verified; `GltfProduct.tsx` (drei `useGLTF` + auto-scale + floor-align + bbox outline); `useGltfBlobUrl` ref-counted cache (cleanup order: `useGLTF.clear` then `revokeObjectURL`); ErrorBoundary → ProductBox fallback; 5 unit + 3 component + 4 e2e GREEN. |
| GLTF-RENDER-2D-01 | 57 | ✅ ACCEPTED | 7/7 truths verified; `gltfSilhouette.ts` (convex hull projection + per-gltfId cache + sentinel for failure); `fabricSync.ts` `gltfId` branch creates `fabric.Polygon`; image-only path gated `!product?.gltfId`; 6 unit + 4 e2e (8/8 across projects) GREEN. |
| GLTF-INTEGRATION-01 | 58 | ✅ ACCEPTED | 10/10 truths verified; `gltfThumbnailGenerator.ts` (256×256 PNG; FOV-based framing; full GPU disposal of 10 PBR maps on success+catch; "fallback" sentinel); `LibraryCard` `badge?` slot + lucide `Box` top-LEFT; `imageUrl > gltfId > undefined` priority (D-09); E1 saved-camera × GLTF round-trip; 14 unit + 3 e2e GREEN. |

All four marked `[x]` in REQUIREMENTS.md. No PARTIAL, no MISSED.

## Cross-Phase Wire Diagram

```
                     ┌─────────────────────────────┐
                     │  Phase 55: gltfStore.ts     │
                     │  saveGltfWithDedup, getGltf │
                     │  (IDB: room-cad-gltf-models)│
                     └──────────────┬──────────────┘
                                    │ getGltf(gltfId) → Blob
            ┌───────────────────────┼────────────────────────┐
            │                       │                        │
            ▼                       ▼                        ▼
  ┌──────────────────┐   ┌────────────────────┐   ┌────────────────────────┐
  │ Phase 56:        │   │ Phase 57:          │   │ Phase 58:              │
  │ useGltfBlobUrl   │   │ gltfSilhouette.ts  │   │ gltfThumbnailGenerator │
  │  → ObjectURL     │   │  → convexHull2D    │   │  → 256×256 PNG dataURL │
  │ GltfProduct.tsx  │   │ fabricSync.ts:940  │   │ ProductLibrary.tsx     │
  │  → useGLTF()     │   │  → fabric.Polygon  │   │  → LibraryCard.badge   │
  │ ProductMesh:     │   │ wrapped in Group   │   │  → Box icon (lucide)   │
  │  <group> wraps   │   │ data.placedProduct │   │ resolveThumbnail:      │
  │  → Phase 53/54   │   │ Id → Phase 53/54   │   │  imageUrl > gltfId     │
  │  click handlers  │   │  click in 2D       │   │  (no imageUrl write!)  │
  └──────────────────┘   └────────────────────┘   └────────────────────────┘
            │                       │                        │
            └─── Phase 31 resolveEffectiveDims() drives all three render paths
                 Phase 48 saved-camera writes pose by placedId (works on GLTF root group)
```

All seven cross-phase wires verified by grep + verification reports:
- `gltfStore.getGltf` consumed by 56 (`useGltfBlobUrl.ts`), 57 (`gltfSilhouette.ts`), 58 (`gltfThumbnailGenerator.ts`) ✓
- `Product.gltfId` flows through type → AddProductModal → ProductMesh / fabricSync / ProductLibrary ✓
- `PlacedProduct.id` → `data.placedProductId` on fabric.Group (57) AND wrapping `<group>` (56) → Phase 53 right-click + Phase 54 left-click dispatch ✓
- `resolveEffectiveDims` (Phase 31) drives auto-scale in `GltfProduct.tsx` AND `scaleHullToProduct` in `fabricSync.ts` ✓
- Phase 58 `resolveThumbnail` priority is render-time only — never writes back to `Product.imageUrl` (D-09 honored) ✓

## End-to-End Flow Trace

| Step | User action | Code path | Status |
|------|-------------|-----------|--------|
| 1 | Drag `.glb` into AddProductModal | `validateGltf` (size + ext) → `saveGltfWithDedup` → IDB write or dedup | ✅ |
| 2 | Submit product | `handleSubmit` spreads `gltfId` onto Product → productStore | ✅ |
| 3 | Library card renders | `resolveThumbnail` calls `getCachedGltfThumbnail` → async PNG dataURL; `<Box>` badge top-LEFT | ✅ |
| 4 | Place in 2D | `productTool` → cadStore `placedProducts.push` | ✅ |
| 5 | 2D silhouette renders | `renderProducts` → `getCachedSilhouette(gltfId)` → `fabric.Polygon` wrapped in Group | ✅ |
| 6 | Switch to 3D | `ProductMesh` branches on `gltfId` → Suspense → `<GltfProduct>` → drei `useGLTF` → `<primitive>` | ✅ |
| 7 | Right-click 3D model | `<group>` `onContextMenu` → Phase 53 menu opens with all 6 actions | ✅ |
| 8 | Click-to-select 2D polygon | Group's `data.placedProductId` → Phase 54 selection dispatch | ✅ |
| 9 | Drag edge handle | Phase 31 `resizeProductAxis` → `widthFtOverride` / `depthFtOverride` → `resolveEffectiveDims` → both renderers update | ✅ |
| 10 | Save camera angle | Phase 48 `__driveSaveCamera("product", placedId, ...)` → focus restores within 1-decimal tolerance (E1 e2e) | ✅ |

## Tech Debt + Deferred Items

### Pre-existing (NOT introduced by v1.14, baseline preserved)

- **4 vitest failures** — SaveIndicator, SidebarProductPicker, AddProductModal Skip-Dimensions, productStore pre-load mutation guard. Baseline unchanged across all four phases (55: 4 → 56: 4 → 57: 4 → 58: 4). None touch v1.14-modified files. **Recommend:** track as carry-over; do not block v1.14 ship.
- **Issue [#141](https://github.com/micahbank2/room-cad-renderer/issues/141)** — wall-user-texture-first-apply flake on chromium-dev. Filed today, pre-existing flake, NOT introduced by v1.14. Already labeled `bug` + `backlog`. No action needed.
- **6 prior pre-existing vitest failures (Phase 37 D-02 permanent)** + **CI vitest disabled (Phase 36-02)** — long-standing carry-over from v1.8.

### v1.14-specific carry-over

None. All four phases verified gap-free. No PARTIAL acceptances, no MISSED items.

### Out of Scope items customers will plausibly ask for in v1.15

| Item | Rationale for deferral |
|------|------------------------|
| OBJ format support | Older format, separate codepath. Defer until demand signals. |
| GLTF animations | Furniture rarely animated; static rendering covers Jessica's needs. |
| Custom material overrides on GLTF | Embedded PBR is sufficient; override is speculative until requested. |
| LOD / progressive loading | 25MB cap keeps load times reasonable; revisit if Jessica hits ceiling. |
| Cloud-hosted GLTF library | Storage scope expansion; v2.0+ territory. |
| Phase 999.4 EXPLODE+saved-camera offset | Carry-over from v1.11; narrow trigger; revisit later. |
| Phase 999.1 ceiling resize handles | Re-deferred from v1.9. |
| Phase 999.3 per-surface tile-size override | Re-deferred from v1.9. |
| R3F v9 / React 19 upgrade ([#56](https://github.com/micahbank2/room-cad-renderer/issues/56)) | Gated on R3F v9 stability. |

## Test Posture

| Suite | Phase 55 | Phase 56 | Phase 57 | Phase 58 | Total |
|-------|----------|----------|----------|----------|-------|
| Unit (vitest) | 6 | 5 | 6 | 4+4+6=14 | 31 GLTF-specific |
| Component (vitest) | 3 | 3 | — | included | — |
| E2E (Playwright chromium-preview) | 2 | 4 | 4 | 3 | **13 GLTF + 1 Phase 48 regression = 14** |
| Vitest baseline failures | 4 (unchanged) | 4 (unchanged) | 4 (unchanged) | 4 (unchanged) | Unchanged |

All e2e green on chromium-preview at every phase boundary. Phase 56→57 and 57→58 explicitly re-ran prior phase suites to confirm regression-clean.

## Overall Verdict: **SHIP**

v1.14 is the cleanest milestone in project history relative to scope. Four high-risk phases (IDB blob layer, drei async loading, Three.js → 2D projection, async thumbnail with WebGL context disposal) all shipped first-pass with PASSED verifications and no carry-over. Cross-phase wiring is correct in every direction. Jessica can upload a `.glb` chair, see it as a chair-shaped silhouette in 2D, switch to 3D and see the real model, save a camera angle on it, right-click it, resize it, and recognize it in the library by both the Box badge and an auto-rendered thumbnail — all without writing back to legacy `imageUrl`.

Tag `v1.14`, close the milestone on GitHub, archive the four phase directories, and queue v1.15 from the open backlog (issue #141 + the deferred items above are the natural starting set).

---

_Audited: 2026-05-04_
_Auditor: Claude (gsd-milestone-auditor)_
