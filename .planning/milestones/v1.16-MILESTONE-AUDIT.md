---
milestone: v1.16
name: Maintenance Pass
audited: 2026-05-04
status: passed-with-notes
phases: [63, 64, 65, 66]
requirements: [DEBT-06, BUG-04, CEIL-02, TILE-02]
backlog-closed: [Phase-999.1 (#70), Phase-999.3 (#105)]
---

# v1.16 Maintenance Pass — Milestone Audit

## Status: SHIP-WITH-NOTES

All 4 requirements shipped. Cross-phase integration clean (zero broken wires). Two long-parked Phase 999.x backlog items from v1.9 finally closed. One audit observation worth flagging: the `/gsd:quick` workflow used by 3 of 4 phases means only Phase 65 has a formal VERIFICATION.md — acceptable per workflow precedent (small surface area, known scope, full SUMMARY.md), but the milestone has lighter formal verification than v1.12 which used full `/gsd:execute-phase` for all 4 phases.

## Per-Requirement Verdict

| ID | Phase | Verdict | Evidence | Issue |
|----|-------|---------|----------|-------|
| **DEBT-06** | 63 | ✅ ACCEPTED (investigation outcome) | SUMMARY documents 3 deterministic full-suite runs; cascade did not reproduce; suspected polluter pattern (top-level `global.URL.createObjectURL` mutation) was a real code smell, replaced with proven `vi.spyOn` + `vi.restoreAllMocks` pattern in `pickerMyTexturesIntegration.test.tsx` + `myTexturesList.test.tsx`. | #146 (closed) |
| **BUG-04** | 64 | ✅ ACCEPTED | Two-part fix: (a) `WallMesh.tsx:223-230` useEffect cleanup added with identity-check `reg[wall.id] === matRefA.current`; (b) `e2e/wall-user-texture-first-apply.spec.ts:202` timeout 3000ms→8000ms. SUMMARY reports 10/10 green on `--repeat-each=5`. Same StrictMode-cleanup pattern class as Phase 58 thumbnail-callback bug. | #141 (closes on PR merge) |
| **CEIL-02** | 65 | ✅ ACCEPTED (formal verification PASS 13/13) | Full VERIFICATION.md PASS. 4-field override-anchor model (widthFtOverride, depthFtOverride, anchorXFt, anchorYFt). 13 vitest + 6 e2e + 26 regression e2e all green. Audit gates: snapEngine.ts + buildSceneGeometry.ts + types/product.ts all 0-diff vs origin/main. Single-undo verified via `__getCeilingHistoryLength` delta = 1. | #70 (closes on merge); 999.1 closed |
| **TILE-02** | 66 | ✅ ACCEPTED | UI surface only — Phase 42 had already shipped the data fields. `WallSurfacePanel.tsx:267-282` adds TILE SIZE input (visible when tiling on); `CeilingPaintSection.tsx:68-82` adds TILE SIZE input (visible when user-uploaded ceiling texture). Range clamp [0.5, 10] ft. No schema change, no new actions, no snapshot version bump. | #105 (closes on merge); 999.3 closed |

## Cross-Phase Wire Diagram

```
Phase 63 (test hygiene)
  └─> tests/pickerMyTexturesIntegration.test.tsx — vi.spyOn cleanup
  └─> tests/myTexturesList.test.tsx — vi.spyOn cleanup
  └─> NO source-tree changes — cannot break Phase 64/65/66 by construction

Phase 64 (BUG-04 wall-texture flake)
  └─> src/three/WallMesh.tsx (useEffect cleanup added)
  └─> e2e/wall-user-texture-first-apply.spec.ts (timeout bump)
  └─> Phase 65 + 66 do NOT touch WallMesh.tsx — cleanup preserved (verified
      `return () =>` at L229 and __wallMeshMaterials registry at L226)

Phase 65 (CEIL-02 ceiling resize)
  └─> src/types/cad.ts: 4 NEW fields on Ceiling (widthFtOverride, depthFtOverride,
      anchorXFt, anchorYFt) at L229-243
  └─> COEXISTS with Phase 42's `Ceiling.scaleFt` at L223 — no field collision
      (override fields control geometry; scaleFt controls texture repeat)
  └─> snapEngine.ts + buildSceneGeometry.ts: 0-diff vs origin/main (verified
      `git diff origin/main -- ...` returned 0 lines)
  └─> Reset action wires: PropertiesPanel RESET_SIZE + CanvasContextMenu
      "Reset size" → both call clearCeilingOverrides

Phase 66 (TILE-02 tile-size UI)
  └─> WallSurfacePanel.tsx writes Wallpaper.scaleFt (Phase 42 field)
  └─> CeilingPaintSection.tsx writes Ceiling.scaleFt (Phase 42 field)
  └─> Phase 65 fields (widthFtOverride etc.) are ON THE SAME Ceiling object
      but write a DIFFERENT property — verified at types/cad.ts L223 (scaleFt)
      vs L229-243 (override fields). PropertiesPanel CeilingDimInput (Phase 65)
      and CeilingPaintSection TILE SIZE input (Phase 66) target distinct fields,
      no race or stomp possible.
```

## Phase 999.x Backlog Items Closed

| Backlog Phase | Issue | Originally Parked | Shipped In | Wall-Clock to Promote |
|---------------|-------|-------------------|------------|----------------------|
| 999.1 (CEIL-02) | #70 | v1.9 (re-deferred from v1.13) | Phase 65 | ~10 min execute |
| 999.3 (TILE-02) | #105 | v1.9 | Phase 66 | ~5 min execute (smallest v1.16 phase) |

Two backlog items dating back to v1.9 — both shipped in v1.16. Maintenance-pass pattern continues to deliver: v1.12 cleared 4 items, v1.16 clears 4 items including 2 long-parked.

## Audit Gates (verified)

| Gate | Result | Source |
|------|--------|--------|
| `git diff origin/main -- src/canvas/snapEngine.ts src/canvas/buildSceneGeometry.ts` | **0 lines** | Phase 30 untouched (Phase 65 contract) |
| `git diff origin/main -- src/types/product.ts` | **0 lines** | Phase 31 product overrides untouched |
| Snapshot version literal `version: 5` | unchanged in cad.ts | Additive-optional fields per Phase 61 OPEN-01 precedent |
| WallMesh useEffect cleanup preserved | `return () =>` at L229 | Phase 64 fix not regressed |
| Ceiling.scaleFt + Ceiling.widthFtOverride coexist | both in cad.ts L223 / L229-243 | No field-name collision |

## Test-Coverage Status

| Phase | New Vitest | New E2E | Pre-existing Failures |
|-------|-----------|---------|----------------------|
| 63 | 0 (refactor only) | 0 | 4 baseline (per SUMMARY 3-run avg) |
| 64 | 0 | 0 (existing spec stabilized) | 4 baseline |
| 65 | 13/13 PASS | 6/6 + 26/26 regression | 4 baseline |
| 66 | 0 (rationale: 6-LOC clamp = over-test) | 0 | 4 baseline |

**Vitest re-run during audit:** `npx vitest run` reports `10 failed | 798 passed | 7 todo`. This matches the original audit-time observation that motivated DEBT-06. Phase 63's investigation finding still stands — the cascade comes and goes depending on test-file load ordering. The `vi.spyOn` cleanup hygiene is correct preventative work, but the underlying race (whichever it is) is **not yet eliminated**. Recommend re-opening #146 OR filing a follow-up DEBT-07 if the 10-failure mode reappears in a future PR audit. Not a v1.16 SHIP blocker — the pre-existing 4 baseline failures are stable, and the additional 6 are stderr-noise / module-singleton races in known-flaky paths, none in v1.16 code.

## Tech Debt + Deferred Items (call-outs)

| Item | Issue | Status |
|------|-------|--------|
| 4 pre-existing vitest failures (stable baseline) | — | Not v1.16-introduced; carry over. |
| Vitest 10-failure cascade reproducibility | #146 | Phase 63 fix shipped as preventative; cascade still observable in audit re-run. May warrant follow-up. |
| L-shape concave-room normal heuristic | — | Phase 65 proportional-bbox-scale works for L-shapes but extreme U/C shapes may misbehave. Flagged for v1.17. |
| `editStartedRef` StrictMode-cleanup pattern | — | Third instance (Phases 58 + 64 + 65). CLAUDE.md Pattern #7 captures it; no further action needed. |
| CAM-05 EXPLODE saved-camera offset | #127 | Re-deferred AGAIN (was on v1.16 candidate list, dropped during scoping). |
| Materials overhaul | #24-#28, #81 | v1.17 candidate. |
| Plain English documentation | #31, #32, #33 | v1.17 candidate (could be doc-only mini-milestone). |
| Columns + levels/platforms | #19 | v1.17 candidate. |
| Window presets | #20 | v1.17 candidate. |
| Per-opening saved-camera | — | v1.17 candidate. |
| 3D dimension billboards | — | v1.17 candidate. |

## Workflow Observation (non-blocking)

Phases 63, 64, 66 used `/gsd:quick` (no formal VERIFICATION.md; SUMMARY.md only). This is consistent with the workflow's documented use case ("known scope + small surface area") and was the right call — Phase 63 was a refactor, Phase 64 was a 2-file fix, Phase 66 was a 2-input UI add. Phase 65 (the only feature delivery) used full `/gsd:execute-phase` and produced a 13/13 PASS verification.

For comparison: v1.12 used `/gsd:execute-phase` for all 4 phases. v1.16 has lighter formal coverage but proportional to scope. Acceptable per workflow precedent — call out, do not fail.

## Overall Verdict: **SHIP-WITH-NOTES**

- All 4 requirements satisfied
- Cross-phase wires verified clean (snapEngine 0-diff, useEffect cleanup preserved, scaleFt + override fields coexist)
- 2 backlog items from v1.9 closed
- Phase 65's formal verification covers the only architecturally-significant change (4 new Ceiling fields, new selectTool drag branch, new CeilingMesh resolver)
- Notes (non-blocking): vitest 10-failure mode observable in audit re-run despite Phase 63 fix; revisit if it reappears in v1.17 PR audits

---

*Audited: 2026-05-04 by gsd-milestone-auditor*
