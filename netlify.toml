[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — all client-side routes (including /help/:section) should
# serve index.html so React Router can handle them.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
