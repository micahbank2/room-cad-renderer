import { describe, it } from "vitest";

describe("migrateSnapshot", () => {
  it.todo("v1 to v2 wraps legacy snapshot into Main Room");
  it.todo("v2 passthrough returns identical snapshot unchanged");
  it.todo("empty/unknown input returns default single-room snapshot");
  it.todo("legacy walls and placedProducts carry over into room_main");
});
